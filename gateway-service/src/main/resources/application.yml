server:
  port: 8080

spring:
  application:
    name: gateway-service
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true

gateway:
  security:
    white-list:
      - /api/auth/login
      - /api/auth/logout
      - /actuator/health
