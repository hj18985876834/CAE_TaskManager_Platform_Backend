package com.example.cae.common.enums;

public enum FailTypeEnum {
    VALIDATION_ERROR,
    DISPATCH_ERROR,
    NODE_OFFLINE,
    EXECUTOR_START_ERROR,
    RUNTIME_ERROR,
    TIMEOUT,
    MANUAL_CANCEL
}
