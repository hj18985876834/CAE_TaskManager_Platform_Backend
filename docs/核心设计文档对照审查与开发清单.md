# 核心设计文档对照审查与开发清单

## 1. 文档目的

本文档用于把最新设计基线与当前后端项目实现逐项对照，形成一份可持续维护的正式开发清单。后续每完成一个功能点，都应同步更新本文档中的：

- 对照结论
- 清单状态
- 变更记录

本轮审查重点不是重新做方案设计，而是确认“最新设计文档”与“当前代码实现”是否一致，并明确剩余差距。

---

## 2. 本轮审查基线

本轮以以下文档为准：

- [后端设计.md](../后端设计.md)
- [接口设计.md](../接口设计.md)
- [数据库设计.md](../数据库设计.md)
- [需求分析与系统设计.md](../需求分析与系统设计.md)
- [任务状态流转实现表.md](../任务状态流转实现表.md)
- [状态流转校验规则表.md](../状态流转校验规则表.md)

本轮同时对照以下实现主链路：

- `task-service`
- `scheduler-service`
- `node-agent`
- `common-lib`
- `docs/sql/init_databases.sql`

审查时间基线：`2026-04-22`

---

## 3. 本轮审查方法与覆盖矩阵

本轮不再采用“发现一个补一个”的零散方式，而是按固定维度逐项核对。审查时遵循以下原则：

- 先确定设计基线，再按维度检查实现
- 每个维度同时看“文档定义”“接口/字段/SQL”“实际代码行为”
- 新发现的问题必须落回本文档正式清单，避免只停留在对话结论
- 对已发现问题也要复核是否存在关联遗漏，防止只改表面不改链路

### 3.1 固定审查维度

为避免遗漏，后续所有审查统一按以下固定方向进行，不再采用“想到一个看一个”的方式。每个方向都必须同时核对：

- 设计文档正式契约
- 数据库/SQL/字段定义
- 接口路径、请求、返回、错误语义
- 实际代码行为与副作用

| 维度 | 覆盖内容 | 本轮已覆盖 |
| --- | --- | --- |
| D1 | 微服务职责边界与模块划分 | 是 |
| D2 | 前端业务接口路径、请求参数、返回字段、分页与筛选 | 是 |
| D3 | 服务内部接口、节点回调接口、内部 DTO 契约 | 是 |
| D4 | 网关路由、鉴权、白名单、角色边界与数据可见性 | 是 |
| D5 | SQL 表结构、索引、唯一约束、补丁脚本与实体/Mapper 一致性 | 是 |
| D6 | 任务状态机、状态流转校验、副作用、终态入口与幂等 | 是 |
| D7 | 上传、校验、解压、`unpackDir/workdir/taskDir` 目录合同 | 是 |
| D8 | 队列读取、优先级、选点、容量判断、预占、释放与调度记录 | 是 |
| D9 | 节点注册、心跳、能力同步、离线补偿与节点启停 | 是 |
| D10 | 日志、结果摘要、结果文件、终态回传的阶段校验与幂等 | 是 |
| D11 | 查询接口、聚合接口、排序规则、时间线展示与权限边界 | 是 |
| D12 | 驾驶舱统计、派生指标、`queueReason` 等解释字段语义 | 是 |
| D13 | JSON 字段、数据库字段、文档术语之间的命名与语义统一 | 是 |
| D14 | 多求解器扩展、模板驱动、模板合法性、反硬编码检查 | 是 |
| D15 | 历史兼容接口、重复入口、设计外暴露路径与遗留路由 | 是 |
| D16 | 错误码、冲突语义、幂等返回、审计/历史记录一致性 | 是 |

说明：

- `D6 ~ D10` 关注“任务主链路是否正确”；
- `D11 ~ D13` 关注“前后端联调和字段语义是否一致”；
- `D14 ~ D16` 关注“后续扩展是否可控、遗留口径是否清理、错误处理是否统一”。

### 3.2 本轮重点核对文件范围

除第 2 节列出的核心文档外，本轮重点复核了以下实现文件：

- `task-service`：`TaskLifecycleManager`、`TaskValidationManager`、`TaskDispatchManager`、`TaskResultManager`、`TaskQueryController`、`InternalTaskDispatchController`、`InternalTaskReportController`、相关 Response/Request/Mapper
- `scheduler-service`：`NodeAppService`、`ScheduleAppService`、`TaskScheduleJob`、`NodeHeartbeatChecker`、`NodeAgentController`、`InternalSchedulerController`、`InternalNodeReservationController`、相关 Response/Request/Mapper
- `node-agent`：`TaskExecuteManager`、`InputFilePrepareService`、`WorkDirManager`、`TaskReportClientImpl`、`CommandTemplateExecutor`、`CommandBuilder`
- `gateway-service`：`JwtAuthFilter`、`WhiteListConfig`、`GatewayRouteConfig`
- `solver-service`：`ProfileAppService`、`UploadSpecResponse`、相关控制器与内部接口

说明：

- 这意味着当前清单已经不只是“主流程能不能跑通”的检查，还覆盖了字段命名、兼容接口、错误码、幂等语义与模板校验合同。

---

## 4. 总体结论

当前项目已经完成了任务主链路的大部分收口，以下关键方向已与最新设计基本一致：

- 首版上传模式统一为 `ZIP_ONLY`
- 校验阶段负责解压到 `workdir`，节点执行优先复用已校验 `workdir`
- `compute_node.port` 已打通数据库、实体、注册、查询与调度派发链路
- 调度容量模型已统一为 `runningCount + reservedCount < maxConcurrency`
- `node_reservation` 已正式落库，说明项目已转入“预占明细 + 汇总计数”模式
- 任务查询层已支持 `queueReason`
- 任务详情、状态历史、调度记录已按独立接口拆分：`GET /api/tasks/{taskId}`、`GET /api/tasks/{taskId}/status-history`、`GET /api/tasks/{taskId}/schedules`
- 首版状态机已完成第一轮收紧：`status-report` 仅负责 `DISPATCHED -> RUNNING`，`mark-finished` 仅允许 `SUCCESS / TIMEOUT`，node-agent 不再主动回写 `DISPATCHED / CANCELED`

本轮累计识别出以下关键偏差。后续开发过程中已有多项完成收口，最新状态以第 8 节正式开发清单为准：

1. `reserve / release-reservation` 内部接口返回契约和冲突语义仍是旧口径
2. 节点离线补偿仍是“一刀切失败”，未按 `SCHEDULED/DISPATCHED` 与 `RUNNING` 分流
3. 日志与结果文件上报的幂等性仍未真正落地
4. 单任务调度记录查询顺序仍为倒序，和时间线设计不一致
5. 节点管理查询接口仍混用 `id` 与 `nodeId`
6. `dispatch-failed` 仍在 `task-service` 内部主动释放预占，和最新职责边界不一致
7. 调度链路当前“先标记 `DISPATCHED`、再真正下发节点”，时序语义偏前置
8. 节点日志/结果上报接口缺少状态阶段校验
9. 存在设计外重复暴露的管理员任务列表接口 `/api/tasks/admin`
10. 旧版 `running-count` 内部接口仍对外暴露
11. 个别核心设计文档示例与汇总段曾残留旧口径，已完成同步修订
12. 管理员调整优先级接口当前放宽到了 `SCHEDULED / DISPATCHED / RUNNING`，超出最新状态约束
13. `user-service`、`solver-service`、`profile-service` 仍保留设计文档外的 `PUT .../status` 兼容接口
14. `task_file` “同一任务仅保留一份有效输入压缩包”的约束当前仅靠应用层最佳努力，缺少事务与数据库级兜底
15. 创建任务接口尚未在写入前校验“求解器 / 模板 / 任务类型”三者合法性
16. `scheduler-service` 仍保留设计文档外的旧版节点注册/心跳内部入口 `/internal/scheduler/nodes/register|heartbeat`
17. `task-service` 仍依赖设计文档外的内部辅助接口 `/internal/tasks/{taskId}/status` 与 `/internal/tasks/node-offline/fail`
18. 模板参数结构 `paramsSchema/paramsSchemaJson` 虽已建模，但任务创建/校验阶段尚未真正执行参数 schema 校验
19. 任务文件校验 `issues[].errorCode` 体系与接口设计文档未统一，当前实现仍会回传文档外错误码
20. `mark-scheduled` 内部接口返回契约仍为布尔值，未按文档返回 `claimed/taskId/nodeId/status`
21. `status-report` 在携带 `fromStatus` 时的幂等处理仍不完整，重复 `DISPATCHED -> RUNNING` 回报可能被误判为冲突
22. 任务文件、结果文件、调度记录等响应字段仍混用 `id` 与文档命名的 `fileId/scheduleId`
23. `mark-failed` 仍允许通过 `failType=TIMEOUT` 直接写出 `TIMEOUT`，与最新状态流转规则冲突
24. `status-report / dispatch-failed / mark-finished / mark-failed` 内部接口返回契约仍未按核心接口契约总表收口
25. 节点离线补偿的调用顺序与返回计数仍和最新规则不一致
26. 驾驶舱 `avgNodeLoad` 统计口径仍沿用 `runningCount + reservedCount`，与最新文档定义不一致
27. 节点求解器启停接口仍返回 `Void`，未按接口设计返回 `nodeId/solverId/enabled`
28. 调度记录前端接口仍缺少 `taskNo`，且单任务调度记录查询权限边界未真正落地
29. 模板正式契约仍停留在文档层，`solver-service / node-agent` 尚未强制校验 `ZIP_ONLY / commandTemplate / ruleJson` 边界
30. 个别服务仍直接写 `400/409` 魔法值错误码，未完全收口到统一错误码常量与冲突语义

结论：当前后端已具备继续开发的清晰基线，但还不能认定为“与最新设计完全一致”。

---

## 5. 已对齐项

### 5.1 数据库与字段口径

已对齐：

- 总表数已按最新设计统一为 `15` 张
- `scheduler-service` 已按 `4` 张表收口：`compute_node`、`node_reservation`、`node_solver_capability`、`schedule_record`
- `compute_node.port` 已纳入 SQL、实体、Mapper、DTO 与注册请求
- `compute_node.running_count` 与 `compute_node.reserved_count` 的语义已经拆开
- `task_file.unpack_dir` 已用于记录校验后的执行目录
- `queueReason` 保持查询时动态派生，不落库

说明：

- `node_reservation` 已存在，说明“是否必须加这张表”的问题在当前架构下已经结束，现设计就是以该表为预占幂等真值。

### 5.2 任务状态机

已对齐：

- 取消了首版不允许的 `RUNNING -> CANCELED`
- `status-report` 已收窄为只允许 `DISPATCHED -> RUNNING`
- `mark-finished.finalStatus` 已收窄为 `SUCCESS / TIMEOUT`
- node-agent 不再主动回写 `DISPATCHED`
- `dispatch-failed` 请求已显式携带 `nodeId`
- `mark-dispatched` / `dispatch-failed` 已增加 `nodeId` 一致性校验

说明：

- 这意味着首版状态机主干已经从“宽松可运行”进入“按规则强约束”阶段，后续主要剩离线补偿与幂等细节收口。

### 5.3 任务文件校验与执行合同

已对齐：

- 任务上传后由后端校验阶段解压
- 解压目录统一落到任务 `workdir`
- 节点执行不再以“固定重新解压 ZIP”为默认路径
- 任务下发时会把 `unpackDir/workdir` 一并带给节点
- 主流程已改为模板驱动，不再围绕某一种求解器写死

说明：

- 当前“上传 ZIP -> 校验解压 -> 调度下发 -> 节点复用 workdir 执行”的目录合同已经基本明确，这与最新设计是一致的。

### 5.4 调度与查询层

已对齐：

- 调度策略对外标识统一为 `FCFS_MIN_LOAD`
- 任务详情接口已收口为基础详情字段，不再内嵌 `statusHistory / scheduleRecords`
- 状态历史与调度记录分别通过独立查询接口提供
- 任务列表 / 详情查询已支持 `queueReason`
- 节点列表、节点详情、可调度节点查询已支持 `reservedCount` 与有效负载展示

---

## 6. 关键偏差审查记录

说明：

- 本节保留本轮审查中识别出的关键偏差记录，便于追踪改造背景；
- 是否仍未收口，以第 8 节“最新正式开发清单”的状态为准。

### 6.1 偏差一：`reserve / release-reservation` 接口契约未按新设计收口

当前实现：

- `InternalNodeReservationController` 仍返回 `Result<Void>`
- `NodeAppService.releaseReservation(...)` 在“记录不存在”或“已释放”时直接静默返回
- `ScheduleAppService.releaseNodeReservation(...)` 也是静默返回

最新设计要求：

- `POST /internal/nodes/{nodeId}/reserve` 返回：
  - `taskId`
  - `nodeId`
  - `reservationStatus`
  - `reservedCount`
- `POST /internal/nodes/{nodeId}/release-reservation` 返回：
  - `taskId`
  - `nodeId`
  - `reservationStatus`
  - `reservedCount`
- 若释放时不存在对应 `node_reservation` 记录，应返回明确冲突或失败，不能静默成功
- 若已为 `RELEASED`，应按幂等成功返回当前状态与计数

影响：

- 当前代码虽然已经有 `node_reservation` 表，但内部接口契约仍停留在旧版“只调用、不反馈”的模式
- 这会导致前后服务无法严格按文档约定观察预占状态，也不利于后续联调和问题定位

涉及文件：

- `scheduler-service/src/main/java/com/example/cae/scheduler/interfaces/internal/InternalNodeReservationController.java`
- `scheduler-service/src/main/java/com/example/cae/scheduler/application/service/NodeAppService.java`
- `scheduler-service/src/main/java/com/example/cae/scheduler/application/service/ScheduleAppService.java`
- `接口设计.md`

结论：

- 这是当前新增 `node_reservation` 后最明显的接口-实现偏差，优先级应上调到 `P0`。

当前状态：

- `2026-04-22` 已完成：`reserve` / `release-reservation` 已返回 `taskId/nodeId/reservationStatus/reservedCount`，`release` 对“无记录”返回明确冲突，对“已释放”按幂等成功返回当前状态。

### 6.2 偏差二：节点离线补偿仍未按新版状态规则分流

当前实现：

- `NodeHeartbeatChecker.markOfflineNodes()` 直接调用 `taskClient.markNodeOfflineTasksFailed(...)`
- `TaskDispatchManager.markNodeOfflineTasksFailed(...)` 会把 `SCHEDULED / DISPATCHED / RUNNING` 全部统一改成 `FAILED`
- 同时直接写入 `failType = NODE_OFFLINE`
- `NodeAppService.markNodeOffline(...)` 会把节点 `reservedCount` 直接清零

最新设计要求：

- `SCHEDULED / DISPATCHED` 阶段优先按派发失败处理
  - 可恢复则回退 `QUEUED`
  - 不可恢复才转 `FAILED`
- 若最终回到 `QUEUED`，`NODE_OFFLINE` 只写状态历史或调度记录，不写回 `sim_task.fail_type`
- `RUNNING` 阶段任务才统一收口为 `FAILED(NODE_OFFLINE)`
- 离线补偿过程应与预占释放和 `node_reservation` 状态保持一致

影响：

- 当前补偿逻辑会把本应可回队的任务直接打成失败
- 任务主记录中的失败字段会被过早写死
- 节点侧 `reservedCount` 与 `node_reservation` 的关系在离线场景下也未完全按设计收口

涉及文件：

- `scheduler-service/src/main/java/com/example/cae/scheduler/infrastructure/support/NodeHeartbeatChecker.java`
- `scheduler-service/src/main/java/com/example/cae/scheduler/infrastructure/client/impl/TaskClientStub.java`
- `task-service/src/main/java/com/example/cae/task/application/manager/TaskDispatchManager.java`
- `需求分析与系统设计.md`
- `任务状态流转实现表.md`
- `状态流转校验规则表.md`

结论：

- 这是当前功能正确性风险最高的一项，优先级应保持 `P0/P1` 之间的最高级别，建议紧接着预占接口契约之后处理。

当前状态：

- `2026-04-22` 已完成第一轮收口：节点离线时，`SCHEDULED/DISPATCHED` 已改为回退 `QUEUED`，`RUNNING` 才收口为 `FAILED(NODE_OFFLINE)`，并去除了节点离线时直接清零 `reservedCount` 的旧实现。

### 6.3 偏差三：日志与结果文件幂等性未真正落地

当前实现：

- `TaskLogChunkMapper.insert(...)` 为普通插入
- SQL 中 `task_log_chunk` 只有 `uk_task_seq(task_id, seq_no)`，重复上报会直接异常，而不是“幂等忽略或覆盖”
- `TaskResultSummaryRepositoryImpl.saveOrUpdate(...)` 已基本满足 `taskId` 维度幂等
- `TaskResultFileRepositoryImpl.save(...)` 为普通插入
- `task_result_file` 当前没有 `task_id + file_type + file_name` 唯一约束

最新设计要求：

- 日志按 `taskId + seqNo` 幂等
- 结果摘要按 `taskId` 幂等覆盖或忽略
- 结果文件按 `taskId + fileType + fileName` 幂等

影响：

- 节点重复重试上报时，日志和结果文件无法稳定按设计处理
- 一旦网络抖动、节点补传或重复回传，会出现插入异常或重复文件记录

涉及文件：

- `task-service/src/main/java/com/example/cae/task/infrastructure/persistence/mapper/TaskLogChunkMapper.java`
- `task-service/src/main/java/com/example/cae/task/infrastructure/persistence/repository/TaskLogRepositoryImpl.java`
- `task-service/src/main/java/com/example/cae/task/infrastructure/persistence/mapper/TaskResultFileMapper.java`
- `task-service/src/main/java/com/example/cae/task/infrastructure/persistence/repository/TaskResultFileRepositoryImpl.java`
- `docs/sql/init_databases.sql`
- `数据库设计.md`

结论：

- 结果摘要这条线已基本正确，剩下的问题主要集中在日志和结果文件。

当前状态：

- `2026-04-22` 已完成：`task_log_chunk` 已按 `taskId + seqNo` 使用数据库级 upsert，`task_result_summary` 已按 `taskId` 使用数据库级 upsert，`task_result_file` 已补齐 `uk_task_file_type_name(task_id, file_type, file_name)` 并改为数据库级 upsert，同时补充了现网可执行补丁脚本。

### 6.4 偏差四：单任务调度记录查询顺序与设计不一致

当前实现：

- `ScheduleRecordMapper.selectByTaskId(...)` 使用 `ORDER BY id DESC`

最新设计要求：

- 单任务调度记录应按时间线升序返回，便于前端直接渲染

影响：

- 前端任务详情页若直接按后端结果展示，调度时间线会倒序

涉及文件：

- `scheduler-service/src/main/java/com/example/cae/scheduler/infrastructure/persistence/mapper/ScheduleRecordMapper.java`
- `接口设计.md`

结论：

- 该问题不影响主流程正确性，但会影响展示合同，优先级为 `P1`。

当前状态：

- `2026-04-22` 已完成：`schedule_record` 按 `created_at ASC, id ASC` 返回单任务调度记录，已与接口设计中“时间线升序展示”的口径保持一致。

### 6.5 偏差五：节点管理查询接口仍混用 `id` 与 `nodeId`

当前实现：

- `NodeListItemResponse` 同时包含 `id` 与 `nodeId`
- `NodeDetailResponse` 同时包含 `id` 与 `nodeId`
- `NodeAssembler` 与 `NodeAppService` 都在同时设置这两个字段

最新设计要求：

- 节点管理对前端的 JSON 响应统一只使用 `nodeId`
- 不应在接口层对前端同时暴露 `id` 和 `nodeId`

影响：

- 前后端容易在节点主键字段上继续形成双口径
- 后续接口文档、前端页面和调试日志会出现“有时看 `id`，有时看 `nodeId`”的漂移

涉及文件：

- `scheduler-service/src/main/java/com/example/cae/scheduler/interfaces/response/NodeListItemResponse.java`
- `scheduler-service/src/main/java/com/example/cae/scheduler/interfaces/response/NodeDetailResponse.java`
- `scheduler-service/src/main/java/com/example/cae/scheduler/application/assembler/NodeAssembler.java`
- `scheduler-service/src/main/java/com/example/cae/scheduler/application/service/NodeAppService.java`
- `接口设计.md`

结论：

- 这是一个典型的“功能可用但响应口径不统一”问题，应纳入正式清单。

当前状态：

- `2026-04-22` 已完成：节点列表与节点详情响应已移除兼容 `id` 字段，对外统一仅返回 `nodeId`，与接口设计和路径语义保持一致。

### 6.6 偏差六：`dispatch-failed` 释放预占职责边界未完全收口

当前实现：

- `TaskDispatchManager.markFailed(...)` 在状态回写后会调用 `releaseReservationQuietly(...)`
- 同时 `TaskScheduleJob` 在派发失败路径上也会调用 `taskScheduleManager.releaseNodeReservation(...)`

最新设计要求：

- `dispatch-failed` 只负责任务状态回写
- 调度失败场景下的预占释放应由 `scheduler-service` 单独调用 `/internal/nodes/{nodeId}/release-reservation`
- `task-service` 不应在 `dispatch-failed` 内部再次承担预占释放职责

影响：

- 当前虽然借助 `node_reservation` 幂等可以避免重复减计数，但职责边界已经偏离最新设计
- 后续联调时容易误判“释放动作到底应该由谁发起”

涉及文件：

- `task-service/src/main/java/com/example/cae/task/application/manager/TaskDispatchManager.java`
- `scheduler-service/src/main/java/com/example/cae/scheduler/application/scheduler/TaskScheduleJob.java`
- `接口设计.md`
- `任务状态流转实现表.md`

结论：

- 这是 `node_reservation` 引入后第二个需要收口的职责边界问题，优先级应不低于 `P1`。

当前状态：

- `2026-04-22` 已完成：`dispatch-failed` 已移除 task-service 内部的预占释放动作，派发失败链路中的节点预占释放现统一由 scheduler-service 显式调用 `/internal/nodes/{nodeId}/release-reservation` 承担。

### 6.7 偏差七：`mark-dispatched` 的触发时序与返回契约仍有偏差

当前实现：

- `TaskScheduleJob` 当前流程是先调用 `markTaskDispatched(...)`
- 然后再调用 `nodeAgentClient.notifyDispatch(...)`
- `mark-dispatched` 当前返回 `Result<Void>`

最新设计要求：

- 调度流程文档描述为：先下发 node-agent，再把任务确认写为 `DISPATCHED`
- `mark-dispatched` 在接口设计中给出了显式返回体：
  - `taskId`
  - `nodeId`
  - `status = DISPATCHED`

影响：

- 当前状态语义偏前置，会让“已下发”与“节点已接受”之间的边界不够清晰
- 返回体仍较弱，不利于联调时直接确认任务状态

涉及文件：

- `scheduler-service/src/main/java/com/example/cae/scheduler/application/scheduler/TaskScheduleJob.java`
- `scheduler-service/src/main/java/com/example/cae/scheduler/infrastructure/client/impl/NodeAgentClientStub.java`
- `task-service/src/main/java/com/example/cae/task/interfaces/internal/InternalTaskDispatchController.java`
- `接口设计.md`

结论：

- 这项不一定要立刻改代码，也可以回到文档统一口径，但必须进清单，避免后续又出现“代码按一种语义、文档按另一种语义”。

### 6.8 偏差八：节点日志/结果上报接口缺少状态阶段校验

当前实现：

- `InternalTaskReportController` 的 `log-report / result-summary-report / result-file-report` 只做了 `nodeToken` 与 `nodeId` 校验
- `TaskResultManager.appendLog(...)`、`saveResultSummary(...)`、`saveResultFile(...)` 没有校验任务当前状态
- 因此当前节点理论上可以在未进入 `RUNNING` 时先写入结果摘要或结果文件

最新设计要求：

- `log-report` 允许在 `DISPATCHED / RUNNING / 终态` 阶段接收
- `result-summary-report` 只允许在 `RUNNING / 终态` 阶段接收
- `result-file-report` 只允许在 `RUNNING / 终态` 阶段接收

影响：

- 当前实现无法阻止节点在错误的状态阶段提前写入结果
- 这会削弱“状态机可解释性”，并且可能让审计上出现“未运行先有结果”的脏数据

当前状态：

- `2026-04-22` 已完成：`TaskResultManager` 已补齐状态阶段校验，`log-report` 仅允许 `DISPATCHED / RUNNING / 终态`，`result-summary-report` 与 `result-file-report` 仅允许 `RUNNING / 终态`，并通过 `mvn -pl task-service -am -DskipTests compile`。

涉及文件：

- `task-service/src/main/java/com/example/cae/task/interfaces/internal/InternalTaskReportController.java`
- `task-service/src/main/java/com/example/cae/task/application/manager/TaskResultManager.java`
- `接口设计.md`
- `状态流转校验规则表.md`

结论：

- 这是一个之前未纳入清单、但与最新状态校验规则直接相关的遗漏，应补入正式开发清单。

### 6.9 偏差九：存在设计外重复暴露的管理员任务列表接口 `/api/tasks/admin`

当前实现：

- `TaskQueryController` 暴露了 `GET /api/tasks/admin`
- `AdminTaskController` 同时也暴露了正式管理员接口 `GET /api/admin/tasks`
- 其中 `/api/tasks/admin` 不在最新接口设计文档中

最新设计要求：

- 管理员任务列表正式接口为 `GET /api/admin/tasks`
- 普通用户任务列表接口为 `GET /api/tasks`

影响：

- 形成了一个设计外的重复接口
- 如果网关权限控制按路径前缀或接口清单做区分，这个重复接口可能形成实际的权限绕过风险
- 即使当前没有直接造成越权，也会让后续前后端和接口文档继续出现双口径

涉及文件：

- `task-service/src/main/java/com/example/cae/task/interfaces/controller/TaskQueryController.java`
- `task-service/src/main/java/com/example/cae/task/interfaces/controller/AdminTaskController.java`
- `接口设计.md`

结论：

- 这是本轮审查新增发现的高价值问题，至少应将其纳入清单并明确收口策略。

### 6.10 偏差十：旧版 `running-count` 内部接口仍对外暴露

当前实现：

- `InternalSchedulerController` 仍暴露 `POST /internal/nodes/{nodeId}/running-count`
- `NodeAppService.updateRunningCount(...)` 已被标记为兼容性空实现

最新设计要求：

- `runningCount` 只由节点心跳维护
- 不再建议通过 `running-count +1/-1` 同时承担真实运行数与调度预占数职责

影响：

- 虽然当前接口已空实现，不会直接破坏数据
- 但该接口仍然对外可见，容易给后续开发或联调造成误导，延续旧口径

涉及文件：

- `scheduler-service/src/main/java/com/example/cae/scheduler/interfaces/internal/InternalSchedulerController.java`
- `scheduler-service/src/main/java/com/example/cae/scheduler/application/service/NodeAppService.java`
- `接口设计.md`

结论：

- 这是一个低优先级但应记录的遗留兼容接口问题，建议纳入收尾清理项。

### 6.11 偏差十一：设计文档曾有少量旧示例与汇总段未同步

当前状态：

- 已于 `2026-04-22` 完成文档收口，保留本节作为历史审查记录。

历史发现：

- `数据库设计.md` 的初始化模板示例曾残留旧命令示例
- `接口设计.md` 的模板接口段曾存在重复约束小节
- `docs/sql/init_databases.sql` 曾残留复杂 `command_template` 与复杂 `rule_json` 示例
- `数据库设计.md`、`需求分析与系统设计.md` 的汇总段曾存在 `node_reservation` 口径同步风险

最新设计要求：

- 文档示例必须和当前模板驱动、`node_reservation` 引入后的设计保持一致

修订结果：

- 四份核心设计文档已统一为“轻模板 + 适配器承担共性逻辑”的模板设计基线
- `接口设计.md` 的模板配置约束已收口为单一小节，避免前后端理解分叉
- `docs/sql/init_databases.sql` 已同步改为轻量命令模板和简单文件规则示例

涉及文件：

- `数据库设计.md`
- 其他与之引用口径相同的总结性文档

结论：

- 该项已完成文档同步清理。后续如新增求解器或上传方式，必须同步更新设计文档、接口文档、数据库示例和适配器实现，避免只在数据库中形成隐式逻辑。

### 6.12 偏差十二：管理员调整优先级的状态边界仍宽于最新设计

当前实现：

- `TaskLifecycleManager.adjustPriority(...)` 仅禁止终态任务调整优先级
- 因此当前 `SCHEDULED / DISPATCHED / RUNNING` 任务也允许被管理员直接改优先级

最新设计要求：

- `POST /api/admin/tasks/{taskId}/priority` 只允许作用于 `CREATED / VALIDATED / QUEUED`
- 对已进入调度或执行阶段的任务，应返回状态冲突，而不是继续修改

影响：

- 会让“优先级仅影响排队与调度顺序”的设计语义被打破
- 前后端和状态流转校验规则表会出现分叉，联调时容易出现“文档不允许、接口却成功”的漂移

涉及文件：

- `task-service/src/main/java/com/example/cae/task/application/manager/TaskLifecycleManager.java`
- `状态流转校验规则表.md`
- `接口设计.md`

结论：

- 这是一个此前未纳入清单的真实状态边界偏差，应纳入正式开发清单。

当前状态：

- `2026-04-22` 已完成：`TaskLifecycleManager.adjustPriority(...)` 已收紧为仅允许 `CREATED / VALIDATED / QUEUED` 调整优先级，`SCHEDULED / DISPATCHED / RUNNING / 终态` 将返回 `TASK_PRIORITY_UPDATE_NOT_ALLOWED`，并通过 `mvn -pl task-service -am -DskipTests compile`。

### 6.13 偏差十三：多个服务仍保留文档外的 `PUT .../status` 兼容接口

当前实现：

- `user-service` 同时暴露：
  - `PUT /api/users/{id}/status`
  - `POST /api/users/{id}/status`
- `solver-service` 同时暴露：
  - `PUT /api/solvers/{solverId}/status`
  - `POST /api/solvers/{solverId}/status`
- `profile-service` 同时暴露：
  - `PUT /api/profiles/{profileId}/status`
  - `POST /api/profiles/{profileId}/status`

最新设计要求：

- 最新接口设计文档统一收口为 `POST .../status`
- 不应继续保留对前端与联调文档不可见的重复兼容入口

影响：

- 外部接口再次形成“文档一个口径、实现两个口径”的漂移
- 网关权限、前端封装、接口联调和论文描述都会被不必要地复杂化

涉及文件：

- `user-service/src/main/java/com/example/cae/user/interfaces/controller/UserController.java`
- `solver-service/src/main/java/com/example/cae/solver/interfaces/controller/SolverController.java`
- `solver-service/src/main/java/com/example/cae/solver/interfaces/controller/ProfileController.java`
- `接口设计.md`

结论：

- 这是又一类设计外兼容接口残留，优先级不如主链路状态机问题高，但必须入清单统一收口。

### 6.14 偏差十四：`task_file` 的单有效压缩包约束尚未被强约束落地

当前实现：

- `TaskLifecycleManager.uploadTaskFile(...)` 会尝试查找并替换现有 `ARCHIVE + input_archive` 记录
- 但该流程当前未显式事务包裹
- `task_file` 表结构也没有 `task_id + file_role + file_key` 唯一约束

最新设计要求：

- 同一任务当前有效的输入压缩包只能有一份
- 用户重新上传时应覆盖或替换原有 `ARCHIVE + input_archive` 记录，而不是追加多条有效记录

影响：

- 在并发上传、重复点击或请求重放场景下，当前实现仍可能插入多条同类 `task_file`
- 一旦出现多条 archive 记录，后续校验、解压、重试和目录清理逻辑都会重新变得不确定

涉及文件：

- `task-service/src/main/java/com/example/cae/task/application/manager/TaskLifecycleManager.java`
- `task-service/src/main/java/com/example/cae/task/infrastructure/persistence/mapper/TaskFileMapper.java`
- `docs/sql/init_databases.sql`
- `数据库设计.md`

结论：

- 这不是“功能现在完全不可用”的问题，但属于数据库约束与幂等兜底缺失，应该纳入正式清单。

当前状态：

- `2026-04-22` 已完成：`uploadTaskFile(...)` 已加事务，`task_file` 已按 `(task_id, file_role, file_key)` 落地数据库级唯一约束与 upsert 兜底，并补充现网补丁脚本 `patch_task_file_archive_unique_constraint.sql`，通过 `mvn -pl task-service -am -DskipTests compile`。

### 6.15 偏差十五：创建任务接口尚未在落库前校验求解器 / 模板 / 任务类型合法性

当前实现：

- `TaskLifecycleManager.createTask(...)` 当前直接把请求写入 `sim_task`
- 没有在创建时校验：
  - `solverId` 是否存在
  - `profileId` 是否存在
  - `profileId` 是否属于该 `solverId`
  - `taskType` 是否与模板定义一致

最新设计要求：

- `POST /api/tasks` 的前置条件已明确写为“求解器、模板、任务类型有效”
- 用户在创建阶段应只能形成合法的“求解器 + 模板 + 任务类型”组合

影响：

- 当前后端允许写入一条直到校验阶段才暴露为非法的任务记录
- 这会削弱接口语义，也会让“创建成功”与“业务上其实不合法”并存，和设计文档不一致

涉及文件：

- `task-service/src/main/java/com/example/cae/task/application/manager/TaskLifecycleManager.java`
- `task-service/src/main/java/com/example/cae/task/interfaces/request/CreateTaskRequest.java`
- `接口设计.md`
- `状态流转校验规则表.md`

结论：

- 这是本轮新增确认的输入合法性缺口，建议纳入正式开发清单。

当前状态：

- `2026-04-22` 已完成：`createTask(...)` 已前置校验 `solverId / profileId / taskType` 合法组合，收口到“求解器存在且启用、模板存在且启用、模板归属求解器正确、taskType 与模板一致”，并通过 `mvn -pl task-service -am -DskipTests compile`。

### 6.16 偏差十六：`scheduler-service` 仍保留旧版节点注册/心跳内部入口

当前实现：

- 正式节点接入入口已经是：
  - `POST /api/node-agent/register`
  - `POST /api/node-agent/heartbeat`
- 但当前 `scheduler-service` 仍额外暴露：
  - `POST /internal/scheduler/nodes/register`
  - `POST /internal/scheduler/nodes/heartbeat`

最新设计要求：

- 节点代理接入路径应统一收口到 `NodeAgentController`
- 设计文档没有再保留这组旧版内部节点接入接口

影响：

- 容易让部署脚本、联调文档和后续开发重新混用两套节点接入口径
- 与此前发现的 `running-count`、`/api/tasks/admin` 类似，都属于设计文档外的历史接口残留

涉及文件：

- `scheduler-service/src/main/java/com/example/cae/scheduler/interfaces/controller/NodeAgentController.java`
- `scheduler-service/src/main/java/com/example/cae/scheduler/interfaces/internal/NodeRegisterController.java`
- `scheduler-service/src/main/java/com/example/cae/scheduler/interfaces/internal/NodeHeartbeatController.java`
- `接口设计.md`

结论：

- 这是本轮扫尾审查新增确认的遗留入口问题，应并入正式清单，避免后续继续混用。

---

### 6.17 偏差十七：`task-service` 仍依赖设计文档外的内部辅助接口

当前实现：

- `InternalTaskDispatchController` 仍额外暴露：
  - `GET /internal/tasks/{taskId}/status`
  - `POST /internal/tasks/node-offline/fail`
- `scheduler-service` 中 `TaskClientStub` 仍直接依赖这两个接口：
  - 调度失败兜底时读取任务当前状态
  - 节点离线补偿时直接触发“一刀切失败”

最新设计要求：

- 当前版本的核心内部接口应以：
  - `queued`
  - `mark-scheduled`
  - `mark-dispatched`
  - `dispatch-failed`
  - `status-report`
  - `mark-finished`
  - `mark-failed`
  - `log-report/result-report`
  为主链路
- 若确实需要额外内部辅助接口，应同步进入接口设计文档，而不是只存在于实现中

影响：

- 现在 scheduler-service 与 task-service 之间仍存在“代码依赖但文档未声明”的隐式合同
- 后续若仅按文档改造内部链路，容易误删或忽略这些入口，导致联调回归

涉及文件：

- `task-service/src/main/java/com/example/cae/task/interfaces/internal/InternalTaskDispatchController.java`
- `scheduler-service/src/main/java/com/example/cae/scheduler/infrastructure/client/impl/TaskClientStub.java`
- `接口设计.md`

结论：

- 这是本轮按“内部接口维度”复核后新增确认的问题，必须明确为“补文档保留”还是“改链路后删除”，不能继续隐式存在。

当前状态：

- `2026-04-22` 已完成：已移除 `scheduler-service` 对 `GET /internal/tasks/{taskId}/status` 的实际依赖，并将 `dispatch-failed` 收口为“迟到失败回写可幂等忽略”；`POST /internal/tasks/node-offline/fail` 已正式补入 `接口设计.md`，并通过 `mvn -pl scheduler-service,task-service -am -DskipTests compile`。

### 6.18 偏差十八：模板参数结构 `paramsSchema` 尚未真正进入任务校验链路

当前实现：

- `solver-service` 已维护并对外返回：
  - `paramsSchema`
  - `paramsSchemaJson`
- 但 `task-service` 中：
  - `createTask(...)` 未校验 `params`
  - `updateTask(...)` 未校验 `params`
  - `validateTask(...)` 也未按模板参数 schema 校验 `params`
- 代码中没有落地 `PARAM_SCHEMA_INVALID`

最新设计要求：

- 模板不仅定义文件规则，也定义参数结构约束
- `params` 不满足模板 schema 时，应按统一校验语义拒绝，并能产出 `PARAM_SCHEMA_INVALID`

影响：

- 当前后端实际上只做了“文件模板校验”，没有做“参数模板校验”
- 这会导致用户可以提交结构不合法的 `params`，与最新“模板驱动”的设计思想不一致

涉及文件：

- `task-service/src/main/java/com/example/cae/task/application/manager/TaskLifecycleManager.java`
- `task-service/src/main/java/com/example/cae/task/application/manager/TaskValidationManager.java`
- `solver-service/src/main/java/com/example/cae/solver/application/service/ProfileAppService.java`
- `接口设计.md`
- `需求分析与系统设计.md`

结论：

- 这是模板驱动方案中的关键缺口，优先级应至少提升到 `P1`。

当前状态：

- `2026-04-22` 已完成：新增统一参数 schema 校验组件；`createTask(...)`、`updateTask(...)` 已按模板做部分参数校验，`validateTask(...)` 已在文件派生参数后做完整参数校验，并统一产出 `PARAM_SCHEMA_INVALID`。

### 6.19 偏差十九：任务文件校验错误码体系与接口设计文档未统一

当前实现：

- 接口文档当前定义了一组稳定的 `issues[].errorCode`
- 但 `TaskValidationManager` 仍会产出文档外错误码，例如：
  - `UNSUPPORTED_ARCHIVE_FORMAT`
  - `DERIVED_PARAM_CONFIG_INVALID`
  - `DERIVED_PARAM_CONFLICT`
  - `DERIVED_PARAM_INVALID`
  - `DERIVED_PARAM_MATCH_FAILED`
  - `DERIVED_PARAM_PATTERN_INVALID`
  - `DERIVED_PARAM_PREPROCESS_INVALID`
  - `DERIVED_PARAM_SOURCE_MISSING`
  - `DERIVED_PARAM_SOURCE_UNREADABLE`
  - `DERIVED_PARAM_SOURCE_UNSUPPORTED`
- 与此同时，文档中已定义的：
  - `PARAM_SCHEMA_INVALID`
  - `ARCHIVE_TOO_LARGE`
  当前主校验链路并未以同样方式稳定产出

最新设计要求：

- 前后端应围绕一套稳定、可枚举的错误码做联调
- 若确实需要新增推导类错误码，也应同步修订接口设计文档

影响：

- 前端无法只根据设计文档完成稳定的错误提示映射
- 后续论文、接口联调与异常处理说明会再次出现“文档一套、实现一套”的漂移

涉及文件：

- `task-service/src/main/java/com/example/cae/task/application/manager/TaskValidationManager.java`
- `接口设计.md`

结论：

- 这是“模板校验已变复杂，但错误码合同未同步”的典型问题，应纳入正式清单统一收口。

当前状态：

- `2026-04-22` 已完成：移除了文档外的 `UNSUPPORTED_ARCHIVE_FORMAT` 与各类 `DERIVED_PARAM_*` 错误码，统一映射为 `INVALID_ARCHIVE_SUFFIX`、`TEMPLATE_RULE_CONFLICT`、`PARAM_SCHEMA_INVALID`，并在校验链路补齐了 `ARCHIVE_TOO_LARGE` 的稳定产出。

### 6.20 偏差二十：`mark-scheduled` 内部接口返回契约仍为旧版布尔值

当前实现：

- `InternalTaskDispatchController.markScheduled(...)` 当前返回 `Result<Boolean>`
- `scheduler-service` 的 `TaskClientStub.markTaskScheduled(...)` 也只消费布尔值

最新设计要求：

- `POST /internal/tasks/{taskId}/mark-scheduled` 应返回至少包含：
  - `claimed`
  - `taskId`
  - `nodeId`
  - `status`
- 这样才能把“成功抢占调度权”和“状态已变化未抢占成功”区分清楚

影响：

- 当前虽然能工作，但调度冲突语义被压缩成单一布尔值
- 不利于后续日志审计、问题定位与内部接口统一

涉及文件：

- `task-service/src/main/java/com/example/cae/task/interfaces/internal/InternalTaskDispatchController.java`
- `task-service/src/main/java/com/example/cae/task/application/manager/TaskDispatchManager.java`
- `scheduler-service/src/main/java/com/example/cae/scheduler/infrastructure/client/impl/TaskClientStub.java`
- `接口设计.md`

结论：

- 这是本轮按“内部返回字段合同”复核后新增发现的问题，应作为 `P1/P2` 级接口收口项处理。

当前状态：

- `2026-04-22` 已完成：`mark-scheduled` 已改为返回结构化 `claimed/taskId/nodeId/status`，`scheduler-service` 已同步消费该合同，并通过 `mvn -pl scheduler-service,task-service -am -DskipTests compile`。

### 6.21 偏差二十一：`status-report` 在携带 `fromStatus` 时的幂等处理仍不完整

当前实现：

- `TaskLifecycleManager.reportStatus(...)` 先校验：
  - `request.fromStatus == task.currentStatus`
- 然后才判断“若当前已是 `RUNNING` 则幂等返回”
- 这意味着当节点或中间链路按文档重试：
  - `fromStatus = DISPATCHED`
  - `toStatus = RUNNING`
  但任务已在上一次请求中成功变为 `RUNNING` 时，请求会先命中 `TASK_STATUS_MISMATCH`

最新设计要求：

- 对同一 `taskId + nodeId` 的重复 `RUNNING` 回传应按幂等成功处理
- 文档允许 `fromStatus` 作为状态校验辅助信息存在，不应因此破坏已进入目标状态后的重试幂等

影响：

- 当前实现对“严格按文档回传 `fromStatus` 的节点重试”不够稳健
- 这类问题平时不一定暴露，但一旦出现网络重试或代理补发，就会造成不必要冲突

涉及文件：

- `task-service/src/main/java/com/example/cae/task/application/manager/TaskLifecycleManager.java`
- `task-service/src/main/java/com/example/cae/task/interfaces/request/StatusReportRequest.java`
- `接口设计.md`
- `状态流转校验规则表.md`

结论：

- 这是本轮按“状态回传幂等”维度复核后新增确认的问题，应进入正式清单。

当前状态：

- `2026-04-22` 已完成：`status-report` 已先处理“同节点重复回传 `RUNNING`”的幂等成功，再进行 `fromStatus` 严格比对，避免合法重试被误判为 `TASK_STATUS_MISMATCH`。

### 6.22 偏差二十二：多个响应 DTO 仍混用 `id` 与文档命名字段

当前实现：

- `TaskFileResponse` 只返回 `id`，未按接口示例使用 `fileId`
- `TaskResultFileResponse` 只返回 `id`，未按接口示例使用 `fileId`
- `TaskScheduleRecordResponse` 只返回 `id`，未按调度记录接口口径统一为 `scheduleId`
- `ScheduleRecordResponse` 虽同时返回 `id` 与 `scheduleId`，但任务详情聚合对象仍只有 `id`

最新设计要求：

- 接口文档当前已经在多个地方明确使用：
  - `fileId`
  - `scheduleId`
  - `nodeId`
  - `taskId`
- 前后端应收口到一套稳定命名，而不是在不同接口中混用“通用 id”和“业务 id”

影响：

- 会让前端类型定义、接口封装与文档说明继续分裂
- 这类问题不一定阻断流程，但会持续制造联调摩擦

涉及文件：

- `task-service/src/main/java/com/example/cae/task/interfaces/response/TaskFileResponse.java`
- `task-service/src/main/java/com/example/cae/task/interfaces/response/TaskResultFileResponse.java`
- `task-service/src/main/java/com/example/cae/task/interfaces/response/TaskScheduleRecordResponse.java`
- `scheduler-service/src/main/java/com/example/cae/scheduler/interfaces/response/ScheduleRecordResponse.java`
- `接口设计.md`

结论：

- 这是字段统一性层面的新增遗漏，建议纳入正式清单，在最终联调前统一收口。

当前状态：

- `2026-04-22` 已完成：`TaskFileResponse`、`TaskResultFileResponse`、`TaskScheduleRecordResponse`、`ScheduleRecordResponse` 已统一收口到 `fileId/scheduleId` 字段口径，相关聚合与远程消费链路已同步调整，并通过 `mvn -pl scheduler-service,task-service -am -DskipTests compile`。

### 6.23 偏差二十三：`mark-failed` 仍允许通过 `failType=TIMEOUT` 写出 `TIMEOUT`

当前实现：

- `TaskResultManager.failTask(...)` 会在 `failType = TIMEOUT` 时把目标状态改写为 `TIMEOUT`
- `MarkFailedRequest` 只要求 `failType` 非空，没有禁止 `TIMEOUT`

最新设计要求：

- `TIMEOUT` 只能通过 `POST /internal/tasks/{taskId}/mark-finished` 且 `finalStatus=TIMEOUT` 产生
- `POST /internal/tasks/{taskId}/mark-failed` 首版只允许 `RUNNING -> FAILED`

影响：

- 会重新引入“未按正式终态接口走 TIMEOUT”这类歧义
- 状态流转规则表、接口总规约和真实代码会再次分叉

涉及文件：

- `task-service/src/main/java/com/example/cae/task/application/manager/TaskResultManager.java`
- `task-service/src/main/java/com/example/cae/task/interfaces/request/MarkFailedRequest.java`
- `接口设计.md`
- `任务状态流转实现表.md`
- `状态流转校验规则表.md`

结论：

- 这是状态机硬约束层面的新偏差，应按 `P0` 收口。

当前状态：

- `2026-04-22` 已完成：`mark-failed` 已固定只产出 `FAILED`，并显式拒绝 `failType=TIMEOUT`；`TIMEOUT` 仅允许通过 `mark-finished(finalStatus=TIMEOUT)` 进入。

### 6.24 偏差二十四：多个内部状态回写接口仍未返回正式契约中的结构化结果

当前实现：

- `InternalTaskReportController.status-report(...)` 返回 `Result<Void>`
- `InternalTaskDispatchController.dispatch-failed(...)` 返回 `Result<Void>`
- `InternalTaskReportController.markFinished(...)` 返回 `Result<Void>`
- `InternalTaskReportController.markFailed(...)` 返回 `Result<Void>`

最新设计要求：

- `status-report` 至少返回 `taskId`、`status=RUNNING`
- `dispatch-failed` 至少返回 `taskId`、回写后的 `status`
- `mark-finished` 至少返回 `taskId`、终态 `status`
- `mark-failed` 至少返回 `taskId`、`status=FAILED`

影响：

- 调度器和 node-agent 无法直接基于响应体确认“本次回写最终被接受到什么状态”
- 核心接口契约总表与实际控制器返回体不一致，联调时容易误判

涉及文件：

- `task-service/src/main/java/com/example/cae/task/interfaces/internal/InternalTaskDispatchController.java`
- `task-service/src/main/java/com/example/cae/task/interfaces/internal/InternalTaskReportController.java`
- `common-lib/src/main/java/com/example/cae/common/dto/TaskDispatchAckDTO.java`
- `接口设计.md`

结论：

- 这是内部接口合同层面的新增缺口，应纳入正式清单，在继续联调前补齐。

当前状态：

- `2026-04-22` 已完成：已新增统一 `TaskStatusAckDTO`，`status-report / dispatch-failed / mark-finished / mark-failed` 均已收口为 `taskId/status` 结构化返回。

### 6.25 偏差二十五：节点离线补偿的调用顺序与返回计数仍和最新规则不一致

当前实现：

- `NodeHeartbeatChecker.markOfflineNodes()` 先调用 `taskClient.markNodeOfflineTasksFailed(...)`，后调用 `nodeAppService.markNodeOffline(...)`
- `TaskDispatchManager.markNodeOfflineTasksFailed(...)` 最终返回的是扫描到的 `affectedTasks.size()`，不是实际发生状态变更的任务数量

最新设计要求：

- 节点离线补偿请求发起前，节点应已被标记为 `OFFLINE`
- `/internal/tasks/node-offline/fail` 返回值必须表示“本次实际发生状态变更的任务数量”，不能把已忽略、已终态或已被抢先处理的任务计入

影响：

- 会让“节点已离线再补偿”这一正式约束在实现上失真
- 离线补偿审计数量不准确，后续日志排查和论文描述都容易出错

涉及文件：

- `scheduler-service/src/main/java/com/example/cae/scheduler/infrastructure/support/NodeHeartbeatChecker.java`
- `task-service/src/main/java/com/example/cae/task/application/manager/TaskDispatchManager.java`
- `scheduler-service/src/main/java/com/example/cae/scheduler/infrastructure/client/impl/TaskClientStub.java`
- `接口设计.md`
- `状态流转校验规则表.md`

结论：

- 这是离线补偿链路的新增偏差，应优先在 `scheduler-service + task-service` 联动层修正。

当前状态：

- `2026-04-22` 已完成：`NodeHeartbeatChecker` 已改为先标记节点 `OFFLINE` 再发起补偿；`/internal/tasks/node-offline/fail` 仅返回实际发生状态变更的任务数量，忽略已终态、已解绑或已被并发处理的任务。

### 6.26 偏差二十六：驾驶舱 `avgNodeLoad` 统计口径仍与最新接口设计不一致

当前实现：

- `SchedulerClient.getOnlineNodeSummary()` 以 `runningCount + reservedCount` 作为负载计算分子
- 统计样本来自所有在线节点，没有按“在线且启用”收口

最新设计要求：

- `avgNodeLoad` 首版固定表示“在线且启用节点”的平均负载
- 统计口径应按 `runningCount / maxConcurrency` 求平均，不应把 `reservedCount` 混入驾驶舱负载展示

影响：

- 前端驾驶舱展示会与接口设计和论文描述不一致
- 调度预占数会被误当成真实运行负载，降低指标解释性

涉及文件：

- `task-service/src/main/java/com/example/cae/task/infrastructure/client/SchedulerClient.java`
- `task-service/src/main/java/com/example/cae/task/application/service/TaskQueryAppService.java`
- `接口设计.md`

结论：

- 这是统计口径层面的新增偏差，应在驾驶舱联调前收口。

### 6.27 偏差二十七：节点求解器启停接口仍未返回 `nodeId/solverId/enabled`

当前实现：

- `NodeController.updateNodeSolverStatus(...)` 返回 `Result<Void>`
- `NodeAppService.updateNodeSolverStatus(...)` 也没有构造结构化响应

最新设计要求：

- `POST /api/nodes/{nodeId}/solvers/{solverId}/status` 应返回：
  - `nodeId`
  - `solverId`
  - `enabled`
- 重复设置为同一值时，应按幂等成功返回当前值

影响：

- 前端不能直接依据响应确认能力开关是否生效
- 接口总规约、详细接口设计与控制器实现存在直接偏差

涉及文件：

- `scheduler-service/src/main/java/com/example/cae/scheduler/interfaces/controller/NodeController.java`
- `scheduler-service/src/main/java/com/example/cae/scheduler/application/facade/NodeFacade.java`
- `scheduler-service/src/main/java/com/example/cae/scheduler/application/manager/NodeManageManager.java`
- `scheduler-service/src/main/java/com/example/cae/scheduler/application/service/NodeAppService.java`
- `接口设计.md`

结论：

- 这是节点管理前端合同层面的新增问题，建议作为 `P1` 收口。

### 6.28 偏差二十八：调度记录前端接口仍缺少 `taskNo`，且单任务查询权限边界未真正落地

当前实现：

- `ScheduleRecordResponse` 不包含 `taskNo`
- `TaskScheduleRecordResponse` 也不包含 `taskNo`
- `/api/tasks/{taskId}/schedules` 当前直接暴露在 `scheduler-service`，控制器不接收用户身份信息，也无法校验任务归属

最新设计要求：

- `/api/schedules` 与 `/api/tasks/{taskId}/schedules` 都应返回 `taskNo`
- 单任务调度记录查询应只允许当前用户查询自己可见任务，管理员可查询任意任务

影响：

- 调度记录接口返回字段与接口文档不一致
- 单任务调度记录查询存在越权风险，至少当前没有按文档落地权限边界

涉及文件：

- `scheduler-service/src/main/java/com/example/cae/scheduler/interfaces/controller/ScheduleController.java`
- `scheduler-service/src/main/java/com/example/cae/scheduler/interfaces/response/ScheduleRecordResponse.java`
- `scheduler-service/src/main/java/com/example/cae/scheduler/application/assembler/ScheduleAssembler.java`
- `task-service/src/main/java/com/example/cae/task/interfaces/response/TaskScheduleRecordResponse.java`
- `task-service/src/main/java/com/example/cae/task/application/service/TaskQueryAppService.java`
- `接口设计.md`

结论：

- 这是前端可见接口层面的新增关键偏差，建议通过“由 task-service 代理单任务调度记录查询”或“补充任务归属校验内部链路”的方式收口。

### 6.29 偏差二十九：模板正式契约仍未在 `solver-service / node-agent` 中强制执行

当前实现：

- `CreateProfileRequest`、`UpdateProfileRequest` 只要求 `uploadMode`、`commandTemplate` 非空，没有限制首版只能为 `ZIP_ONLY`
- `ProfileAppService` 直接透传 `commandTemplate`
- `CommandBuilder` 仍会注入并解析 `${workDir}`，意味着数据库模板依然可以继续使用文档已禁止的旧占位符
- `ruleJson` 当前也没有在模板管理侧做“仅简单约束”的边界校验

最新设计要求：

- 首版 `uploadMode` 应统一为 `ZIP_ONLY`
- `commandTemplate` 只能使用稳定占位符，首版不再支持 `${workDir}` 作为对外模板占位符
- `ruleJson` 首版应优先限定在后缀、数量、大小等简单约束范围

影响：

- 管理员仍可能录入“文档上不允许、代码里却能通过”的模板
- 模板正式契约会继续停留在纸面，后续新增求解器时容易再次失控

涉及文件：

- `solver-service/src/main/java/com/example/cae/solver/interfaces/request/CreateProfileRequest.java`
- `solver-service/src/main/java/com/example/cae/solver/interfaces/request/UpdateProfileRequest.java`
- `solver-service/src/main/java/com/example/cae/solver/application/service/ProfileAppService.java`
- `node-agent/src/main/java/com/example/cae/nodeagent/infrastructure/support/CommandBuilder.java`
- `接口设计.md`
- `后端设计.md`
- `需求分析与系统设计.md`

结论：

- 这是模板管理正式契约层面的新增关键偏差，若不尽快落地到代码校验，后续模板新增与联调会继续反复回到旧口径。

当前状态：

- `2026-04-22` 已完成：`solver-service` 已新增模板契约校验，创建/修改模板时强制 `uploadMode=ZIP_ONLY`、禁止 `${workDir}` 与复杂 shell 控制、限制 `ruleJson` 仅允许简单约束键；`UploadSpec` 与内部模板详情接口也会对历史配置做合同校验；`node-agent` 的 `CommandBuilder` 已移除 `${workDir}` 支持并在执行前再次校验轻量命令模板边界。

---

### 6.30 偏差三十：个别服务仍直接写 `400/409` 魔法值，错误码口径未完全统一

当前实现：
- `solver-service` 的 `ProfileRuleValidator` 仍直接抛出 `new BizException(400, ...)`
- `task-service` 的 `LocalTaskFileStorageService` 仍直接抛出 `new BizException(400, ...)`
- `node-agent` 的 `TaskDispatchManager` 仍直接抛出 `new BizException(400/409, ...)`

最新设计要求：

- 错误码、冲突语义和幂等返回应尽量收口到统一常量与共享口径
- 即便首版仍使用通用 `400/409`，也应通过 `ErrorCodeConstants.BAD_REQUEST / CONFLICT` 等公共常量表达，避免不同服务继续各写一套魔法值
- 后续若补充更细的领域错误码，调用链与异常处理也应能基于统一常量平滑收敛，而不是在代码里继续散落裸数字

影响：
- 当前对外行为未必立刻出错，但实现层已经出现“同一类非法请求/冲突在不同服务中用不同写法表达”的漂移
- 后续若继续补充错误码文档、网关映射或统一异常审计，这类裸数字会成为遗漏点
- 也会削弱这轮“字段、接口、状态、错误语义统一收口”的成果

涉及文件：
- `solver-service/src/main/java/com/example/cae/solver/infrastructure/support/ProfileRuleValidator.java`
- `task-service/src/main/java/com/example/cae/task/infrastructure/storage/LocalTaskFileStorageService.java`
- `node-agent/src/main/java/com/example/cae/nodeagent/application/manager/TaskDispatchManager.java`
- `common-lib/src/main/java/com/example/cae/common/constant/ErrorCodeConstants.java`

结论：
- 这是 `D16` 维度下新增识别出的实现一致性问题，优先级可定为 `P2`；它不阻断主链路，但建议在本轮接口与状态合同收口后尽快统一。

## 7. 非阻断性优化建议

以下问题当前不一定阻断功能，但建议后续整理：

### 7.1 预占逻辑存在重复实现

当前 `NodeAppService` 与 `ScheduleAppService` 都各自持有一套 `reserve/release` 逻辑，虽然功能上能工作，但和设计文档中建议的 `NodeCapacityManager` 收口方向不一致。

建议：

- 后续可抽取统一的节点容量管理组件
- 把预占、释放、幂等返回、冲突语义统一到一个入口

价值：

- 减少逻辑分叉
- 降低后续维护和联调时的语义漂移风险

### 7.2 网关仍保留未使用的旧路由匹配项

当前 `GatewayRouteConfig` 仍包含：

- `/api/scheduler/nodes/**`
- `/api/scheduler/records/**`

但当前后端并没有对应控制器，也不在最新接口文档中。

建议：

- 后续在清理遗留兼容路径时一并收口这些旧路由匹配

价值：

- 降低网关配置和接口设计文档之间的漂移
- 避免未来误以为系统仍保留这组路径

---

## 8. 最新正式开发清单

### 8.1 已完成项

| 编号 | 优先级 | 状态 | 工作项 | 说明 |
| --- | --- | --- | --- | --- |
| A-01 | P0 | DONE | 收紧首版状态机 | 已去除首版不允许的 `RUNNING -> CANCELED` 等流转 |
| A-02 | P0 | DONE | 收窄 `status-report` | 已固定为 `DISPATCHED -> RUNNING` |
| A-03 | P0 | DONE | 收紧 `mark-finished` | 已仅允许 `SUCCESS / TIMEOUT` |
| A-04 | P0 | DONE | 去除 node-agent 超范围状态回写 | 已不再主动回写 `DISPATCHED / CANCELED` |
| A-05 | P0 | DONE | 补齐 `nodeId` 一致性校验 | `mark-dispatched` / `dispatch-failed` 已收口 |
| A-06 | P0 | DONE | 预占/释放改为 `taskId + nodeId` 级幂等 | 已引入 `node_reservation` 并接入主链路 |
| A-07 | P0 | DONE | 落地 `node_reservation` 数据库结构 | SQL、实体、仓储已补齐 |
| A-13 | P0 | DONE | 打通 `compute_node.port` | 已贯通数据库、DTO、注册、查询与派发 |
| A-14 | P0 | DONE | 收口 `reserve / release-reservation` 返回契约与冲突语义 | 已返回 `reservationStatus/reservedCount`，并补齐“无记录冲突/已释放幂等”语义 |
| A-08 | P0 | DONE | 按新文档重做节点离线补偿分流 | `SCHEDULED/DISPATCHED` 已回队，`RUNNING` 才失败，且不再直接清零 `reservedCount` |
| A-09 | P1 | DONE | 实现日志、结果摘要、结果文件真正幂等 | 已落地日志/结果摘要/结果文件的数据库级幂等，并补充唯一约束补丁脚本 |

### 8.2 未完成项

| 编号 | 优先级 | 状态 | 工作项 | 类型 | 影响模块 |
| --- | --- | --- | --- | --- | --- |
| A-10 | P1 | DONE | 调整单任务调度记录查询顺序为升序 | 已按 `created_at ASC, id ASC` 收口单任务调度时间线 |
| A-11 | P1 | DONE | 同步修正文档中残留旧示例和旧汇总口径 | 已统一 `数据库设计.md`、`接口设计.md`、`docs/sql/init_databases.sql` 中残留旧模板示例，并确认 `scheduler-service` 的 `node_reservation` 汇总口径一致 | `数据库设计.md`、`接口设计.md`、`需求分析与系统设计.md`、`docs/sql/init_databases.sql`、相关核心文档 |
| A-12 | P1 | DONE | 在上述改造完成后做一次字段级终审 | 已完成字段级终审，并收口 `mark-scheduled/mark-dispatched` 仅接受 body `nodeId` 且补齐必填校验，未发现新的核心字段漂移 | `docs`、核心服务代码 |
| A-15 | P2 | DONE | 抽取 `NodeCapacityManager`，统一预占逻辑入口 | 已新增 `NodeCapacityManager`，统一 `reserve/release` 逻辑，`ScheduleAppService` 与 `NodeAppService` 不再重复维护节点预占实现 | `scheduler-service` |
| A-16 | P1 | DONE | 清理节点管理接口混用 `id/nodeId` 的响应字段问题 | 已统一节点管理响应仅返回 `nodeId` |
| A-17 | P1 | DONE | 收口 `dispatch-failed` 的预占释放职责边界，由 `scheduler-service` 独占释放 | 已保留 task-service 不直接释放预占的职责边界，并补齐 node-agent 在已接收任务后、未进入 RUNNING 前准备失败时调用 scheduler-service 释放预占的闭环，避免 `reservedCount/node_reservation` 泄漏 |
| A-18 | P2 | DONE | 统一 `mark-dispatched` 的触发时序与返回契约 | 已改为“节点接收成功后再回写 `DISPATCHED`”，并补齐结构化返回体 `taskId/nodeId/status` | `scheduler-service`、`task-service`、`接口设计.md` |
| A-19 | P1 | DONE | 移除或封禁设计外重复接口 `/api/tasks/admin`，统一管理员列表入口 | 已移除设计外重复入口，管理员任务列表统一收口到 `/api/admin/tasks` | `task-service`、`接口设计.md` |
| A-20 | P1 | DONE | 为日志/结果上报补齐状态阶段校验 | 已按设计收口：`log-report` 允许 `DISPATCHED / RUNNING / 终态`，结果摘要/结果文件仅允许 `RUNNING / 终态` | `task-service`、`状态流转校验规则表.md` |
| A-21 | P3 | DONE | 清理旧版 `running-count` 兼容内部接口 | 已移除 `scheduler-service` 的旧兼容入口及 `node-agent` 对应空实现客户端方法 | `scheduler-service`、`node-agent` |
| A-22 | P1 | DONE | 收紧管理员调整优先级接口的状态边界，仅允许 `CREATED / VALIDATED / QUEUED` | 已按设计收紧，调度/执行阶段与终态任务均拒绝调整优先级 | `task-service`、`状态流转校验规则表.md` |
| A-23 | P2 | DONE | 清理 `user-service`、`solver-service`、`profile-service` 中设计文档外的 `PUT .../status` 兼容接口 | 已移除 `user-service`、`solver-service` 中设计外的 `PUT .../status` 兼容入口，仅保留正式 `POST .../status` | `user-service`、`solver-service`、`接口设计.md` |
| A-24 | P1 | DONE | 为 `task_file` 的单有效输入压缩包合同补齐事务与数据库级唯一性兜底 | 已补齐事务、数据库唯一约束、upsert 落地与现网补丁脚本 | `task-service`、`docs/sql`、`数据库设计.md` |
| A-25 | P1 | DONE | 在创建任务阶段前置校验 `solverId / profileId / taskType` 合法组合 | 已收口创建前置条件：求解器/模板存在且启用，组合关系与 taskType 一致 | `task-service`、`solver-service`、`接口设计.md` |
| A-26 | P3 | DONE | 清理旧版节点注册/心跳内部入口 `/internal/scheduler/nodes/register|heartbeat`，统一节点接入路径 | 已删除旧版内部节点注册/心跳控制器，仅保留正式的 node-agent 接入路径 | `scheduler-service`、`接口设计.md` |
| A-27 | P2 | DONE | 收口或补齐文档外内部辅助接口 `/internal/tasks/{taskId}/status`、`/internal/tasks/node-offline/fail` 的设计口径 | 已移除 `status` 依赖并补齐节点离线补偿接口文档口径 | `task-service`、`scheduler-service`、`接口设计.md` |
| A-28 | P1 | DONE | 按模板 `paramsSchema/paramsSchemaJson` 对任务 `params` 做后端校验，并落地 `PARAM_SCHEMA_INVALID` | 已补齐创建/编辑的部分校验与 validate 阶段的完整校验，并复用统一错误码 |
| A-29 | P1 | DONE | 统一任务文件校验 `issues[].errorCode` 契约，消除文档外错误码或同步修订接口文档 | 已收口到接口文档内稳定错误码，并补齐 `ARCHIVE_TOO_LARGE` 产出 |
| A-30 | P2 | DONE | 按接口设计收口 `mark-scheduled` 返回契约，返回 `claimed/taskId/nodeId/status` | 已改为结构化返回，并由 `scheduler-service` 同步消费 | `task-service`、`scheduler-service`、`接口设计.md` |
| A-31 | P1 | DONE | 修正 `status-report` 在携带 `fromStatus` 时的幂等处理，确保重复 `RUNNING` 回传按同节点幂等成功 | 已按同节点优先命中 RUNNING 幂等，再执行严格状态校验 |
| A-32 | P2 | DONE | 统一任务文件、结果文件、调度记录响应中的 `id/fileId/scheduleId` 字段口径 | 已统一收口到文档命名字段，并同步调整聚合与远程消费链路 | `task-service`、`scheduler-service`、`接口设计.md` |
| A-33 | P0 | DONE | 收紧 node-agent 终态回写规则，禁止 `mark-failed` 通过 `failType=TIMEOUT` 产出 `TIMEOUT` | 已补齐节点侧终态回写闭环：超时异常不再调用 `mark-failed`，而是改为 `mark-finished(finalStatus=TIMEOUT)`；`mark-failed` 仅用于真正的 `FAILED` 终态 | `task-service`、`node-agent`、`状态流转校验规则表.md` |
| A-34 | P1 | DONE | 按核心接口契约总表收口内部状态回写接口返回体 | 已新增统一 `TaskStatusAckDTO`，`status-report / dispatch-failed / mark-finished / mark-failed` 均返回 `taskId/status` 结构化回执 | `task-service`、`common-lib`、`接口设计.md` |
| A-35 | P1 | DONE | 修正节点离线补偿的调用顺序与“实际变更数量”返回语义 | 已改为先标记节点 `OFFLINE` 再触发补偿，且 `/internal/tasks/node-offline/fail` 仅返回实际发生状态变更的任务数量 | `scheduler-service`、`task-service`、`状态流转校验规则表.md` |
| A-36 | P1 | DONE | 把驾驶舱 `avgNodeLoad` 统计口径改为“在线且启用节点的 `runningCount / maxConcurrency` 平均值” | 已改为仅采样在线且启用节点，且只按 `runningCount / maxConcurrency` 计算平均负载，不再把 `reservedCount` 纳入驾驶舱统计 | `task-service`、`接口设计.md` |
| A-37 | P1 | DONE | 为节点求解器启停接口补齐 `nodeId/solverId/enabled` 结构化返回 | 已补齐 `NodeSolverStatusResponse`，节点求解器启停接口返回 `nodeId/solverId/enabled`，不再是空响应 | `scheduler-service`、`接口设计.md` |
| A-38 | P1 | DONE | 收口调度记录前端接口：补齐 `taskNo`，并为单任务调度记录查询落地权限边界 | 已保留 task-service 侧带权限校验的 `/api/tasks/{taskId}/schedules`，并删除 scheduler-service 残留的同路径公开接口，避免服务直连绕过任务归属/管理员权限边界 | `scheduler-service`、`task-service`、`gateway-service`、`common-lib`、`接口设计.md` |
| A-39 | P1 | DONE | 在模板管理侧强制执行 `ZIP_ONLY + 轻量 commandTemplate + 简单 ruleJson` 正式契约 | 已在 `solver-service` 录入与内部输出链路、`node-agent` 执行边界同时落地 `ZIP_ONLY / 轻量 commandTemplate / 简单 ruleJson` 硬约束 | `solver-service`、`node-agent`、`接口设计.md` |
| A-40 | P2 | DONE | 统一残留的裸数字错误码写法，收口到共享错误码常量与一致冲突语义 | 已清理扫描范围内残留的 `400/409` 裸数字错误码，统一改为 `ErrorCodeConstants.BAD_REQUEST / CONFLICT` | `solver-service`、`task-service`、`node-agent`、`common-lib` |
| A-41 | P0 | DONE | 收口调度下发竞态，保证 node-agent 只有在 task 已成功落为 `DISPATCHED` 后才能进入 `RUNNING` | 已在 node-agent 侧为 `status-report(RUNNING)` 增加基于业务返回码的重试等待，求解器执行不会早于 `DISPATCHED` 持久化成功 | `scheduler-service`、`node-agent`、`task-service`、`任务状态流转实现表.md` |
| A-42 | P0 | DONE | 重新收口 `dispatch-failed` 责任边界，node-agent 不再直连 task-service 回写派发失败 | 已改为 node-agent 回调 scheduler-service，由 scheduler-service 统一调用 task-service `dispatch-failed` 并决定是否释放预占 | `node-agent`、`scheduler-service`、`task-service`、`接口设计.md` |
| A-43 | P1 | DONE | 对齐请求 DTO 校验长度与数据库字段长度 | 已收紧 `summaryText`、`storagePath` 以及模板 `profileCode/taskType/profileName/parserName/commandTemplate` 的请求校验长度，与当前数据库列宽保持一致 | `task-service`、`solver-service`、`docs/sql/init_databases.sql`、`数据库设计.md` |
| A-44 | P2 | DONE | 修正 `init_databases.sql` 中与最新合同不一致的示例数据 | 已补齐 `VALIDATED` 状态流转示例，并移除引用不存在任务的调度记录初始化数据 | `docs/sql/init_databases.sql`、`数据库设计.md` |
| A-45 | P1 | DONE | 收口内部 HTTP 客户端的 `Result.code` 校验，避免状态回写“假成功” | 已为 node-agent 任务回写客户端和 scheduler-service 的 task-service 调用链补齐 `Result.code` 校验，冲突/失败不再被误判为成功 | `node-agent`、`scheduler-service`、`task-service` |
| A-46 | P1 | DONE | 收口调度器派发失败补偿的异常兜底，避免 catch 块内部再次抛错中断整轮调度 | 已为 `TaskScheduleJob` 增加“释放预占/回写 dispatch-failed/记录调度失败”的安全兜底逻辑，补偿链异常不再中断后续任务调度 | `scheduler-service` |
| A-47 | P1 | DONE | 对齐 `ruleJson` 合同与当前样例/实现能力，消除“样例能写、API 不让配”的漂移 | `solver-service` 已允许并校验作用域选择器（如 `solverCodes`、`taskTypes`）以及受控的 `deriveParam/deriveParams`，与 `task-service` 校验链路和现有 SQL 样例保持一致 | `solver-service`、`task-service`、`数据库设计.md`、`接口设计.md`、`需求分析与系统设计.md` |
| A-48 | P2 | DONE | 清理模块说明文档中残留的旧接口路径，避免定位误导 | 已同步修订 `user-service.md`、`solver-service.md`、`scheduler-service .md`、`node-agent .md` 中残留的旧版 `PUT .../status`、`running-count`、旧节点注册/心跳路径 | 模块说明文档 |
| A-49 | P1 | DONE | 补齐查询型远程客户端的 `Result.code` 校验，避免“空数据兜底”掩盖真实下游失败 | 已为 `task-service` 的 `SchedulerClient`、`SolverClient` 以及 `scheduler-service` 的 `SolverClientStub` 补齐业务码校验，上层按既有容错边界决定降级或失败 | `task-service`、`scheduler-service` |
| A-50 | P1 | DONE | 二次收口剩余 DTO 列宽与数据库字段长度不一致的漏项 | 已补齐 `CreateTaskRequest / UpdateTaskRequest / CreateSolverRequest / UpdateSolverRequest / CreateFileRuleRequest` 的长度约束，与 `sim_task`、`solver_definition`、`solver_profile_file_rule` 的正式列宽保持一致 | `task-service`、`solver-service`、`docs/sql/init_databases.sql`、`数据库设计.md` |
| A-51 | P1 | DONE | 清理网关旧调度路径残留并补齐用户/节点入口 DTO 列宽漏项 | 已移除 `GatewayRouteConfig` 与 `gateway-service.md` 中残留的 `/api/scheduler/nodes/**`、`/api/scheduler/records/**` 旧路径，并收紧 `user-service`、`scheduler-service` 中 `username/realName/nodeCode/nodeName/host/solverVersion` 的长度约束，与正式数据库合同保持一致 | `gateway-service`、`user-service`、`scheduler-service`、`docs/sql/init_databases.sql`、`数据库设计.md` |
| A-52 | P2 | DONE | 清理单任务调度记录查询归属的残留文档表述 | 已修正 `gateway-service.md`、`scheduler-service .md` 与 `docs/e2e-smoke-checklist.md` 中把 `/api/tasks/{taskId}/schedules` 归到 `scheduler-service` 或网关单独拆路由的旧表述，统一为由 `task-service` 对外承接权限校验 | 模块说明文档、`docs/e2e-smoke-checklist.md` |
| A-53 | P2 | DONE | 将 `scheduler-service` 内部任务客户端改为强类型 DTO 消费，移除弱类型 `Map` 解析 | `TaskClientStub` 已改为基于 `ParameterizedTypeReference<Result<...>>` 强类型消费 `mark-scheduled / mark-dispatched / dispatch-failed / node-offline/fail` 返回体，避免运行时字段名兜底和未检查类型转换 | `scheduler-service`、`task-service`、`common-lib` |
| A-54 | P1 | DONE | 收口状态历史与调度记录相关请求 DTO 的剩余列宽偏差 | 已收紧 `StatusReportRequest.fromStatus/toStatus/operatorType/status` 到 30，以及 `InternalScheduleRecordRequest.strategyName/scheduleStatus` 到 50/30，与 `task_status_history`、`schedule_record` 正式列宽保持一致 | `task-service`、`scheduler-service`、`docs/sql/init_databases.sql`、`数据库设计.md` |
| A-55 | P1 | DONE | 收口模板与结果上报相关枚举字段的剩余 30/32 列宽偏差 | 已收紧 `CreateProfileRequest/UpdateProfileRequest.uploadMode`、`CreateFileRuleRequest/UpdateFileRuleRequest.fileType`、`ResultFileReportRequest.fileType`、`MarkFinishedRequest.finalStatus` 到 30，与 `solver_task_profile.upload_mode`、`solver_profile_file_rule.file_type`、`task_result_file.file_type`、`sim_task.status` 的正式列宽保持一致 | `solver-service`、`task-service`、`docs/sql/init_databases.sql`、`数据库设计.md` |
| A-56 | P2 | DONE | 修正 `node-agent` 模块说明文档中仍把 `${workDir}` 列为支持占位符的错误表述 | 已同步修订 `node-agent .md`，移除 `${workDir}` 支持描述，并明确当前统一使用 `${taskDir}` 对应校验阶段确认的 `unpackDir` | 模块说明文档 |
| A-57 | P1 | DONE | 收口任务详情接口返回体，移除文档外嵌入的状态历史与调度记录字段 | 已从 `TaskDetailResponse` 与 `TaskQueryAppService` 中移除 `statusHistory/scheduleRecords` 聚合返回，统一通过 `/api/tasks/{taskId}/status-history` 与 `/api/tasks/{taskId}/schedules` 获取，避免详情接口额外耦合下游查询 | `task-service`、`接口设计.md`、`task-service.md`、`后端设计.md` |
| A-58 | P1 | DONE | 收口单任务调度记录查询接口的异常语义，禁止把下游失败伪装为空列表 | 已移除 `TaskQueryAppService.getTaskScheduleRecords(...)` 中对 `schedulerClient.listTaskScheduleRecords(...)` 的空列表兜底，保持独立查询接口在下游失败时显式报错 | `task-service`、`接口设计.md` |
| A-59 | P1 | DONE | 收口调度记录查询接口的 `taskNo` 补全语义，禁止在补全失败时返回不完整记录 | 已移除 `ScheduleAppService.fetchTaskBasics(...)` 中对 `taskClient.getTaskBasics(...)` 的空 `Map` 兜底，确保 `/api/schedules` 与内部单任务调度记录链路在 `taskNo` 无法补全时显式失败，而不是返回缺字段记录 | `scheduler-service`、`task-service`、`接口设计.md` |
| A-60 | P1 | DONE | 补齐管理员任务列表正式返回字段，并收口任务列表/详情名称字段的静默降级 | 已新增 `AdminTaskListItemResponse`，为 `/api/admin/tasks` 正式返回 `userId/username/failType`；同时引入 `UserClient` 复用 `user-service` 内部用户查询，并移除任务列表/详情中对 `solverName/profileName/nodeName` 的静默异常吞掉，避免返回不完整名称字段 | `task-service`、`user-service`、`接口设计.md`、`task-service.md` |
| A-61 | P1 | DONE | 收口管理员驾驶舱轻量统计接口的异常语义，禁止把节点统计失败伪装为 0 值 | 已移除 `TaskQueryAppService.getDashboardSummary()` 中对节点统计异常的静默兜底，并同步收紧 `SchedulerClient.getOnlineNodeSummary()` 的响应结构校验；`/api/admin/dashboard/summary` 在节点统计下游失败或响应体不合约时将显式报错，不再返回伪造的 `onlineNodeCount=0/avgNodeLoad=0` | `task-service`、`接口设计.md` |
| A-62 | P0 | DONE | 收口 `/internal/tasks/queued` 派发契约，禁止对执行元数据和参数 JSON 做静默降级 | 已移除 `TaskDispatchManager` 中对 `solver-service` 执行元数据的吞异常兜底，并在 `solverMeta/profileExecutionMeta/commandTemplate/paramsJson` 不合法时直接失败，避免把不完整 `TaskDTO` 继续交给 `scheduler-service` 和 `node-agent` | `task-service`、`scheduler-service`、`node-agent`、`接口设计.md`、`后端设计.md` |
| A-63 | P1 | DONE | 收口 `task-service` 远程客户端对“成功但数据结构不合法”响应的静默降级 | 已收紧 `SolverClient` 与 `UserClient`：当 `solver/profile/upload-spec/user` 内部接口返回 `code=0` 但 `data` 结构不符合正式契约时直接报错，不再返回 `null/[]` 继续让查询、校验或名称补全链路误判为正常 | `task-service`、`solver-service`、`user-service`、`接口设计.md` |
| A-64 | P1 | DONE | 收口本地查询组装层对 `paramsJson/metricsJson` 的静默降级 | 已移除 `TaskAssembler`、`TaskResultAssembler` 在解析已落库 JSON 失败时返回空对象的兜底逻辑；任务详情与结果摘要现在会对损坏数据显式报错，避免前端拿到“成功但不可信”的 `params/metrics` | `task-service`、`接口设计.md`、`后端设计.md` |
| A-65 | P1 | DONE | 收口调度记录查询中时间字段解析失败的静默降级 | 已将 `SchedulerClient` 对 `createdAt` 的解析改为严格校验，调度记录时间字段若不符合正式返回契约将直接报错，不再以 `null createdAt` 混入时间线数据 | `task-service`、`scheduler-service`、`接口设计.md` |
| A-66 | P1 | DONE | 收口 `scheduler-service` 内部任务客户端对正式返回 DTO 的静默兜底 | 已收紧 `TaskClientStub`：`/internal/tasks/queued`、`/internal/tasks/basics`、`mark-scheduled`、`mark-dispatched`、`dispatch-failed`、`node-offline/fail` 在 `code=0` 但 `data` 缺失或关键字段不完整时将显式报错，不再伪造空列表、空映射或补缺省 ACK | `scheduler-service`、`task-service`、`接口设计.md` |
| A-67 | P1 | DONE | 收口 `scheduler-service` 求解器内部客户端与节点求解器查询链路的静默降级 | 已收紧 `SolverClientStub`：当 `/internal/solvers/{id}`、`/internal/profiles/{id}` 返回 `code=0` 但 `data` 缺失或关键字段不完整时直接报错，避免节点求解器列表、调度前求解器/模板校验继续返回缺少 `solverName/profileName` 的伪成功结果 | `scheduler-service`、`solver-service`、`接口设计.md` |
| A-68 | P1 | DONE | 收口 `/internal/schedules` 写调度记录接口的请求合同，禁止隐式默认 `FCFS_MIN_LOAD/UNKNOWN` | 已将 `InternalScheduleRecordRequest.strategyName/scheduleStatus` 收紧为必填，并在 `ScheduleAppService.recordSchedule(...)` 中显式校验 `scheduleStatus` 仅允许正式枚举值，移除对空值的隐式默认填充 | `scheduler-service`、`接口设计.md`、`scheduler-service .md` |
| A-69 | P1 | DONE | 收口调度记录查询对 `nodeName` 的静默缺字段兜底 | 已将 `ScheduleAppService.enrichScheduleRecordResponses(...)` 改为当 `nodeId` 非空却无法补全 `nodeName` 时显式报错，不再返回缺少正式字段的调度记录 | `scheduler-service`、`接口设计.md` |
| A-70 | P1 | DONE | 收口 `node-agent` 对 `task-service` 状态回写 ACK 的弱类型消费 | `TaskReportClientImpl` 已改为强类型消费 `Result<TaskStatusAckDTO>` / `Result<Void>`，并对 `status-report / mark-finished / mark-failed` 的 `taskId/status` 正式 ACK 做硬校验，不再把坏回执当成功 | `node-agent`、`task-service`、`接口设计.md` |
| A-71 | P1 | DONE | 收口 `node-agent` 对 `scheduler-service` 注册/派发失败回执的弱类型消费与静默降级 | `SchedulerNodeClientImpl` 已改为强类型消费注册与 `dispatch-failed` 回执；注册必须返回合法 `nodeId/nodeToken`，不再在解析失败时静默保留旧 `nodeId` | `node-agent`、`scheduler-service`、`接口设计.md` |
| A-72 | P1 | DONE | 收口首版不支持的运行中取消链路，避免暴露“可触发但不落状态”的半成品接口 | `scheduler-service` 与 `node-agent` 的 `/cancel-task` 链路已统一改为显式返回“首版不支持运行中取消”，并同步修订模块说明文档，避免进程被中断却不产生正式状态流转 | `node-agent`、`scheduler-service`、`task-service`、`状态流转校验规则表.md` |
| A-73 | P1 | DONE | 收口 `task-service` 对 `release-reservation` 结构化回执的静默降级 | `SchedulerClient.releaseNodeReservation(...)` 已改为强类型消费 `Result<NodeReservationActionResult>`，并校验 `taskId/nodeId/reservationStatus/reservedCount`，不再把空 `data` 当成功 | `task-service`、`scheduler-service`、`接口设计.md` |
| A-74 | P1 | DONE | 收口 `scheduler-service` 对 `node-agent` 下发 ACK 的弱类型消费 | `NodeAgentClientStub` 已改为强类型消费节点 ACK，校验 `accepted/message` 结构，不再以 `Result.class + Map` 解析 `/internal/dispatch-task` 返回体 | `scheduler-service`、`node-agent`、`接口设计.md` |
| A-75 | P1 | DONE | 收口用户取消任务接口 `/api/tasks/{taskId}/cancel` 的正式返回契约 | `TaskController.cancelTask(...)` 已改为返回 `taskId/status` 结构化结果，和接口设计的 `CANCELED` 回执保持一致 | `task-service`、`接口设计.md` |
| A-76 | P1 | DONE | 补齐排队取消接口对已 `CANCELED` 任务的幂等成功语义 | `TaskLifecycleManager.cancelTask(...)` 已对已取消任务直接按幂等成功返回，不再误报 `TASK_CANCEL_NOT_ALLOWED` | `task-service`、`状态流转校验规则表.md`、`任务状态流转实现表.md` |
| A-77 | P2 | DONE | 收口管理员调整优先级接口 `/api/admin/tasks/{taskId}/priority` 的正式返回契约 | `AdminTaskController.adjustPriority(...)` 已改为返回结构化 `taskId/status/priority`，与接口设计总表保持一致 | `task-service`、`接口设计.md` |
| A-78 | P1 | DONE | 收口任务压缩包上传接口 `/api/tasks/{taskId}/files` 的返回字段缺口 | `TaskFileResponse` 已补齐 `status` 字段，上传回执现在会显式返回上传后任务状态（首版为 `CREATED`） | `task-service`、`接口设计.md` |
| A-79 | P1 | DONE | 收口任务提交接口 `/api/tasks/{taskId}/submit` 的正式返回字段缺口 | `TaskSubmitResponse` 已补齐 `submitTime`，`submit/retry/cancel` 共用回执模型已统一返回该字段，其中提交场景会显式带回本次入队时间 | `task-service`、`接口设计.md` |
| A-80 | P1 | DONE | 收口任务状态历史查询接口对外暴露的文档外内部字段 | `/api/tasks/{taskId}/status-history` 已移除文档外的 `id/taskId/operatorId`，仅保留正式合同字段，避免向普通用户暴露内部操作人 ID | `task-service`、`接口设计.md` |
| A-81 | P1 | DONE | 收口任务日志与结果查询接口对外暴露的文档外内部字段 | 已按正式接口合同裁剪日志分片与结果摘要/结果文件响应字段：`TaskLogResponse` 不再暴露 `id/taskId`，`TaskResultSummaryResponse` 不再暴露 `id/createdAt/updatedAt`，`TaskResultFileResponse` 不再暴露 `taskId` | `task-service`、`接口设计.md` |
| A-82 | P1 | DONE | 收口任务输入文件上传回执与文件列表响应混用同一宽 DTO 的合同漂移 | 已拆分上传回执 `TaskFileUploadResponse` 与输入文件列表 `TaskInputFileResponse`；上传接口仅返回 `fileId/originName/storagePath/status`，文件列表仅返回正式合同字段，不再暴露文档外内部字段 | `task-service`、`接口设计.md` |
| A-83 | P1 | DONE | 收口任务创建/编辑/优先级调整回执混用宽 DTO 的合同漂移 | 已移除 `TaskCreateResponse` 文档外的 `priority`，普通编辑任务 `PUT /api/tasks/{taskId}` 改为仅返回 `taskId/status` 的 `TaskUpdateResponse`，管理员调优先级 `POST /api/admin/tasks/{taskId}/priority` 独立返回 `TaskPriorityUpdateResponse(taskId/status/priority)`，回执语义已与正式接口合同一致 | `task-service`、`接口设计.md` |
| A-84 | P1 | DONE | 收口任务校验成功回执仍额外暴露 `issues=[]` 的合同漂移 | 已为 `TaskValidateResponse` 增加空字段裁剪，并移除成功场景对 `issues` 的显式赋值；`POST /api/tasks/{taskId}/validate` 现仅在失败时返回 `issues[]`，成功回执已收口到 `taskId/valid/status` | `task-service`、`接口设计.md` |
| A-85 | P1 | DONE | 收口 `submit/cancel/retry` 复用同一回执 DTO 导致 `submitTime` 外溢的合同漂移 | 已拆分 `submit` 与 `cancel/retry` 的回执模型：`submit` 继续返回带 `submitTime` 的 `TaskSubmitResponse`，`cancel` 与 `retry` 改为返回仅含 `taskId/status` 的 `TaskActionResponse`，对外字段已与正式合同一致 | `task-service`、`接口设计.md` |
| A-86 | P1 | DONE | 同步修订 `接口设计.md` 中提交任务接口总表与详细章节的返回字段不一致 | 已同步更新 `POST /api/tasks/{taskId}/submit` 的详细章节返回示例，补齐 `submitTime`，现在与总表和后端实现一致 | `接口设计.md` |
| A-87 | P1 | DONE | 拆分普通用户与管理员任务列表查询请求合同，避免外部查询参数漂移 | 已新增 `MyTaskListQueryRequest` 与 `AdminTaskListQueryRequest`，分别承接 `/api/tasks` 与 `/api/admin/tasks` 的正式查询参数；控制器和查询服务入口已切换为分离模型，普通用户接口不再暴露管理员侧 `userId/nodeId/failType` 及其他文档外筛选项 | `task-service`、`接口设计.md` |
| A-88 | P1 | DONE | 收口任务列表/详情 `queueReason` 对调度器故障的静默降级 | 已移除 `TaskQueryAppService.resolveQueueReason(...)` 对 `scheduler-service` 异常的吞错兜底，查询链路现在会对真实下游失败显式报错，不再伪造“排队中，等待调度器处理”这类正常业务说明 | `task-service`、`接口设计.md` |
| A-89 | P1 | DONE | 收口任务结果摘要查询接口对“摘要不存在”返回 `success + null` 的合同漂移 | 已将 `TaskResultAppService.getResultSummary(...)` 收口为：任务存在但结果摘要尚未生成时显式返回失败，不再返回 `Result.success(null)`；并同步在 `接口设计.md` 中补充“不存在摘要时应失败而非 success+null”的说明，`mvn -pl task-service -am -DskipTests compile` 已通过 | `task-service`、`接口设计.md` |
| A-90 | P1 | DONE | 收口任务列表默认排序仍按 `id DESC` 而非正式合同规定的时间倒序 | 已将 `TaskMapper.selectMyPage/selectAdminPage` 收口为按 `COALESCE(submit_time, created_at) DESC, id DESC` 返回，保证普通用户与管理员任务列表默认以时间倒序展示；`mvn -pl task-service -am -DskipTests compile` 已通过 | `task-service`、`接口设计.md` |
| A-91 | P1 | DONE | 收口节点分页查询 `solverId` 对非法字符串静默忽略的合同漂移 | 已将 `NodePageQueryRequest.solverId` 收口为强类型 `Long`，并同步移除 `ComputeNodeMapper` 对 `solverIdAsLong` 的静默兼容；现在 `/api/nodes` 若收到非法 `solverId` 会在接口绑定阶段显式失败，不再被当作“未筛选求解器”继续成功查询，`mvn -pl scheduler-service -am -DskipTests compile` 已通过 | `scheduler-service`、`接口设计.md` |
| A-92 | P1 | DONE | 收口 node-agent 回调 `dispatch-failed` 漏做 `X-Node-Token` 鉴权的实现偏差 | 已为 `scheduler-service` 的 `/internal/tasks/{taskId}/dispatch-failed` 补齐 `X-Node-Token` 必填校验，并复用 `NodeAppService.ensureValidNodeToken(...)` 对请求体 `nodeId` 做正式鉴权；与《接口设计》中“除注册外节点代理回调中心均需带 token”的规则已一致，`mvn -pl scheduler-service -am -DskipTests compile` 已通过 | `scheduler-service`、`接口设计.md` |
| A-93 | P1 | DONE | 收口 `task-service` 节点回调接口仍把 `X-Node-Token` 标为可选的合同漂移 | 已将 `InternalTaskReportController` 中 `status-report / log-report / result-summary-report / result-file-report / mark-finished / mark-failed` 的 `X-Node-Token` 全部收紧为 `required = true`，与《接口设计》中“除注册外节点代理回调中心接口统一使用 token 鉴权”的正式合同一致；`mvn -pl task-service -am -DskipTests compile` 已通过 | `task-service`、`接口设计.md` |
| A-94 | P1 | DONE | 收口 `node-agent` 运行态回报重试只覆盖少量业务冲突、未覆盖瞬时网络/下游异常且会误打不可恢复失败的实现偏差 | 已将 `TaskReportManager.reportRunning(...)` 的重试范围扩展到 `RestClientException` 与 `BAD_GATEWAY` 等暂态失败，并在重试耗尽后把这类“运行前状态回报失败”改为通过 `dispatch-failed(recoverable=true)` 回退，而非直接按不可恢复 `EXECUTOR_START_ERROR` 失败收口；与《接口设计》中“状态回传失败可重试”的口径一致，`mvn -pl node-agent -am -DskipTests compile` 已通过 | `node-agent`、`接口设计.md` |
| A-95 | P1 | DONE | 收口 `SCHEDULED/DISPATCHED -> QUEUED` 回队错误刷新 `submitTime`、破坏原始排队顺序的状态副作用偏差 | 已将 `QUEUED` 迁移副作用按来源状态拆分：`VALIDATED -> QUEUED` 与管理员 `FAILED/TIMEOUT -> QUEUED` 继续刷新 `submitTime`，而 `SCHEDULED/DISPATCHED -> QUEUED` 的 recoverable 回队与节点离线补偿回队改为保留原始 `submitTime`，仅清理 `nodeId/fail/start/end` 等运行态字段；与状态流转设计中的调度公平性规则一致，`mvn -pl task-service -am -DskipTests compile` 已通过 | `task-service`、`任务状态流转实现表.md`、`状态流转校验规则表.md` |
| A-96 | P1 | DONE | 收口 `status-report` 首次转 `RUNNING` 后预占释放失败会被吞掉，重复 `RUNNING` 回报又不再补释放的幂等偏差 | 已在 `TaskLifecycleManager.reportStatus(...)` 的幂等 `RUNNING` 回报分支中补做一次 `releaseReservationQuietly(...)`，利用 `release-reservation` 的幂等真值保护修复首次释放失败留下的 `node_reservation/reservedCount` 泄漏风险，同时不引入重复减计数；`mvn -pl task-service -am -DskipTests compile` 已通过 | `task-service`、`scheduler-service`、`状态流转校验规则表.md` |
| A-97 | P1 | DONE | 收口 `status-report` 在 `RUNNING` 已落库但预占释放失败时仍向节点返回成功，导致无后续重试机会的实现偏差 | 已将 `TaskLifecycleManager.reportStatus(...)` 在正常 `DISPATCHED -> RUNNING` 分支和幂等 `RUNNING` 分支中的预占释放改为严格模式：`release-reservation` 失败将显式抛错，由 node-agent 按既有“状态回传失败可重试”语义整体重试；从而避免 task 已转 `RUNNING` 但 `reservedCount/node_reservation` 永久泄漏的半成功状态，`mvn -pl task-service -am -DskipTests compile` 已通过 | `task-service`、`node-agent`、`状态流转校验规则表.md` |
| A-98 | P2 | DONE | 收口任务输入文件查询接口中 `storagePath` 与 `unpackDir` 路径展示口径不一致的实现偏差 | 已将 `TaskQueryAppService.toTaskInputFileResponse(...)` 中的 `unpackDir` 与 `storagePath` 一样统一走 `TaskStoragePathSupport.toDisplayTaskPath(...)`，确保前端看到的输入文件路径字段都遵循同一展示口径；`mvn -pl task-service -am -DskipTests compile` 已通过 | `task-service`、`接口设计.md` |
| A-99 | P1 | DONE | 收口节点离线补偿中“任务已回队但预占释放失败被静默吞掉，且 OFFLINE 节点不会再补偿重试”的链路偏差 | 已将 `TaskDispatchManager.markNodeOfflineTasksFailed(...)` 中 `SCHEDULED/DISPATCHED -> QUEUED` 的预占释放改为严格模式，释放失败会回滚整次离线补偿事务；同时 `NodeHeartbeatChecker` 已补齐对 stale `OFFLINE` 节点的重复补偿扫描能力，避免首次补偿半成功后遗留 `reservedCount/node_reservation` 脏数据。`mvn -pl task-service,scheduler-service -am -DskipTests compile` 已通过 | `task-service`、`scheduler-service`、`状态流转校验规则表.md`、`任务状态流转实现表.md` |
| A-100 | P2 | DONE | 收口 `status-report` 正式字段 `changeReason` 被 task-service 忽略导致状态历史说明丢失的接口合同偏差 | 已复核 `TaskLifecycleManager.pickReason(...)` 当前实现，确认其已优先读取正式字段 `changeReason`，再兼容旧字段 `reason`，与《接口设计》和 node-agent 调用口径一致；`mvn -pl task-service -am -DskipTests compile` 已通过 | `task-service`、`node-agent`、`接口设计.md` |
| A-101 | P1 | DONE | 收口“重新上传 ZIP 时先删旧输入/工作目录，再保存新文件”导致保存失败后旧输入丢失的实现偏差 | 已将 `TaskLifecycleManager.uploadTaskFile(...)` 改为先保存并落库新归档，再执行旧工作区/运行产物清理；新增 `deleteTaskPreparedArtifacts(...)` 仅清理 `workdir/logs/output`，不再在上传开始前删除整任务目录。同时失败回滚时会避免删除与旧归档同一路径的新文件，降低覆盖上传导致“旧输入被先删空”的风险；`mvn -pl task-service -am -DskipTests compile` 已通过 | `task-service`、`后端设计.md`、`需求分析与系统设计.md` |
| A-102 | P1 | DONE | 收口“调度选中节点后若预占瞬时失败，不再尝试同批次其他候选节点”的实现偏差 | 已为 `ScheduleStrategy` 补齐“候选节点有序列表”合同，`FcfsLeastLoadStrategy` 现在会输出稳定排序后的节点序列，`ScheduleAppService.scheduleTask(...)` 则按序逐个尝试 `reserve`；当首选节点因并发瞬时被抢满而返回 `NO_AVAILABLE_NODE` 时，会继续尝试同批次后续候选节点，仅在所有候选都预占失败时才结束本轮调度。`mvn -pl scheduler-service -am -DskipTests compile` 已通过 | `scheduler-service`、`后端设计.md`、`接口设计.md`、`需求分析与系统设计.md` |
| A-103 | P1 | DONE | 收口 `dispatch-failed` 对已进入 `RUNNING` 的迟到失败回调幂等成功的状态规则偏差 | 已收紧 `TaskDispatchManager.markFailed(...)` 的状态判断：`dispatch-failed` 现在仅对 `SCHEDULED/DISPATCHED` 执行真实回写；当任务已进入 `RUNNING` 时会按状态冲突拒绝，不再把迟到派发失败回调伪装成幂等成功；终态与已回队任务仍保留原有幂等语义。`mvn -pl task-service -am -DskipTests compile` 已通过 | `task-service`、`接口设计.md`、`任务状态流转实现表.md`、`状态流转校验规则表.md` |
| A-104 | P0 | DONE | 收口节点离线补偿批处理把多条任务状态更新与多次预占释放放进同一事务，导致局部外部成功后整体回滚的事务边界偏差 | 已将 `TaskDispatchManager.markNodeOfflineTasksFailed(...)` 改为按任务逐条执行独立事务：每条任务在单独事务里完成状态回写与预占释放，单条失败只回滚该任务，不再把整批已成功处理的任务一起回滚，从而避免“前面预占已释放、后面失败导致全批状态回滚”的跨服务不一致。`mvn -pl task-service -am -DskipTests compile` 已通过 | `task-service`、`scheduler-service`、`后端设计.md`、`状态流转校验规则表.md` |
| A-105 | P0 | DONE | 收口输入归档上传直接信任 `originalFilename` 拼接存储路径，可能突破任务目录边界的文件系统合同偏差 | 已为 `LocalTaskFileStorageService.saveInputFile(...)` 增加上传文件名清洗与目录边界校验：先提取安全 basename，再按 `inputDir.resolve(fileName).normalize()` 生成目标路径，并强制校验目标仍位于任务 `input` 目录内；非法文件名会直接拒绝，避免归档写出任务目录。`mvn -pl task-service -am -DskipTests compile` 已通过 | `task-service`、`后端设计.md`、`需求分析与系统设计.md` |
| A-106 | P1 | DONE | 收口覆盖上传在“同名归档”场景下仍会先物理覆盖旧文件，数据库失败时无法恢复旧内容的持久化偏差 | 已将 `LocalTaskFileStorageService.saveInputFile(...)` 的物理存储名改为随机唯一文件名，原始上传文件名仅保留在 `originName` 展示字段中；因此同名重复上传时不会再直接覆盖旧归档物理文件，后续数据库失败也不会先破坏旧内容。`mvn -pl task-service -am -DskipTests compile` 已通过 | `task-service`、`后端设计.md`、`需求分析与系统设计.md` |
| A-107 | P1 | DONE | 收口 `commandTemplate` 只校验模板文本、不校验占位符运行时取值，参数值可绕过轻量模板约束的执行安全偏差 | 已在 `node-agent` 的 `CommandBuilder` 中补齐占位符值校验：模板变量在替换前会统一拒绝包含换行、管道、重定向、命令替换等复杂 shell 控制片段，从而防止任务参数或路径值把轻量模板扩展成脚本。`mvn -pl node-agent -am -DskipTests compile` 已通过 | `node-agent`、`solver-service`、`后端设计.md`、`接口设计.md` |
| A-108 | P1 | DONE | 收口 `parserName` 已贯通字段但未贯通行为，结果解析器扩展点仍未真正落地的实现缺口 | 已在 `node-agent` 增加结果解析器扩展骨架：新增 `ResultParser` 接口、通用 `GenericResultParser` 与 `ResultParserSelectDomainService`，`CommandTemplateExecutor` 执行完成后会按 `parserName` 选择解析器生成 `summaryText/metrics/resultFiles`，后续新增专用解析器无需再改执行主流程。`mvn -pl node-agent -am -DskipTests compile` 已通过 | `node-agent`、`solver-service`、`后端设计.md`、`需求分析与系统设计.md` |
| A-109 | P1 | DONE | 收口节点任务目录合同中 `workdir/logs/meta` 语义与实现不一致，且 `unpackDir` 为 `workdir` 子目录时任务根目录和 `${taskDir}` 漂移的偏差 | 已统一 `task-service` 与 `node-agent` 的目录合同：`TaskPathResolver.resolveLogDir(...)` 改为 `logs` 并补充 `meta` 目录解析，`LocalTaskFileStorageService` 清理运行产物时会同步清理 `meta`；`WorkDirManager` 改为创建 `input/workdir/output/logs/meta`，其中运行时 `workDir` 固定指向 `.../workdir`，`${taskDir}` 优先指向校验后的 `unpackDir`，否则回退到 `workdir`；同时按路径中的 `workdir` 段反推任务根目录，避免 `unpackDir=/.../workdir/caseA` 时把 `output/logs` 错建到 `caseA` 上层或下层错误位置。后续兼容解压也会统一落到 `workdir`。`mvn -pl task-service,node-agent -am -DskipTests compile` 已通过 | `task-service`、`node-agent`、`需求分析与系统设计.md`、`后端设计.md` |
| A-110 | P1 | DONE | 收口节点结果文件上报直接信任 `storagePath/fileName`，缺少任务 `output/` 目录边界和真实文件存在性校验的结果文件合同偏差 | 已在 `TaskResultManager.saveResultFile(...)` 补齐结果文件路径校验：`fileName` 必须是普通文件名，`storagePath` 会先归一化为绝对路径并强制校验位于当前任务 `output/` 目录下，且共享目录中该文件必须真实存在；落库时改为使用校验后的真实路径与实际文件大小，不再直接信任节点上报值。后续 `A-190/A-191` 已明确嵌套结果文件可使用编码展示名，`fileName` 不再要求等于 `storagePath` 路径末段。`mvn -pl task-service -am -DskipTests compile` 已通过 | `task-service`、`node-agent`、`接口设计.md`、`需求分析与系统设计.md` |
| A-111 | P1 | DONE | 收口 node-agent 在求解器实际成功后若摘要/结果/终态回传异常，会被外层 `catch` 误写为 `FAILED` 的终态语义偏差 | 已在 `TaskExecuteManager.execute(...)` 区分“求解器已成功”与“求解器失败”两类异常来源：成功后的回传异常只记录为 post-success callback failure，不再调用 `mark-failed`；同时 `TaskReportManager` 为成功终态、失败终态与超时终态回传增加统一重试，摘要/结果/终态回传链路按幂等接口整体重试，避免瞬时异常直接中断。`mvn -pl node-agent -am -DskipTests compile` 已通过 | `node-agent`、`task-service`、`接口设计.md`、`状态流转校验规则表.md` |
| A-112 | P1 | DONE | 收口 scheduler-service 调用 node-agent 的 `/internal/dispatch-task`、`/internal/cancel-task` 未按接口设计执行 `X-Node-Token` 鉴权的安全合同偏差 | 已为 node-agent `DispatchController` 补齐 `X-Node-Token` 必填校验，并校验其与本机配置 `nodeToken` 一致；同时 `scheduler-service` 的 `NodeAgentClientStub` 在调用 `/internal/dispatch-task`、`/internal/cancel-task` 时会从目标节点记录中读取 `nodeToken` 写入请求头，从而使 `compute_node.node_token`、接口设计和真实派发链路保持一致。`mvn -pl scheduler-service,node-agent -am -DskipTests compile` 已通过 | `scheduler-service`、`node-agent`、`接口设计.md` |
| A-113 | P2 | DONE | 收口 `common-lib` 公共异常层仍残留裸数字 `400/401/403/404/500`，导致 A-40 在共享层未真正闭环的错误码偏差 | 已在 `common-lib` 公共层完成二次收口：`BizException(String)` 默认错误码改为 `ErrorCodeConstants.BAD_REQUEST`，`GlobalExceptionHandler` 全部改用共享错误码常量，并新增 `ErrorCodeConstants.INTERNAL_SERVER_ERROR` 承接通用异常的 `500` 响应；公共层错误码口径现已与业务服务保持一致。`mvn -pl common-lib -am -DskipTests compile` 已通过 | `common-lib`、`后端设计.md` |
| A-114 | P2 | DONE | 收口 `gateway-service` 异常处理仍残留裸数字 `401/403/500` 与硬编码 JSON 回退体的错误码偏差 | 已将 `GatewayExceptionHandler` 中的 `401/403/500` 全部收口到 `ErrorCodeConstants`，并将 JSON 序列化失败时的兜底响应同步改为使用共享 `INTERNAL_SERVER_ERROR` 常量，网关层异常响应口径现已与 `common-lib` 和业务服务一致。`mvn -pl gateway-service -am -DskipTests compile` 已通过 | `gateway-service`、`common-lib` |
| A-115 | P1 | DONE | 收口节点注册链路“先按 `solverIds` 替换能力、再按带版本 `solvers` 再替换一次”导致的重复写入与非原子事实同步偏差 | 已将 `NodeAppService.registerNode(...)` 与 `registerNodeFromAgent(...)` 都收口为事务化注册入口，并拆出单次 `saveOrUpdateNode(...)` 基础信息同步；`registerNodeFromAgent(...)` 不再复用旧的 `registerNode(...)` 做两次能力替换，而是直接按带版本的 `solvers` 一次性同步节点事实能力，从而使节点基础信息与 `node_solver_capability` 的更新保持同事务、单次落库。`mvn -pl scheduler-service -am -DskipTests compile` 已通过 | `scheduler-service`、`接口设计.md`、`需求分析与系统设计.md`、`数据库设计.md` |
| A-116 | P1 | DONE | 收口 node-agent 在缺失求解器版本配置时伪造 `solverVersion="v1"` 上报，以及未显式拦截重复 `solverId` 的“事实上报”合同偏差 | 已在 `node-agent` 的 `SchedulerNodeClientImpl` 中补齐求解器配置前置校验：注册前会显式拦截空列表、非法或重复 `solverId`，并要求每个已配置 `solverId` 都必须存在真实 `solverVersion`，缺失时直接失败，不再伪造 `v1`。同时 `scheduler-service` 的 `NodeDomainService` 已补齐节点注册请求中重复 `solverId` 和空版本的服务侧校验，避免请求绕过客户端后在 `uk_node_solver` 处以底层 SQL 异常失败。`mvn -pl node-agent -am -DskipTests compile` 与 `mvn -pl scheduler-service -am -DskipTests compile` 已通过 | `node-agent`、`scheduler-service`、`接口设计.md`、`需求分析与系统设计.md` |
| A-117 | P0 | DONE | 收口调度器对 `mark-scheduled` / `mark-dispatched` 缺少显式重试与幂等恢复，导致瞬时网络失败可把“服务端已成功”误判为“调度失败”的调度状态闭环偏差 | 已在 `TaskScheduleJob` 中为 `mark-scheduled` 与 `mark-dispatched` 增加显式重试；其中 `mark-scheduled` 还补齐了“同节点已进入 `SCHEDULED / DISPATCHED / RUNNING / 终态`”的幂等恢复判定，避免首个响应丢失后把已成功调度误判为失败并错误释放预占。这样调度器已按《接口设计》中“允许网络重试、同节点幂等确认”的合同消费这两条内部接口。`mvn -pl scheduler-service -am -DskipTests compile` 已通过 | `scheduler-service`、`task-service`、`接口设计.md`、`任务状态流转实现表.md`、`状态流转校验规则表.md` |
| A-118 | P1 | DONE | 收口 node-agent 执行前失败回调 `dispatch-failed` 缺少重试，瞬时网络失败可能遗留 `SCHEDULED/DISPATCHED` 与节点预占的闭环偏差 | 已为 `TaskReportManager.reportPreRunFailure(...)` 补齐有限重试封装：执行前可恢复失败与不可恢复失败都会通过同一 `dispatch-failed` 重试通道回调 scheduler-service；网络异常和 `BAD_GATEWAY` 会重试，真实业务冲突不盲目重试。`SchedulerNodeClientImpl` 已负责强校验 `taskId/status` ACK，确保回队或失败回写及预占释放闭环能消费到正式回执。`mvn -pl node-agent -am -DskipTests compile` 已通过 | `node-agent`、`scheduler-service`、`task-service`、`接口设计.md`、`状态流转校验规则表.md` |
| A-119 | P1 | DONE | 收口结果文件下载接口直接信任库内 `storagePath`，旧脏数据可能突破任务 `output/` 目录边界的下载安全偏差 | 已在 `TaskResultAppService.openResultFile(...)` 增加下载前校验：会再次校验结果文件属于当前任务 `output/` 目录、`fileName` 与路径末段一致、共享目录中真实存在且为普通文件；旧脏数据、越界路径或已丢失文件统一按 `RESULT_FILE_NOT_FOUND` 收口，不再直接把库内 `storagePath` 交给存储层打开。`mvn -pl task-service -am -DskipTests compile` 已通过 | `task-service`、`接口设计.md`、`需求分析与系统设计.md` |
| A-120 | P2 | DONE | 收口 `result-summary-report` 残留文档外 `success` 兼容字段，避免与正式字段 `successFlag` 形成双口径 | 已删除 `ResultSummaryReportRequest` 中文档外 `success` 兼容字段，并移除 `TaskResultManager.saveResultSummary(...)` 对该字段的回退逻辑；同时把 `successFlag` 收紧为必填且只能为 `0/1`，确保结果摘要回传只按正式合同消费。`mvn -pl task-service -am -DskipTests compile` 已通过 | `task-service`、`接口设计.md` |
| A-121 | P2 | DONE | 收口 `log-report` 请求 DTO 残留文档外 `content` 别名，统一日志回传正式字段为 `logContent` | 已删除 `LogReportRequest` 中的 `getContent()/setContent()` 兼容别名，日志回传请求现在只保留正式字段 `logContent`；`mvn -pl task-service -am -DskipTests compile` 已通过 | `task-service`、`接口设计.md` |
| A-122 | P2 | DONE | 收口 `status-report` 请求 DTO 残留文档外 `status/reason` 兼容字段，统一运行态回传正式字段为 `toStatus/changeReason` | 已删除 `StatusReportRequest` 中的文档外 `status/reason` 兼容字段，并同步移除 `TaskLifecycleManager.pickStatus/pickReason` 的回退逻辑；`status-report` 现在只按正式字段 `fromStatus/toStatus/changeReason/operatorType` 消费。`mvn -pl task-service -am -DskipTests compile` 已通过 | `task-service`、`接口设计.md` |
| A-123 | P2 | DONE | 收口任务创建/编辑请求中设计外 `paramsJson` JSON 别名，统一前端合同字段为 `params` | 已删除 `CreateTaskRequest` 与 `UpdateTaskRequest` 中的 `@JsonAlias(\"paramsJson\")`，任务创建与编辑请求现在只保留正式字段 `params`；`mvn -pl task-service -am -DskipTests compile` 已通过 | `task-service`、`接口设计.md` |
| A-124 | P2 | DONE | 收口节点查询响应中残留的文档外 `effectiveLoad/loadRatio` 字段，统一节点列表/详情/可用节点正式返回合同 | 已移除 `scheduler-service` 中 `NodeListItemResponse`、`NodeDetailResponse`、`AvailableNodeResponse` 的设计外字段 `effectiveLoad/loadRatio`，并同步删除 `NodeAssembler`、`NodeAppService` 组装链路中的额外赋值逻辑；节点列表、详情与可用节点查询现在仅返回《接口设计》定义字段。`mvn -pl scheduler-service -am -DskipTests compile` 已通过 | `scheduler-service`、`接口设计.md` |
| A-125 | P2 | DONE | 收口 node-agent 下发/取消接口缺少正式请求校验的问题，避免不完整内部请求拖到执行阶段才失败 | 已为 `DispatchTaskRequest`、`CancelTaskRequest` 补齐最小必要 Bean Validation 约束，并在 `DispatchController` 的 `/internal/dispatch-task`、`/internal/cancel-task` 上启用 `@Valid`；基础执行元数据如 `taskId/taskNo/solverId/taskType/commandTemplate` 现会在接口层先行校验，非法请求不再拖到执行链路中途失败。`mvn -pl node-agent -am -DskipTests compile` 已通过 | `node-agent`、`接口设计.md` |
| A-126 | P2 | DONE | 清理 `scheduler-service/task-service` 中残留的运行中取消内部接口，避免首版保留未使用且设计外的 `/internal/nodes/{nodeId}/cancel-task` | 已移除 `scheduler-service` 的 `/internal/nodes/{nodeId}/cancel-task` 控制器入口、对应服务/管理方法，以及 `task-service` 中无调用方的 `SchedulerClient.cancelTaskOnNode(...)`；原 `NodeTaskCancelRequest.java` 已清空为“首版不支持运行中取消”的占位说明，不再提供可编译 DTO 类型。`mvn -pl task-service,scheduler-service -am -DskipTests compile` 已通过 | `task-service`、`scheduler-service`、`接口设计.md` |
| A-127 | P2 | DONE | 清理 `scheduler-service` 中残留的手工节点注册死链路，统一节点接入只走 `/api/node-agent/register` | 已移除 `NodeFacade/NodeManageManager/NodeAppService/NodeDomainService/NodeAssembler` 中仅服务于旧手工注册方式的无入口方法链，并把 `NodeRegisterRequest.java` 清空为“首版只支持 node-agent 自注册”的占位说明，避免继续保留看似可用的旧注册契约。`mvn -pl scheduler-service -am -DskipTests compile` 已通过 | `scheduler-service`、`接口设计.md` |
| A-128 | P2 | DONE | 清理 `scheduler-service` 中残留的无 token 心跳重载，统一节点心跳必须携带 `X-Node-Token` | 已移除 `NodeFacade/NodeManageManager/NodeAppService` 中无调用方的无 token `heartbeat(...)` 重载，节点心跳现在只保留带 `nodeToken` 的正式调用链，和“除注册外节点代理调用中心服务均需 `X-Node-Token`”的设计口径保持一致。`mvn -pl scheduler-service -am -DskipTests compile` 已通过 | `scheduler-service`、`接口设计.md` |
| A-129 | P2 | DONE | 补齐节点回传鉴权所需的 `/internal/nodes/{nodeId}/token/verify` 正式接口契约，避免实现必要接口停留在设计外 | 已在《接口设计》中把 `/internal/nodes/{nodeId}/token/verify` 纳入 scheduler-service 内部协作接口总表，并新增独立章节说明其调用方、`nodeToken` 查询参数、`true/false` 返回语义以及在 node-agent 回传 task-service 前的鉴权职责；当前实现与文档现已一致，无需额外代码改动。 | `task-service`、`scheduler-service`、`接口设计.md` |
| A-130 | P2 | DONE | 同步修订 `后端设计.md` 中 scheduler 节点接入章节的旧控制器拆分与缺失的 token 校验职责，避免和接口设计/现实现不一致 | 已将 `后端设计.md` 中旧表述 `NodeRegisterController`、`NodeHeartbeatController`、`InternalNodeCapacityController` 收口为当前正式结构：`NodeAgentController` 负责注册/心跳，`InternalNodeReservationController` 负责预占，`InternalSchedulerController` 负责调度内部协作与 `verifyNodeToken(...)`；并补充了 token 职责边界说明。 | `后端设计.md`、`scheduler-service`、`接口设计.md` |
| A-131 | P3 | DONE | 收口 `需求分析与系统设计.md` 中残留的“token 或 nodeKey”模糊安全表述，统一节点鉴权术语为 `nodeToken / X-Node-Token` | 已将 `需求分析与系统设计.md` 的安全性需求修订为“节点代理使用 `nodeToken` 鉴权，除注册接口外统一通过请求头 `X-Node-Token` 传递”，与当前《接口设计》、`后端设计.md` 和实现保持一致。 | `需求分析与系统设计.md`、`接口设计.md`、`后端设计.md` |
| A-132 | P3 | DONE | 清理 `后端设计.md` 附录/命名示例中残留的旧控制器名与 `paramsJson` 老字段，避免正文已修但示例继续误导 | 已同步修订 `后端设计.md` 的控制器清单、统一命名规范、scheduler 包树示例和命令构建说明：将旧名 `NodeRegisterController`、`NodeHeartbeatController`、`InternalNodeCapacityController`、`InternalNodeHeartbeatController` 收口为当前正式结构，并把命令变量示例 `paramsJson` 改为 `params`。 | `后端设计.md`、`scheduler-service`、`node-agent` |
| A-133 | P3 | DONE | 修订 `数据库设计.md` 中服务-数据表汇总漏写 `node_reservation` 的偏差，保证数据库设计汇总与真实落库一致 | 已将 `数据库设计.md` 的“服务与数据表对应”汇总修订为 `scheduler-service` 持有 `compute_node`、`node_reservation`、`node_solver_capability`、`schedule_record` 四张正式主表；现与建表 SQL 和实现一致。 | `数据库设计.md`、`docs/sql/init_databases.sql`、`scheduler-service` |
| A-134 | P1 | DONE | 收口 `solver-service` 中 `description/remark` 双口径漂移，统一求解器与文件规则说明字段语义 | 已将 `solver-service` 的求解器定义、文件规则领域模型、持久化 PO/Mapper、请求响应 DTO 以及 `task-service/common-lib` 的内部消费统一收口到正式字段 `description`；移除设计文档外的 `remark` 兼容口径，现与《接口设计》、`数据库设计.md` 及建表 SQL 一致，并通过 `mvn -pl common-lib,solver-service,task-service -am -DskipTests compile` 验证。 | `solver-service`、`task-service`、`common-lib`、`数据库设计.md`、`接口设计.md` |
| A-135 | P2 | DONE | 修订 SQL 文档与升级脚本中 `task_file.unpack_dir` 的旧语义注释，避免继续误导为“解压后工作目录” | 已将 `docs/sql/init_databases.sql` 与 `docs/sql/patch_zip_only_schema_upgrade.sql` 中 `task_file.unpack_dir` 注释统一改为“校验后真实执行目录，可等于 workdir 或其子目录”，与《数据库设计.md》、`接口设计.md`、状态流转文档和当前执行目录合同一致。 | `docs/sql/init_databases.sql`、`docs/sql/patch_zip_only_schema_upgrade.sql`、`数据库设计.md`、`接口设计.md` |
| A-136 | P2 | DONE | 清理 `后端设计.md` 中残留的旧节点接入 DTO 与方法签名，统一为当前 node-agent 自注册与 token 心跳合同 | 已将 `后端设计.md` 中旧 `register/heartbeat(NodeDTO)`、`NodeManageManager.register(NodeRegisterRequest)`、`SchedulerNodeClient.register(NodeDTO)`、`NodeDTO` 正式职责等残留改为当前正式结构：注册使用 `NodeAgentRegisterRequest`，心跳使用 `NodeHeartbeatRequest + X-Node-Token`，node-agent 客户端使用 `NodeInfo`，调度选点使用 `ComputeNode`。 | `后端设计.md`、`scheduler-service`、`node-agent`、`接口设计.md` |
| A-137 | P2 | DONE | 清理 `后端设计.md` 中离线补偿旧请求名与旧方法名，统一为 `NodeOfflineTasksRequest / markNodeOfflineTasksFailed` | 已将 `后端设计.md` 中 `NodeOfflineFailRequest` 与 `failTasksByOffline(...)` 全部收口为当前正式实现 `NodeOfflineTasksRequest` 与 `markNodeOfflineTasksFailed(...)`，和 `task-service` 代码及《接口设计》一致。 | `后端设计.md`、`task-service`、`scheduler-service`、`接口设计.md` |
| A-138 | P2 | DONE | 收口 `/internal/tasks/queued` 使用的 `TaskDTO` 中设计外 `paramsJson` 字段，统一内部调度载荷为 `params` | 已删除 `common-lib` 中 `TaskDTO.paramsJson` 及 task-service 对该字段的双写，`/internal/tasks/queued` 现仅暴露正式字段 `params`；同时同步修订 `common-lib.md` 中 `TaskDTO/FileRuleDTO` 的旧说明口径，保持模块说明与内部契约一致。`mvn -pl common-lib,task-service,scheduler-service -am -DskipTests compile` 已通过。 | `common-lib`、`task-service`、`scheduler-service`、`common-lib.md`、`接口设计.md`、`后端设计.md` |
| A-139 | P3 | DONE | 修订开发清单前文中已过期的“任务详情聚合状态历史/调度记录”结论，避免与 `A-57` 后的正式契约冲突 | 已将清单前文的过期表述收口为当前正式设计：`GET /api/tasks/{taskId}` 仅返回任务基础详情，`status-history` 与 `schedules` 通过独立接口查询，避免清单摘要继续误导后续审查与开发。 | `docs/核心设计文档对照审查与开发清单.md` |
| A-140 | P3 | DONE | 修订模块说明文档中残留的旧字段合同，避免 `solver-service.md`、`task-service.md` 继续描述已删除的兼容口径 | 已将 `solver-service.md` 中 `description/remark` 双口径残留收口为正式 `description` 口径，并将 `task-service.md` 中 `StatusReportRequest` 的说明修订为正式字段 `nodeId/fromStatus/toStatus/changeReason/operatorType`，避免模块说明继续误导联调与定位。 | `solver-service.md`、`task-service.md` |
| A-141 | P3 | DONE | 修订 `接口设计.md` 中管理员调优先级状态边界的过宽表述，避免与实现和状态规则表冲突 | 已将 `/api/admin/tasks/{taskId}/priority` 在核心契约总表、接口总规约表和详细章节中的状态约束统一收口为 `CREATED / VALIDATED / QUEUED`；不再使用“仅非终态任务”这种过宽表述，现与 `task-service` 实现及《状态流转校验规则表》一致。 | `接口设计.md`、`task-service`、`状态流转校验规则表.md` |
| A-142 | P3 | DONE | 同步修订 `状态流转校验规则表.md` 中管理员调优先级接口的冲突语义，避免与接口设计的“非允许状态返回冲突”表述不完全一致 | 已将 `POST /api/admin/tasks/{taskId}/priority` 在状态规则表中的冲突语义统一改为“非允许状态返回冲突”，与《接口设计》、`task-service` 实现及当前状态边界保持一致。 | `状态流转校验规则表.md`、`接口设计.md`、`task-service` |
| A-143 | P0 | DONE | 修复 node-agent 已接收任务后 `mark-dispatched` 失败的补偿缺口，避免任务停留 `SCHEDULED` 且预占残留 | 已将 `TaskScheduleJob` 的异常处理收口为：只要任务已经成功标记 `SCHEDULED`，后续无论是 node-agent 下发失败，还是 node-agent 已接收但 `mark-dispatched` 确认失败，都统一调用 scheduler-service 的 `handleDispatchFailure(...)`，复用 `dispatch-failed` 状态回写、预占释放与调度失败记录闭环；通过 `mvn -pl scheduler-service -am -DskipTests compile` 验证。 | `scheduler-service`、`接口设计.md`、`后端设计.md`、`任务状态流转实现表.md` |
| A-144 | P1 | DONE | 收口 `dispatch-failed` 迟到回写的终态幂等语义与节点一致性校验 | 已为 `TaskDispatchManager.markFailed(...)` 补齐“已 `FAILED` 的迟到回写仍需校验 `nodeId` 一致性”逻辑，并将《状态流转校验规则表》中 `dispatch-failed` 的冲突语义同步修订为“同一 `taskId + nodeId` 的终态迟到回写可幂等忽略，其他状态冲突”；通过 `mvn -pl task-service -am -DskipTests compile` 验证。 | `task-service`、`状态流转校验规则表.md`、`接口设计.md` |
| A-145 | P1 | DONE | 修复 `queueReason` 动态派生的最终兜底语义，避免把“已到队首但等待调度器处理”误显示为“前方仍有更高优先级任务” | 已将 `TaskQueryAppService.resolveQueueReason(...)` 的最终兜底文案修正为正式合同“排队中，等待调度器处理”，并同步把“前方仍有更高优先级或更早提交任务”分支文案补齐为与《接口设计》一致的完整表述；通过 `mvn -pl task-service -am -DskipTests compile` 验证。 | `task-service`、`接口设计.md`、`后端设计.md`、`需求分析与系统设计.md` |
| A-146 | P1 | DONE | 收紧 `task-service` 对单任务调度记录正式返回字段的消费校验，避免把缺字段调度记录继续对外透传 | 已在 `SchedulerClient.listTaskScheduleRecords(...)` 增加正式字段硬校验：`scheduleId/taskId/taskNo/strategyName/scheduleStatus/scheduleMessage/createdAt` 缺失将显式报错，且当 `nodeId` 非空时要求 `nodeName` 同步有效，同时校验返回记录 `taskId` 与请求任务一致；通过 `mvn -pl task-service -am -DskipTests compile` 验证。 | `task-service`、`scheduler-service`、`接口设计.md` |
| A-147 | P1 | DONE | 收紧 `queueReason` 依赖的节点快照消费校验，避免把 scheduler-service 坏回执误判为“暂无节点”或“节点满载” | 已收紧 `SchedulerClient.getQueueNodeSnapshot(...)`：`/internal/nodes/available` 现在会校验每条记录的 `nodeId/runningCount/reservedCount/maxConcurrency`，`/api/nodes` 会校验分页 `total/records` 以及节点记录中的 `nodeId/enabled/maxConcurrency`，坏回执将显式报错，不再被折算成 0；通过 `mvn -pl task-service -am -DskipTests compile` 验证。 | `task-service`、`scheduler-service`、`接口设计.md` |
| A-148 | P1 | DONE | 收紧任务查询名称补全链路的正式字段校验，避免 `solverName/profileName/nodeName/username` 空值继续对外透传 | 已收紧 `SolverClient.getSolverName/getProfileName`、`SchedulerClient.getNodeName`、`UserClient.getUsername`：当下游返回 `code=0` 但正式名称字段为空时，查询链路将显式报错，不再把空名称透传到任务列表、详情或管理员任务查询结果；通过 `mvn -pl task-service -am -DskipTests compile` 验证。 | `task-service`、`solver-service`、`scheduler-service`、`user-service`、`接口设计.md` |
| A-149 | P1 | DONE | 收紧驾驶舱节点汇总消费校验，避免坏节点记录被静默计入或忽略 | 已收紧 `SchedulerClient.getOnlineNodeSummary()`：`/api/nodes` 返回的每条节点记录现在都必须满足 `nodeId/enabled/runningCount/maxConcurrency` 完整，非法行会显式报错，不再被静默跳过或继续计入 `onlineNodeCount/avgNodeLoad`；通过 `mvn -pl task-service -am -DskipTests compile` 验证。 | `task-service`、`scheduler-service`、`接口设计.md` |
| A-150 | P1 | DONE | 收口 `/api/schedules` 对外查询参数仍暴露文档外 `strategyName` 筛选的合同漂移 | 已从 `SchedulePageQueryRequest` 与 `ScheduleRecordMapper` 中移除公开查询链路对 `strategyName` 的筛选支持，`/api/schedules` 现仅接受正式合同中的 `taskId/nodeId/scheduleStatus/startTime/endTime`；通过 `mvn -pl scheduler-service -am -DskipTests compile` 验证。 | `scheduler-service`、`接口设计.md` |
| A-151 | P1 | DONE | 收口任务/节点/调度查询接口对枚举与长度边界的弱校验，避免非法筛选值被当成空结果 | 已在 `TaskQueryBuilder`、`NodeAppService`、`ScheduleAppService` 中统一补齐查询参数规范化与枚举边界校验，并为公开查询 DTO 补充长度/范围约束；非法 `status/failType/scheduleStatus/node status/enabled` 现会显式报错，不再静默落入空结果；通过 `mvn -pl task-service -am -DskipTests compile` 与 `mvn -pl scheduler-service -am -DskipTests compile` 验证。 | `task-service`、`scheduler-service`、`接口设计.md`、`后端设计.md` |
| A-152 | P1 | DONE | 收口查询时间参数格式与范围校验，避免与全局时间合同 `yyyy-MM-dd HH:mm:ss` 脱节 | 已为 `/api/tasks`、`/api/admin/tasks`、`/api/schedules` 的 `startTime/endTime` 统一补齐 `yyyy-MM-dd HH:mm:ss` 绑定格式，并在任务查询与调度查询应用层增加 `startTime <= endTime` 校验，消除“任务查询走默认 ISO、调度查询裸字符串透传”的联调偏差；通过 `mvn -pl task-service -am -DskipTests compile` 与 `mvn -pl scheduler-service -am -DskipTests compile` 验证。 | `task-service`、`scheduler-service`、`接口设计.md` |
| A-153 | P1 | DONE | 修复 `task-service` 内部节点统计仍把公开分页接口当作一次性全量接口使用的合同偏差 | 已将 `SchedulerClient.getOnlineNodeSummary()` 改为按正式分页合同分批拉取 `/api/nodes`，单页固定使用公开上限 `200` 并循环直至拉满 `total`，避免继续依赖文档外 `pageSize=1000` 和单页全量假设；通过 `mvn -pl task-service -am -DskipTests compile` 验证。 | `task-service`、`scheduler-service`、`接口设计.md`、`后端设计.md` |
| A-154 | P1 | DONE | 收口 `/api/schedules` 分页默认排序仍按 `id DESC` 而非正式时间倒序的合同偏差 | 已将 `ScheduleRecordMapper.page(...)` 的公开分页排序收口为 `created_at DESC, id DESC`，与接口设计中“调度记录分页按时间倒序展示”的对外合同保持一致，同时保留单任务时间线 `createdAt ASC, id ASC` 不变；通过 `mvn -pl scheduler-service -am -DskipTests compile` 验证。 | `scheduler-service`、`接口设计.md` |
| A-155 | P1 | DONE | 收口全局接口响应时间格式仍未真正落地 `yyyy-MM-dd HH:mm:ss` 的实现偏差 | 已将 `common-lib` 中的 `JacksonConfig` 从占位实现补齐为正式全局 Jackson 配置，统一为 `LocalDateTime` 注册 `yyyy-MM-dd HH:mm:ss` 的序列化与反序列化规则，并把 `gateway-service` 的启动扫描范围收口到 `com.example.cae`，避免任务、调度、节点以及网关异常回包继续按默认 ISO 格式对外返回；通过 `mvn -pl common-lib,task-service,scheduler-service,gateway-service,solver-service,user-service -am -DskipTests compile` 验证。 | `common-lib`、`task-service`、`scheduler-service`、`gateway-service`、`solver-service`、`user-service`、`接口设计.md` |
| A-156 | P1 | DONE | 收口 `user-service/solver-service` 公开响应对象仍同时暴露 `id` 与 `userId/solverId/profileId/ruleId` 的字段语义漂移 | 已收口用户、求解器、模板、文件规则等外部管理响应中的同义双字段：登录回执保留文档约定 `userId`，管理详情/列表保留稳定 `id`，任务创建辅助、上传规范、创建回执和内部接口继续保留明确业务字段；同时 `task-service` 对内部文件规则读取兼容 `id/ruleId`，避免影响校验链路；通过 `mvn -pl user-service,solver-service,task-service -am -DskipTests compile` 验证。 | `user-service`、`solver-service`、`task-service`、`接口设计.md`、`后端设计.md` |
| A-157 | P1 | DONE | 同步求解器与模板查询接口分页合同，避免 `GET /api/solvers`、`GET /api/profiles` 实现与接口文档示例不一致 | 已将《接口设计》中 `GET /api/solvers` 调整为分页返回合同，并补齐 `GET /api/profiles` 分页查询模板接口正文，和 `solver-service` 的 `pageSolvers/pageProfiles` 实现以及《后端设计》保持一致；通过 `mvn -pl user-service,solver-service,task-service -am -DskipTests compile` 验证。 | `solver-service`、`接口设计.md`、`后端设计.md` |
| A-158 | P1 | DONE | 收口 `solver-service` 求解器/模板分页查询参数边界校验不足的合同偏差 | 已为 `GET /api/solvers`、`GET /api/profiles` 补齐 `enabled=0/1` 边界校验、字符串筛选长度约束和首尾空白归一化，并同步《接口设计》查询参数说明，避免非法筛选值静默落成空结果或低效模糊查询；通过 `mvn -pl solver-service -am -DskipTests compile` 验证。 | `solver-service`、`接口设计.md`、`后端设计.md` |
| A-159 | P1 | DONE | 收口 node-agent 注册能力未校验全局求解器定义存在的实现偏差 | 已在 `NodeAppService.registerNodeFromAgent(...)` 落库前调用 `solver-service` 校验每个 `solvers[].solverId` 必须存在于全局求解器定义，未知求解器直接拒绝注册，避免写入无法被平台识别的 `node_solver_capability` 脏能力记录；同步更新《接口设计》《后端设计》《需求分析与系统设计》《数据库设计》；通过 `mvn -pl scheduler-service -am -DskipTests compile` 验证。 | `scheduler-service`、`solver-service`、`接口设计.md`、`后端设计.md`、`需求分析与系统设计.md`、`数据库设计.md` |
| A-160 | P1 | DONE | 修复 `/internal/solvers/{solverId}` 内部响应仍返回 `id` 而调用方读取 `solverId` 的合同不一致 | 已为 `solver-service` 内部求解器详情接口新增 `InternalSolverDetailResponse`，内部合同明确返回 `solverId`，公开管理响应继续使用 `id`，避免 `scheduler-service` 的 `SolverClientStub` 在节点注册能力校验和调度校验中误判坏响应；同步更新《接口设计》《后端设计》；通过 `mvn -pl solver-service,scheduler-service -am -DskipTests compile` 验证。 | `solver-service`、`scheduler-service`、`接口设计.md`、`后端设计.md` |
| A-161 | P2 | DONE | 收口 `/internal/profiles/{id}` 文档路径参数与实现/调用方 `profileId` 语义不一致的合同歧义 | 已将《接口设计》内部模板接口统一为 `GET /internal/profiles/{profileId}`，补齐 `InternalProfileDetailResponse` 的关键返回字段，并在《后端设计》中补充 `ProfileController` 内部接口说明；该项为文档合同收口，不涉及代码和数据库结构变更。 | `接口设计.md`、`后端设计.md` |
| A-162 | P1 | DONE | 补齐旧库升级脚本未覆盖 `node_reservation`、`reserved_count` 和结果/输入文件幂等唯一约束的部署风险 | 已扩展 `docs/sql/patch_zip_only_schema_upgrade.sql` 的适用范围到 `scheduler_db`，补齐 `compute_node.reserved_count`、`node_reservation` 建表逻辑，并为 `task_file` 与 `task_result_file` 补充幂等唯一约束升级段；脚本保持可重复执行语义，并在加唯一键前保留最新元数据、清理重复元数据。该项为 SQL 脚本修正，不涉及 Java 编译。 | `docs/sql/patch_zip_only_schema_upgrade.sql`、`数据库设计.md` |
| A-163 | P1 | DONE | 补齐调度前模板归属与任务类型二次校验，避免队列任务在模板变更后被错误派发 | 已在 `scheduler-service` 的 `SolverClient.ProfileMeta` 和 `SolverClientStub` 中补齐 `taskType` 读取，并在 `ScheduleAppService.validateSolverAndProfile(...)` 调度前校验模板 `solverId` 必须等于任务 `solverId`、模板 `taskType` 必须匹配任务 `taskType`；同步更新《需求分析与系统设计》《接口设计》《后端设计》；通过 `mvn -pl scheduler-service -am -DskipTests compile` 验证。 | `scheduler-service`、`接口设计.md`、`后端设计.md`、`需求分析与系统设计.md` |
| A-164 | P1 | DONE | 补齐任务提交入队前对求解器/模板当前启用状态与归属关系的复核 | 已在 `TaskLifecycleManager.submitTask(...)` 中于 `VALIDATED -> QUEUED` 前复用 `validateTaskDefinition(...)`，重新校验求解器当前启用、模板当前启用、模板归属求解器和任务类型匹配，避免已校验任务在管理员禁用或调整模板后仍进入队列；同步更新《接口设计》《后端设计》《需求分析与系统设计》；通过 `mvn -pl task-service -am -DskipTests compile` 验证。 | `task-service`、`接口设计.md`、`后端设计.md`、`需求分析与系统设计.md` |
| A-165 | P1 | DONE | 显式收口 `dispatch-failed recoverable=true` 回队时不写任务主表失败字段的实现语义 | 已将 `TaskDispatchManager.markFailed(...)` 改为先计算目标状态：`recoverable=true` 仅执行回队流转并保留状态历史原因，`recoverable=false` 才写入任务主表 `failType/failMessage` 后进入 `FAILED`，不再依赖 `Task.requeue()` 的副作用清理失败字段；通过 `mvn -pl task-service -am -DskipTests compile` 验证。 | `task-service`、`任务状态流转实现表.md`、`状态流转校验规则表.md` |
| A-166 | P2 | DONE | 同步 `mark-dispatched` 迟到确认幂等语义在状态规则文档中的表述 | 已将《状态流转校验规则表》《任务状态流转实现表》同步为：正常只执行 `SCHEDULED -> DISPATCHED`，但同一 `taskId + nodeId` 的迟到确认若任务已进入 `RUNNING` 或终态，可幂等返回当前状态，不回退、不补写状态历史；并同步《后端设计》《需求分析与系统设计》，与《接口设计》和现有实现保持一致。 | `接口设计.md`、`后端设计.md`、`需求分析与系统设计.md`、`任务状态流转实现表.md`、`状态流转校验规则表.md`、`task-service` |
| A-167 | P1 | DONE | 放宽节点日志回传 `logContent` 空行校验，避免求解器输出空行导致日志线程中断 | 已将 `LogReportRequest.logContent` 从 `@NotBlank` 调整为 `@NotNull`，允许保存空字符串日志分片，避免 OpenFOAM 等求解器输出空行时被内部接口拒绝；同步《接口设计》《状态流转校验规则表》明确 `logContent` 必须存在但允许为空字符串；通过 `mvn -pl task-service -am -DskipTests compile` 验证。 | `task-service`、`node-agent`、`接口设计.md`、`状态流转校验规则表.md` |
| A-168 | P0 | DONE | 修复 node-agent 求解器成功后结果/终态回调失败导致任务长期停留 `RUNNING` 的状态闭环缺口 | 已在 `TaskReportManager.reportPostSuccessCallbackFailure(...)` 中增加终态兜底：求解器进程成功但结果摘要、结果文件或 `mark-finished` 回调持续失败时，记录本地错误后尝试通过 `mark-failed` 上报 `FAILED/RUNTIME_ERROR`，避免任务长期停留 `RUNNING`；若中心已进入终态，则依赖终态幂等保护不回退状态。同步《状态流转校验规则表》《后端设计》；通过 `mvn -pl node-agent -am -DskipTests compile` 验证。 | `node-agent`、`task-service`、`状态流转校验规则表.md`、`后端设计.md` |
| A-169 | P1 | DONE | 补齐结果文件 `fileType` 枚举校验，避免节点异常上报脏类型落库 | 已在 `TaskResultManager.saveResultFile(...)` 中使用 `ResultFileTypeEnum` 校验并规范化 `fileType`，只允许 `RESULT / LOG / REPORT / IMAGE`，非法类型返回 `BAD_REQUEST`，避免写入文档外结果类型；同步《接口设计》《状态流转校验规则表》《后端设计》；通过 `mvn -pl task-service -am -DskipTests compile` 验证。 | `task-service`、`common-lib`、`接口设计.md`、`状态流转校验规则表.md`、`后端设计.md`、`数据库设计.md` |
| A-191 | P1 | DONE | 修复嵌套结果文件展示名放开后下载侧仍要求 `fileName == storagePath` 末段，以及深层相对路径编码后可能超过 `file_name(255)` 的合同偏差 | 已将 `TaskResultAppService.openResultFile(...)` 下载校验改为只校验展示名为安全普通文件名、物理文件位于任务 `output/` 下且真实存在，不再要求展示名等于物理文件名；同时 node-agent 生成嵌套结果文件展示名时会替换路径分隔符和控制字符，超过 255 字符时保留前缀并追加短哈希，保证符合数据库与接口字段长度。 | `task-service`、`node-agent`、`接口设计.md`、`后端设计.md`、`需求分析与系统设计.md`、`数据库设计.md` |
| A-192 | P1 | DONE | 收口结果摘要/结果文件迟到回传可在任意终态写入，以及 node-agent 对终态幂等 ACK 过度严格的状态合同偏差 | 已将 `TaskResultManager` 的结果摘要和结果文件接收状态收紧为仅允许 `RUNNING / SUCCESS / TIMEOUT`，避免已 `FAILED / CANCELED` 的迟到节点回调继续写入结果；同时 `TaskReportClientImpl` 对 `mark-finished / mark-failed` 的 ACK 改为接受“中心已处于任一终态”的幂等返回，确保迟到失败回传不会因中心已先进入 `SUCCESS / TIMEOUT / FAILED / CANCELED` 而被误判为网关异常。 | `task-service`、`node-agent`、`接口设计.md`、`状态流转校验规则表.md`、`需求分析与系统设计.md` |
| A-193 | P2 | DONE | 收口结果文件 `fileName` 写入侧未拒绝控制字符，但下载侧已按安全规则拒绝，导致可上报不可下载的展示名安全偏差 | 已在 `TaskResultManager.saveResultFile(...)` 的路径校验阶段补齐 `fileName` 控制字符校验，与下载侧 `TaskResultAppService.openResultFile(...)`、接口设计和数据库说明保持一致；节点侧仍会主动替换控制字符，中心侧作为兜底拒绝非法展示名入库。 | `task-service`、`接口设计.md`、`需求分析与系统设计.md`、`数据库设计.md` |
| A-194 | P1 | DONE | 收口 node-agent 日志回传异常会使 stdout/stderr 读取线程退出，导致后续运行日志不可见的可观察性偏差 | 已在 `TaskReportManager.pushLog(...)` 增加日志分片短重试和降级处理：`log-report` 短暂失败会重试，持续失败只丢弃当前分片并写本地 warn，不再把异常抛出到日志读取线程，也不影响求解器主进程和终态回传。 | `node-agent`、`后端设计.md`、`状态流转校验规则表.md` |
| A-195 | P1 | DONE | 收口 node-agent 进程超时强杀、stdout/stderr 日志线程等待无边界，以及运行中取消被误报为 CANCELED 的执行生命周期偏差 | 已在 `ProcessRunner` 中为 stdout/stderr 读取线程命名并设置限时等待，超时路径会先强杀求解器进程、等待退出并收口日志线程后再上报 TIMEOUT；中断路径也会清理进程并限时等待日志线程。已在 `TaskExecuteManager` 中将本地运行时取消按首版状态合同转换为预运行失败或运行失败，不再尝试上报设计外 `RUNNING -> CANCELED`。`mvn -pl node-agent -am -DskipTests compile` 已通过。 | `node-agent`、`后端设计.md`、`状态流转校验规则表.md` |
| A-196 | P1 | DONE | 收口 node-agent 心跳 `runningCount` 将预运行 active task 也计入真实运行数，导致与 `reservedCount` 预占口径重复的容量模型偏差 | 已将 `TaskRuntimeRegistry` 拆分为本地并发使用的 `activeCount()` 与心跳上报使用的 `runningCount()`；任务只有在 `status-report` 成功后才标记为真实运行并计入心跳 `runningCount`，节点接收并发仍使用 active task 数限制。`mvn -pl node-agent -am -DskipTests compile` 已通过。 | `node-agent`、`后端设计.md`、`接口设计.md` |
| A-197 | P1 | DONE | 区分 node-agent 已接收任务后的 `mark-dispatched` 确认失败，避免 scheduler-service 误按派发失败主动回队 | 已在 `TaskScheduleJob` 中增加 `nodeAccepted` 分支：节点尚未 accepted 的派发失败仍走 `dispatch-failed`，节点已 accepted 后的 `mark-dispatched` 确认失败只记录调度失败，不主动回队；已同步《接口设计》《状态流转校验规则表》《后端设计》。`mvn -pl scheduler-service -am -DskipTests compile` 已通过。 | `scheduler-service`、`接口设计.md`、`状态流转校验规则表.md`、`后端设计.md` |
| A-198 | P1 | DONE | 收口节点离线补偿中状态回队与预占释放强事务耦合，避免释放预占失败导致任务长期停留 `SCHEDULED/DISPATCHED` | 已保留 scheduler-service `release-reservation` 无记录冲突语义，禁止静默修改 `reservedCount`；task-service 离线补偿中 `SCHEDULED/DISPATCHED -> QUEUED` 状态收口后改为尽力释放预占，释放失败只记录告警，不回滚任务状态补偿。`mvn -pl task-service -am -DskipTests compile` 已通过。 | `task-service`、`状态流转校验规则表.md`、`任务状态流转实现表.md`、`后端设计.md` |
| A-199 | P1 | DONE | 补齐 `node_reservation` 与 `compute_node.reserved_count` 的容量一致性重算入口，避免历史异常导致节点长期假满载或容量偏低 | 已在 scheduler-service 增加 `POST /internal/nodes/{nodeId}/reconcile-reservation`，以 `node_reservation.status=RESERVED` 为真值修正单节点 `compute_node.reserved_count`，仅修复汇总字段，不改预占明细与任务状态。`mvn -pl scheduler-service -am -DskipTests compile` 已通过。 | `scheduler-service`、`接口设计.md`、`后端设计.md`、`数据库设计.md` |
| A-200 | P1 | DONE | 补齐任务状态与预占明细的只读交叉审计能力，避免终态/回队任务仍持有 `RESERVED` 时只能靠人工查库定位 | 已新增 `POST /internal/nodes/{nodeId}/audit-reservation`，按单节点列出当前 `RESERVED` 明细，并通过 task-service 基础任务信息识别任务不存在、任务状态已非 `SCHEDULED / DISPATCHED`、任务绑定节点与预占节点不一致等问题；该接口只读审计，不修改 `node_reservation`、`compute_node` 或任务状态，后续修复仍通过正式释放、离线补偿或 `reconcile-reservation` 执行。 | `scheduler-service`、`task-service`、`common-lib`、`接口设计.md`、`后端设计.md`、`数据库设计.md` |
| A-201 | P1 | DONE | 补齐预占释放失败后的结构化调度审计记录，避免状态已收口但容量异常只能从服务日志排查 | 已为 task-service 调用 scheduler-service `release-reservation` 的严格/静默路径补齐 `schedule_record` 失败记录：当释放预占失败时写入 `scheduleStatus=FAILED`、`strategyName=FCFS_MIN_LOAD`、保留 `taskId/nodeId` 与失败说明；严格路径仍继续抛错，离线补偿等静默路径不回滚已完成状态收口。 | `task-service`、`scheduler-service`、`接口设计.md`、`后端设计.md`、`状态流转校验规则表.md` |
| A-202 | P1 | DONE | 细化调度失败记录的原因语义与列宽保护，避免“无可用节点”类失败只能看到笼统说明或因异常长消息导致审计落库失败 | 已将 scheduler-service 调度失败说明细分为无在线节点、无启用求解器能力、节点不可调度或容量不足、预占冲突、策略无候选等短说明；`confirmScheduleSuccess`、`recordScheduleFailure` 和 `/internal/schedules` 写入均统一按 `schedule_message` 255 字符列宽截断并补默认说明，保持调度审计可落库、可解释。 | `scheduler-service`、`接口设计.md`、`后端设计.md`、`数据库设计.md` |
| A-203 | P0 | DONE | 收口 `dispatch-failed` 与预占释放职责重复问题，避免 task-service 和 scheduler-service 双方同时释放同一预占 | 已移除 task-service `dispatch-failed` 状态回写后的直接 `release-reservation` 调用；该接口现在只负责状态校验、状态流转和状态历史落库，预占释放统一由 scheduler-service 在收到回写 ACK 后调用 `release-reservation`，离线补偿路径仍保持状态优先、静默释放预占。 | `task-service`、`scheduler-service`、`接口设计.md`、`后端设计.md`、`任务状态流转实现表.md`、`状态流转校验规则表.md` |
| A-204 | P1 | DONE | 清理 node-agent 执行前失败直连 task-service 的残留接口合同，避免 A-203 后文档和 task-service 控制器仍保留旧鉴权分支 | 复核确认 node-agent 代码已通过 scheduler-service 的 `/internal/tasks/{taskId}/dispatch-failed` 回调；已移除 task-service `dispatch-failed` 控制器中的可选 `X-Node-Token` 与节点鉴权分支，并将《接口设计》《状态流转校验规则表》统一为 node-agent 回调 scheduler-service、scheduler-service 校验 token 并编排状态回写与预占释放。 | `node-agent`、`task-service`、`scheduler-service`、`接口设计.md`、`状态流转校验规则表.md` |
| A-205 | P2 | DONE | 细化 node-agent 已 accepted 后 `mark-dispatched` 确认失败的调度记录语义，避免管理员误判为节点拒收或普通派发失败 | 已复核该分支不会立即释放预占或主动回队，后续依赖 node-agent 的 `RUNNING` 回传或执行前 `dispatch-failed` 兜底闭环；同时将 scheduler-service 记录的失败说明改为明确包含“node accepted task, mark-dispatched confirm failed”，并同步《接口设计》《后端设计》《状态流转校验规则表》。 | `scheduler-service`、`node-agent`、`接口设计.md`、`后端设计.md`、`状态流转校验规则表.md` |
| A-206 | P1 | DONE | 收口 `status-report(RUNNING)` ACK 丢失后的二次兜底，避免中心已进入 `RUNNING` 但 node-agent 按执行前失败清理本地任务后长期悬挂 | 已在 node-agent 的可恢复执行前失败路径中增加二级兜底：先走 scheduler-service `dispatch-failed(recoverable=true)` 回队；若回队因中心状态冲突被拒绝，说明 `RUNNING` 可能已落库但 ACK 丢失，则改用 `mark-failed(RUNTIME_ERROR)` 收口运行态任务，避免中心长期停留 `RUNNING` 而节点未启动求解器。 | `node-agent`、`task-service`、`scheduler-service`、`接口设计.md`、`后端设计.md`、`状态流转校验规则表.md` |
| A-207 | P1 | DONE | 收口 node-agent 失败回调说明长度，避免用于兜底的 `dispatch-failed/mark-failed` 因 255 字段校验失败而再次阻断状态闭环 | 已在 node-agent 统一归一化回调失败说明：运行失败、执行前派发失败、`RUNNING` ACK 歧义兜底、求解器成功后结果/终态回调失败都会补默认值并截断到 255 字符以内，再写入 `reason/failMessage`；确保失败收口链路符合接口和数据库字段长度合同。 | `node-agent`、`接口设计.md`、`后端设计.md`、`状态流转校验规则表.md` |
| A-208 | P1 | DONE | 收口 `RUNNING` 终态与离线补偿后的残留预占清理，避免 `status-report(RUNNING)` 曾释放失败时任务已终态但 `node_reservation` 仍停留 `RESERVED` | 已在 task-service 的 `mark-finished / mark-failed` 和节点离线补偿 `RUNNING -> FAILED(NODE_OFFLINE)` 成功落库后补齐二次预占释放；该动作仅用于修复历史残留预占，释放失败只记录调度审计，不回滚终态。同步《需求分析与系统设计》《接口设计》《后端设计》《任务状态流转实现表》《状态流转校验规则表》。 | `task-service`、`scheduler-service`、`需求分析与系统设计.md`、`接口设计.md`、`后端设计.md`、`任务状态流转实现表.md`、`状态流转校验规则表.md` |
| A-209 | P1 | DONE | 收口 `/internal/tasks/node-offline/fail` 未校验目标节点是否已 `OFFLINE` 的实现偏差，避免离线补偿入口被在线节点误触发 | 已在 task-service 的离线补偿入口前置调用 scheduler-service 节点详情查询，严格校验 `nodeId` 当前状态必须为 `OFFLINE`；若节点仍为 `ONLINE` 或状态未知则返回冲突，防止错误调用把在线节点上的任务误按离线补偿收口。该项为实现追平既有设计合同，无需修改核心设计文档正文。 | `task-service`、`scheduler-service` |
| A-210 | P1 | DONE | 补齐节点离线补偿的结构化调度审计，避免管理员只能从状态历史看到 `NODE_OFFLINE`，却无法在调度时间线上定位本轮调度是如何收口的 | 已在 task-service 的节点离线补偿路径中，对每条实际发生状态变更的任务补写 `schedule_record(FAILED)`；`SCHEDULED / DISPATCHED -> QUEUED` 与 `RUNNING -> FAILED(NODE_OFFLINE)` 均保留 `taskId/nodeId/scheduleMessage=node offline ...`。同步《需求分析与系统设计》《接口设计》《后端设计》《任务状态流转实现表》《状态流转校验规则表》；`mvn -pl task-service,node-agent,scheduler-service -am -DskipTests compile` 已通过。 | `task-service`、`scheduler-service`、`需求分析与系统设计.md`、`接口设计.md`、`后端设计.md`、`任务状态流转实现表.md`、`状态流转校验规则表.md` |
| A-211 | P1 | DONE | 区分求解器成功后的回调失败与普通运行失败，并清理失败任务残留的成功结果元数据 | 已新增统一失败类型 `CALLBACK_ERROR`，node-agent 在求解器成功但结果摘要、结果文件或 `mark-finished` 回调持续失败时，改用 `mark-failed(CALLBACK_ERROR)` 收口；task-service 在该终态成功落库后同步清理已写入的结果摘要与结果文件元数据，避免 `FAILED` 任务仍展示成功结果。同步《需求分析与系统设计》《后端设计》《接口设计》《数据库设计》《任务状态流转实现表》《状态流转校验规则表》；`mvn -pl task-service,node-agent,scheduler-service -am -DskipTests compile` 已通过。 | `common-lib`、`node-agent`、`task-service`、`需求分析与系统设计.md`、`后端设计.md`、`接口设计.md`、`数据库设计.md`、`任务状态流转实现表.md`、`状态流转校验规则表.md` |
| A-212 | P1 | DONE | 收口 `CALLBACK_ERROR` 结果元数据清理与终态事务耦合，避免清理失败把 `FAILED` 终态一并回滚回 `RUNNING` | 已将 task-service 在 `mark-failed(CALLBACK_ERROR)` 后的结果摘要/结果文件元数据清理改为尽力执行：清理失败只记录告警，不回滚已完成的 `FAILED` 终态；同步《需求分析与系统设计》《后端设计》《接口设计》《数据库设计》《任务状态流转实现表》《状态流转校验规则表》；`mvn -pl task-service,node-agent,common-lib -am -DskipTests compile` 已通过。 | `task-service`、`需求分析与系统设计.md`、`后端设计.md`、`接口设计.md`、`数据库设计.md`、`任务状态流转实现表.md`、`状态流转校验规则表.md` |
| A-213 | P1 | DONE | 收口节点离线补偿与部分成功结果元数据的残留偏差，避免 `FAILED(NODE_OFFLINE)` 任务仍展示离线前已写入的成功结果 | 已在 task-service 的节点离线补偿 `RUNNING -> FAILED(NODE_OFFLINE)` 路径补齐结果元数据清理：若离线前已写入结果摘要或结果文件元数据，终态落库后尽力清理；清理失败只记录告警，不回滚终态。同步《需求分析与系统设计》《后端设计》《接口设计》《数据库设计》《任务状态流转实现表》《状态流转校验规则表》；`mvn -pl task-service -am -DskipTests compile` 已通过。 | `task-service`、`需求分析与系统设计.md`、`后端设计.md`、`接口设计.md`、`数据库设计.md`、`任务状态流转实现表.md`、`状态流转校验规则表.md` |
---

## 9. 建议推进顺序

推荐按以下顺序继续开发：

1. 先做 `A-38`、`A-36`、`A-37`
2. 然后处理 `A-40`
3. 最后收尾 `A-23`、`A-26`、`A-15`

原因：

- `A-38`、`A-36`、`A-37` 直接影响前端对外接口和展示口径，适合在主状态链路收紧后尽快收口；
- `A-40` 不影响主流程是否能跑通，但它关系到错误码与冲突语义是否还能继续保持统一，适合在主合同收口后顺手清理；
- `A-23`、`A-26` 仍是遗留兼容接口清理项，`A-15` 属于结构整理项，可以放在本轮功能合同收口之后。

---

## 10. 本轮审查结论摘要

一句话结论：

当前项目已经完成了“从旧版任务链路到新版任务链路”的大部分主干改造，最新设计中的核心架构方向已经基本落地；其中 `node_reservation` 的返回契约、冲突语义、节点离线补偿分流、日志/结果上报状态阶段校验、管理员优先级状态边界、单有效输入压缩包合同、创建任务前置合法性校验、`mark-scheduled` 返回合同、任务文件/结果文件/调度记录字段口径、模板参数校验、任务校验错误码合同、状态回传幂等、单任务调度记录时间线、节点响应字段口径、`dispatch-failed` 的职责边界以及内部辅助接口设计口径都已完成第一轮收口。下文提到的 `mark-failed -> TIMEOUT` 逆向入口、离线补偿顺序与计数口径、驾驶舱统计口径、节点求解器启停返回体、调度记录前端权限边界、模板硬约束与裸数字错误码等问题，表示本轮审查过程中曾识别出的偏差项；其是否仍未收口，应以第 8 节正式开发清单中的最新状态为准，而不能仅以本段历史概述判断。

---

## 11. 更新记录

- `2026-04-21`：基于最新版本的《后端设计》《接口设计》《数据库设计》《需求分析与系统设计》《任务状态流转实现表》《状态流转校验规则表》，重新审查当前项目实现，并重写本文档。
- `2026-04-21`：确认 `A-01 ~ A-07`、`A-13` 已完成。
- `2026-04-21`：二次补充漏项审查后，新增 `A-16`、`A-17`、`A-18`，并确认 `需求分析与系统设计.md`、节点响应字段合同、`dispatch-failed` 释放职责、`mark-dispatched` 时序语义仍存在未收口点。
- `2026-04-21`：三次补充漏项审查后，新增 `A-19`、`A-20`、`A-21`，并确认重复管理员接口、日志/结果上报状态校验缺失、旧版 `running-count` 兼容接口仍属于当前清单遗漏。
- `2026-04-21`：四次补充漏项审查后，新增 `A-22`、`A-23`、`A-24`、`A-25`，并确认优先级调整状态边界过宽、多个服务残留文档外兼容状态接口、`task_file` 单有效压缩包合同缺少强约束、创建任务阶段尚未前置校验 `solverId / profileId / taskType` 合法性。
- `2026-04-21`：五次扫尾审查后，新增 `A-26`，并确认 `scheduler-service` 仍保留旧版节点注册/心跳内部入口，与最新节点代理接入设计不一致。
- `2026-04-21`：六次按固定维度（接口、数据库、状态机、网关、内部接口、模板驱动、字段合同）复核后，新增 `A-27`、`A-28`、`A-29`、`A-30`、`A-31`、`A-32`，并补充了本轮审查方法与覆盖矩阵，确认当前仍存在内部辅助接口未入文档、参数 schema 未落地、校验错误码漂移、`mark-scheduled` 返回契约缺口、`status-report` 幂等细节未收口、`id/fileId/scheduleId` 字段口径未统一等问题。
- `2026-04-22`：完成 `A-14`，`scheduler-service` 的 `reserve/release-reservation` 已返回结构化结果，`task-service` 调用链已增加结果校验，并通过 `mvn -pl scheduler-service,task-service -am -DskipTests compile`。
- `2026-04-22`：完成 `A-08`，节点离线补偿已按 `SCHEDULED/DISPATCHED -> QUEUED`、`RUNNING -> FAILED(NODE_OFFLINE)` 分流，并通过 `mvn -pl scheduler-service,task-service -am -DskipTests compile`。
- `2026-04-22`：完成 `A-09`，日志、结果摘要、结果文件已统一改为数据库级幂等处理，补充了 `task_result_file` 唯一约束初始化与补丁脚本，并通过 `mvn -pl task-service -am -DskipTests compile`。
- `2026-04-22`：完成 `A-28`，新增统一参数 schema 校验组件，已覆盖 `createTask`、`updateTask` 的部分参数校验与 `validateTask` 的完整参数校验，并通过 `mvn -pl task-service -am -DskipTests compile`。
- `2026-04-22`：完成 `A-29`，任务校验 `issues[].errorCode` 已收口到接口文档内稳定错误码，补齐了 `ARCHIVE_TOO_LARGE` 的校验链路产出，并通过 `mvn -pl task-service -am -DskipTests compile`。
- `2026-04-22`：完成 `A-31`，`status-report` 已按“同节点重复 RUNNING 回传优先幂等成功”的规则收口，并通过 `mvn -pl task-service -am -DskipTests compile`。
- `2026-04-22`：完成 `A-11`，已统一 `数据库设计.md`、`接口设计.md` 中残留旧模板示例，并确认 `scheduler-service` 的 `node_reservation` 汇总口径与修订版设计一致。
- `2026-04-22`：完成 `A-12`，已完成字段级终审，收口 `mark-scheduled/mark-dispatched` 仅接受 body `nodeId` 的内部合同，并通过 `mvn -pl task-service -am -DskipTests compile`。
- `2026-04-22`：完成 `A-18`，调度器已调整为 node-agent 接收成功后再回写 `DISPATCHED`，`mark-dispatched` 已返回 `taskId/nodeId/status` 结构，并通过 `mvn -pl scheduler-service,task-service -am -DskipTests compile`。
- `2026-04-22`：完成 `A-21`，已移除旧版 `/internal/nodes/{nodeId}/running-count` 兼容入口及 node-agent 对应空实现方法，并通过 `mvn -pl scheduler-service,node-agent -am -DskipTests compile`。
- `2026-04-22`：新增专题文档 [模板配置便利性评估与收敛方案](./模板配置便利性评估与收敛方案.md)，明确“模板描述差异、适配器承担共性”的后续模板开发约束，避免模板配置继续膨胀。
- `2026-04-22`：已同步修订《需求分析与系统设计》《数据库设计》《后端设计》《接口设计》中的模板配置口径，统一为“轻模板 + 适配器承担共性逻辑”的设计基线。
- `2026-04-22`：清理 `接口设计.md` 中重复的模板配置约束小节，并同步修订 `docs/sql/init_databases.sql` 的初始化模板示例，确保核心设计文档与配套 SQL 示例口径一致。
- `2026-04-22`：完成 `A-10`，`schedule_record` 已按 `created_at ASC, id ASC` 返回单任务调度记录，并通过 `mvn -pl scheduler-service -am -DskipTests compile`。
- `2026-04-22`：完成 `A-16`，节点列表与节点详情响应已移除兼容 `id` 字段、统一仅返回 `nodeId`，并通过 `mvn -pl scheduler-service -am -DskipTests compile`。
- `2026-04-22`：完成 `A-17`，`dispatch-failed` 已移除 task-service 内部的重复预占释放逻辑，并通过 `mvn -pl task-service -am -DskipTests compile`。
- `2026-04-22`：完成 `A-19`，已移除设计外重复管理员任务列表接口 `/api/tasks/admin`，统一收口到 `/api/admin/tasks`，并通过 `mvn -pl task-service -am -DskipTests compile`。
- `2026-04-22`：完成 `A-20`，`TaskResultManager` 已补齐日志/结果上报的状态阶段校验，`log-report` 允许 `DISPATCHED / RUNNING / 终态`，`result-summary-report` 与 `result-file-report` 仅允许 `RUNNING / 终态`，并通过 `mvn -pl task-service -am -DskipTests compile`。
- `2026-04-22`：完成 `A-22`，`TaskLifecycleManager.adjustPriority(...)` 已收紧为仅允许 `CREATED / VALIDATED / QUEUED` 调整优先级，并通过 `mvn -pl task-service -am -DskipTests compile`。
- `2026-04-22`：完成 `A-24`，`task_file` 已补齐 `(task_id, file_role, file_key)` 唯一约束、数据库级 upsert、上传事务与现网补丁脚本，并通过 `mvn -pl task-service -am -DskipTests compile`。
- `2026-04-22`：完成 `A-25`，`createTask(...)` 已前置校验 `solverId / profileId / taskType` 合法组合，并补充求解器/模板启用状态校验，已通过 `mvn -pl task-service -am -DskipTests compile`。
- `2026-04-22`：完成 `A-30`，`mark-scheduled` 已改为返回 `claimed/taskId/nodeId/status`，`scheduler-service` 已同步消费该合同，并通过 `mvn -pl scheduler-service,task-service -am -DskipTests compile`。
- `2026-04-22`：完成 `A-32`，任务文件、结果文件、调度记录响应已统一收口到 `fileId/scheduleId`，并通过 `mvn -pl scheduler-service,task-service -am -DskipTests compile`。
- `2026-04-22`：完成 `A-27`，已移除 `/internal/tasks/{taskId}/status` 的实际依赖，并将 `/internal/tasks/node-offline/fail` 正式补入 `接口设计.md`，通过 `mvn -pl scheduler-service,task-service -am -DskipTests compile`。
- `2026-04-22`：基于你新一轮修订后的《接口设计》《数据库设计》《后端设计》《需求分析与系统设计》《任务状态流转实现表》《状态流转校验规则表》再次逐维度复核项目，实现侧新增识别出 `A-33 ~ A-39`：包括 `mark-failed -> TIMEOUT` 逆向入口、内部状态接口返回体、离线补偿顺序与计数、驾驶舱统计口径、节点求解器启停返回体、调度记录接口合同与模板硬约束未落地等问题。
- `2026-04-22`：继续按固定 16 个审查方向完成 `D5 ~ D16` 复核，除 `A-33 ~ A-39` 外，新增识别出 `A-40`：`solver-service / task-service / node-agent` 仍散落直接写 `400/409` 的 `BizException` 魔法值，错误码与冲突语义尚未完全统一到共享常量；其余 `D11 ~ D15` 未再发现新的核心偏差。
- `2026-04-22`：完成 `A-33`，`mark-failed` 已固定只产出 `FAILED`，并显式拒绝 `failType=TIMEOUT`；通过 `mvn -pl task-service -am -DskipTests compile`。
- `2026-04-22`：完成 `A-34`，新增统一 `TaskStatusAckDTO`，`status-report / dispatch-failed / mark-finished / mark-failed` 已统一返回 `taskId/status` 结构化回执；通过 `mvn -pl task-service -am -DskipTests compile` 与 `mvn -pl scheduler-service,node-agent -am -DskipTests compile`。
- `2026-04-22`：完成 `A-35`，节点离线补偿已改为“先标记节点 `OFFLINE`，再触发 `/internal/tasks/node-offline/fail`”，且补偿返回值已收口为实际发生状态变更的任务数量；通过 `mvn -pl scheduler-service,task-service -am -DskipTests compile`。
- `2026-04-22`：完成 `A-39`，`solver-service` 已在模板创建/修改、`UploadSpec` 输出与内部模板详情链路强制校验 `ZIP_ONLY + 轻量 commandTemplate + 简单 ruleJson` 契约，`node-agent` 的 `CommandBuilder` 也已移除 `${workDir}` 支持并增加执行前模板边界校验；通过 `mvn -pl solver-service,node-agent -am -DskipTests compile`。
- `2026-04-22`：完成 `A-38`，新增 `TaskBasicDTO + /internal/tasks/basics` 内部任务基础信息查询，`scheduler-service` 调度记录响应已补齐 `taskNo`，并将 `/api/tasks/{taskId}/schedules` 收回 `task-service` 做任务归属/管理员权限校验；同时调整网关路由，编译通过 `mvn -pl gateway-service,scheduler-service,task-service -am -DskipTests compile`。
- `2026-04-22`：完成 `A-36`，驾驶舱 `avgNodeLoad` 已改为仅对在线且启用节点按 `runningCount / maxConcurrency` 求平均，不再混入 `reservedCount`；通过 `mvn -pl task-service -am -DskipTests compile`。
- `2026-04-22`：完成 `A-37`，节点求解器启停接口已补齐 `NodeSolverStatusResponse`，返回 `nodeId/solverId/enabled` 结构化结果；通过 `mvn -pl scheduler-service -am -DskipTests compile`。
- `2026-04-22`：完成 `A-40`，扫描范围内残留的 `new BizException(400/409, ...)` 已统一替换为共享错误码常量；通过 `mvn -pl task-service,node-agent -am -DskipTests compile`。
- `2026-04-22`：完成 `A-23` 与 `A-26`，已移除设计外的 `PUT .../status` 兼容入口，并删除旧版 `/internal/scheduler/nodes/register|heartbeat` 控制器，统一保留正式接口路径；通过 `mvn -pl user-service,solver-service,scheduler-service -am -DskipTests compile`。
- `2026-04-22`：完成 `A-15`，新增 `NodeCapacityManager` 统一节点预占/释放逻辑，`ScheduleAppService` 与 `NodeAppService` 已改为复用同一入口；通过 `mvn -pl scheduler-service -am -DskipTests compile`。
- `2026-04-22`：按“必须找到具体代码证明是否真的完成”的要求重新核验 `A-01 ~ A-40`，发现 `A-17`、`A-33`、`A-38` 虽标记 `DONE` 但存在跨服务闭环缺口，已先将三项状态回退为 `NEED_FIX`，待修复与编译验证后再恢复为 `DONE`。
- `2026-04-22`：完成 `A-33` 二次收口，node-agent 超时异常已改为走 `mark-finished(finalStatus=TIMEOUT)`，不再通过 `mark-failed(failType=TIMEOUT)` 回写；通过 `mvn -DskipTests compile`。
- `2026-04-22`：完成 `A-17` 二次收口，node-agent 在派发已接收但进入 `RUNNING` 前准备失败时，已在 `dispatch-failed` 回写成功后显式调用 scheduler-service 释放预占，补齐 `reservedCount/node_reservation` 闭环；通过 `mvn -DskipTests compile`。
- `2026-04-22`：完成 `A-38` 二次收口，已删除 scheduler-service 残留的 `/api/tasks/{taskId}/schedules` 对外接口，仅保留 task-service 侧带权限校验的正式入口；通过 `mvn -DskipTests compile`。
- `2026-04-22`：基于最新设计文档再次逐项核验代码实现，新增识别出 `A-41 ~ A-45`：包括调度下发竞态未完全收口、`dispatch-failed` 正式调用方边界仍未统一、请求 DTO 与数据库列宽不一致、初始化 SQL 示例数据过期，以及内部 HTTP 客户端未统一校验 `Result.code` 导致的“假成功”风险；后续修复以这些编号继续推进。
- `2026-04-22`：完成 `A-41 ~ A-45`，已收口调度下发竞态、`dispatch-failed` 正式调用边界、请求 DTO 列宽一致性、初始化 SQL 示例数据与内部 HTTP 客户端 `Result.code` 校验，并通过 `mvn -DskipTests compile` 完成跨模块编译验证。
- `2026-04-22`：继续复核后新增识别出 `A-46 ~ A-48`：分别对应调度器派发失败补偿兜底不足、`ruleJson` 合同与当前样例/实现能力不一致，以及模块说明文档残留旧接口路径；上述问题均已完成修复并再次通过 `mvn -DskipTests compile` 验证。
- `2026-04-22`：继续复核后新增识别出 `A-49`：查询型远程客户端仍存在“HTTP 成功但业务失败被空数据兜底”的残留；现已补齐 `task-service` 与 `scheduler-service` 相关客户端的 `Result.code` 校验，并再次通过 `mvn -DskipTests compile` 验证。
- `2026-04-22`：继续逐项核验 DTO 与建表合同，新增识别出 `A-50`：`CreateTaskRequest / UpdateTaskRequest / CreateSolverRequest / UpdateSolverRequest / CreateFileRuleRequest` 仍存在长度上限大于数据库列宽的漏项；现已按 `sim_task`、`solver_definition`、`solver_profile_file_rule` 正式列宽完成收口，并通过 `mvn -DskipTests compile` 验证。
- `2026-04-22`：继续按“网关入口合同 + 用户/节点基础主数据列宽”方向复核，新增识别出 `A-51`：`GatewayRouteConfig` 与 `gateway-service.md` 仍残留旧调度路径 `/api/scheduler/nodes/**`、`/api/scheduler/records/**`，且 `user-service`、`scheduler-service` 仍存在 `username/realName/nodeCode/nodeName/host/solverVersion` 列宽大于数据库字段的漏项；现已完成代码与文档收口，并通过 `mvn -DskipTests compile` 验证。
- `2026-04-22`：继续按“模块说明文档与联调清单口径”复核，新增识别出 `A-52`：`gateway-service.md`、`scheduler-service .md`、`docs/e2e-smoke-checklist.md` 仍把 `/api/tasks/{taskId}/schedules` 归到 `scheduler-service` 或写成网关单独拆路由；现已统一修订为由 `task-service` 对外承接该接口并完成权限校验。
- `2026-04-22`：继续按“内部客户端是否严格消费正式 DTO 合同”复核，新增识别出 `A-53`：`scheduler-service` 的 `TaskClientStub` 仍以 `Result.class + Map` 方式解析 `mark-scheduled / mark-dispatched / dispatch-failed / node-offline/fail` 返回体，存在弱类型解析和未检查转换风险；现已改为 `ParameterizedTypeReference<Result<...>>` 强类型消费，并通过 `mvn -DskipTests compile` 验证。
- `2026-04-22`：继续按“状态/调度字符串字段列宽”复核，新增识别出 `A-54`：`StatusReportRequest` 与 `InternalScheduleRecordRequest` 中仍有 `fromStatus/toStatus/operatorType/status/strategyName/scheduleStatus` 长度上限大于数据库字段宽度的漏项；现已完成收口，并通过 `mvn -DskipTests compile` 验证。
- `2026-04-22`：继续按“模板/结果上报枚举字段列宽”复核，新增识别出 `A-55`：`CreateProfileRequest / UpdateProfileRequest / CreateFileRuleRequest / UpdateFileRuleRequest / ResultFileReportRequest / MarkFinishedRequest` 中仍保留若干 32 长度上限，而对应数据库列宽为 30；现已完成收口，并通过 `mvn -DskipTests compile` 验证。
- `2026-04-22`：继续按“模块说明文档与模板占位符合同”复核，新增识别出 `A-56`：`node-agent .md` 仍把 `${workDir}` 写成当前支持的命令模板占位符，与正式设计及 `CommandBuilder` 实现不一致；现已修订为仅保留 `${taskDir}` 等正式占位符。
- `2026-04-22`：继续按“查询接口正式返回契约”复核，新增识别出 `A-57`：`GET /api/tasks/{taskId}` 当前实现仍额外嵌入 `statusHistory/scheduleRecords`，与已拆分的独立查询接口设计不一致，并让详情接口额外耦合状态历史与调度记录查询；现已完成收口，并通过 `mvn -DskipTests compile` 验证。
- `2026-04-22`：继续按“独立查询接口异常语义”复核，新增识别出 `A-58`：`GET /api/tasks/{taskId}/schedules` 当前实现仍会在调度服务调用失败时返回空列表，掩盖真实下游异常；现已移除该兜底逻辑，并通过 `mvn -DskipTests compile` 验证。
- `2026-04-22`：继续按“调度记录正式返回字段完整性”复核，新增识别出 `A-59`：`ScheduleAppService.fetchTaskBasics(...)` 当前仍会在 `task-service` 调用失败时返回空 `Map`，导致调度记录接口可能缺少文档要求的 `taskNo`；现已移除该兜底逻辑，并通过 `mvn -DskipTests compile` 验证。
- `2026-04-22`：继续按“任务查询正式返回字段完整性”复核，新增识别出 `A-60`：`/api/admin/tasks` 当前实现缺少正式契约要求的 `userId/username/failType`，且任务列表/详情中对 `solverName/profileName/nodeName` 仍以静默吞异常方式降级；现已新增管理员列表项返回模型与 `UserClient`，并收口名称字段补全语义，通过 `mvn -DskipTests compile` 验证。
- `2026-04-22`：继续按“驾驶舱轻量汇总接口异常语义”复核，新增识别出 `A-61`：`/api/admin/dashboard/summary` 当前实现会在 `scheduler-service` 统计失败时静默返回 `onlineNodeCount=0/avgNodeLoad=0`，把下游故障伪装成真实业务结果；现已移除 `TaskQueryAppService` 中的吞异常兜底，并同步收紧 `SchedulerClient.getOnlineNodeSummary()` 的响应结构校验，通过 `mvn -DskipTests compile` 验证。
- `2026-04-22`：继续按“调度输入合同完整性”复核，新增识别出 `A-62`：`/internal/tasks/queued` 当前实现会在拿不到 `solverMeta/profileExecutionMeta` 或 `paramsJson` 解析失败时继续返回降级后的 `TaskDTO`，存在把坏数据推进到调度与节点执行链路的风险；现已改为对执行元数据与参数 JSON 严格校验，不再静默降级，并通过 `mvn -DskipTests compile` 验证。
- `2026-04-22`：继续按“远程客户端是否仍在把坏响应转换成 `null/[]`”复核，新增识别出 `A-63`：`SolverClient` 与 `UserClient` 在 `code=0` 但 `data` 结构不符合正式契约时仍会静默返回 `null/[]`，导致查询、校验和名称补全链路继续出现“看似成功、实则字段缺失”的假象；现已统一改为显式抛错，并通过 `mvn -DskipTests compile` 验证。
- `2026-04-22`：继续按“本地查询组装是否仍在伪造空对象”复核，新增识别出 `A-64`：`TaskAssembler` 与 `TaskResultAssembler` 在解析已落库 `paramsJson/metricsJson` 失败时仍会返回空 `Map`，掩盖任务详情与结果摘要中的真实数据损坏；现已改为显式报错，并通过 `mvn -DskipTests compile` 验证。
- `2026-04-22`：继续按“调度记录时间线字段完整性”复核，新增识别出 `A-65`：`SchedulerClient` 在解析调度记录 `createdAt` 失败时仍会默默返回 `null`，可能让前端时间线混入不可信记录；现已改为严格校验时间字段，解析失败直接报错，并通过 `mvn -DskipTests compile` 验证。
- `2026-04-22`：继续按“节点管理 + 调度管理外部/内部接口正式契约完整性”方向复核，新增识别出 `A-66 ~ A-69`：分别对应 `scheduler-service` 内部任务客户端仍会在 `code=0` 但 `data` 缺失时伪造空列表/ACK、`SolverClientStub` 仍会把坏响应降级为 `null`、`/internal/schedules` 仍隐式默认 `FCFS_MIN_LOAD/UNKNOWN`，以及调度记录查询在 `nodeId` 非空时仍会静默漏掉 `nodeName`；现已完成代码与模块说明文档收口，并通过 `mvn -DskipTests compile` 验证。
- `2026-04-22`：继续按“node-agent 回调契约与执行链”方向复核，新增识别出 `A-70 ~ A-72`：分别对应节点代理对 `task-service` 状态回写 ACK 的弱类型消费、对 `scheduler-service` 注册/派发失败回执的弱类型消费与静默降级，以及首版不支持的运行中取消链路仍暴露为可调用半成品。
- `2026-04-22`：完成 `A-70 ~ A-72`，`node-agent` 已改为强类型消费 `task-service / scheduler-service` 的正式 ACK 合同，注册回执不再静默降级；同时首版运行中取消链路已统一改为显式返回“不支持”，并同步修订 `node-agent .md`、`scheduler-service .md`，通过 `mvn -DskipTests compile` 验证。
- `2026-04-22`：继续沿同一条“节点回调 -> 预占释放 -> 节点下发 ACK”链路下钻，新增识别出 `A-73 ~ A-74`：分别对应 `task-service` 对 `release-reservation` 结构化回执仍有空数据静默降级，以及 `scheduler-service` 对 `node-agent` 下发 ACK 仍使用 `Result.class + Map` 弱解析。
- `2026-04-22`：完成 `A-73 ~ A-74`，`release-reservation` 调用链已强制校验结构化回执，`scheduler-service` 下发节点任务也已改为强类型消费 `accepted/message` ACK，并通过 `mvn -DskipTests compile` 验证。
- `2026-04-23`：继续按“任务状态机与对外接口边界”方向复核，新增识别出 `A-75 ~ A-76`：分别对应 `/api/tasks/{taskId}/cancel` 仍返回 `Void` 而非 `taskId/status`，以及排队取消接口对已 `CANCELED` 任务尚未按设计实现幂等成功。
- `2026-04-23`：完成 `A-75 ~ A-76`，`/api/tasks/{taskId}/cancel` 已改为返回结构化 `taskId/status`，且对已 `CANCELED` 任务按幂等成功处理；通过 `mvn -pl task-service -am -DskipTests compile` 验证。
- `2026-04-23`：继续沿“任务状态机与对外接口边界”方向相邻复核，新增识别出 `A-77`：`/api/admin/tasks/{taskId}/priority` 仍返回 `Void`，和接口设计总表中的结构化回执要求不一致。
- `2026-04-23`：完成 `A-77`，`/api/admin/tasks/{taskId}/priority` 已改为返回结构化 `taskId/status/priority`，通过 `mvn -pl task-service -am -DskipTests compile` 验证。
- `2026-04-23`：继续沿任务创建链路复核外部返回体，新增识别出 `A-78`：`/api/tasks/{taskId}/files` 的上传成功回执仍缺少设计文档要求的 `status=CREATED` 字段。
- `2026-04-23`：完成 `A-78`，`TaskFileResponse` 已补齐 `status` 字段，上传回执会显式返回上传后任务状态；通过 `mvn -pl task-service -am -DskipTests compile` 验证。
- `2026-04-23`：继续沿同一条任务创建链路复核，新增识别出 `A-79`：`/api/tasks/{taskId}/submit` 的成功回执仍缺少接口设计总表要求的 `submitTime` 字段。
- `2026-04-23`：完成 `A-79`，`TaskSubmitResponse` 已补齐 `submitTime`，`submit/retry/cancel` 共用回执模型已统一带出该字段，并通过 `mvn -pl task-service -am -DskipTests compile` 验证。
- `2026-04-23`：继续按“任务查询与对外字段边界”相邻复核，新增识别出 `A-80`：`/api/tasks/{taskId}/status-history` 仍向前端返回文档外的 `id/taskId/operatorId`，存在对普通用户暴露内部操作人 ID 的偏差。
- `2026-04-23`：完成 `A-80`，`/api/tasks/{taskId}/status-history` 已裁剪为正式合同字段，不再向前端返回 `id/taskId/operatorId`，并通过 `mvn -pl task-service -am -DskipTests compile` 验证。
- `2026-04-23`：继续按同一方向复核日志与结果查询响应，新增识别出 `A-81`：日志分片与结果摘要/结果文件接口仍返回若干文档外内部字段，和正式对外合同不一致。
- `2026-04-23`：完成 `A-81`，`TaskLogResponse` 已移除 `id/taskId`，`TaskResultSummaryResponse` 已移除 `id/createdAt/updatedAt`，`TaskResultFileResponse` 已移除 `taskId`，并同步收口 `TaskLogAssembler`、`TaskResultAssembler` 映射逻辑；通过 `mvn -pl task-service -am -DskipTests compile` 验证。
- `2026-04-23`：继续沿“任务查询与对外字段边界”方向复核输入文件接口，新增识别出 `A-82`：`POST /api/tasks/{taskId}/files` 与 `GET /api/tasks/{taskId}/files` 当前共用 `TaskFileResponse`，导致上传回执与文件列表都暴露了若干文档外内部字段，正式合同边界未真正收口。
- `2026-04-23`：完成 `A-82`，已拆分 `TaskFileUploadResponse` 与 `TaskInputFileResponse`，分别收口上传回执和输入文件列表返回字段，并删除旧的宽 DTO `TaskFileResponse`；通过 `mvn -pl task-service -am -DskipTests compile` 验证。
- `2026-04-23`：继续沿“任务查询与对外字段边界”方向复核任务创建与管理动作回执，新增识别出 `A-83`：`TaskCreateResponse` 与 `TaskUpdateResponse` 仍比正式合同更宽，且普通编辑任务与管理员调优先级共用同一回执 DTO，导致对外字段语义混用。
- `2026-04-23`：完成 `A-83`，已移除 `TaskCreateResponse` 的文档外 `priority` 字段，普通编辑任务回执恢复为 `taskId/status`，管理员调优先级接口改为独立返回 `TaskPriorityUpdateResponse(taskId/status/priority)`；通过 `mvn -pl task-service -am -DskipTests compile` 验证。
- `2026-04-23`：继续沿“任务查询与对外字段边界”方向复核任务校验回执，新增识别出 `A-84`：`POST /api/tasks/{taskId}/validate` 在成功场景仍会额外返回文档外的 `issues=[]`，与“仅失败时返回 issues[]”的正式合同不一致。
- `2026-04-23`：完成 `A-84`，已为 `TaskValidateResponse` 增加空字段裁剪，并移除校验成功场景对 `issues` 的显式赋值；`POST /api/tasks/{taskId}/validate` 现仅在失败时返回 `issues[]`；通过 `mvn -pl task-service -am -DskipTests compile` 验证。
- `2026-04-23`：继续沿“任务查询与对外字段边界”方向复核动作型回执复用，新增识别出 `A-85`：`submit/cancel/retry` 当前共用 `TaskSubmitResponse`，导致 `cancel` 与 `retry` 也会额外暴露文档外的 `submitTime`。
- `2026-04-23`：完成 `A-85`，已拆分 `submit` 与 `cancel/retry` 的回执模型，`submit` 保留 `submitTime`，`cancel` 与 `retry` 改为返回仅含 `taskId/status` 的 `TaskActionResponse`；通过 `mvn -pl task-service -am -DskipTests compile` 验证。
- `2026-04-23`：继续沿“任务查询与对外字段边界”方向复核正式契约文档，新增识别出 `A-86`：`接口设计.md` 中 `POST /api/tasks/{taskId}/submit` 的总表已要求 `submitTime`，但详细章节返回示例仍停留在 `taskId/status`，文档内部口径不一致。
- `2026-04-23`：完成 `A-86`，已同步修订 `接口设计.md` 中 `POST /api/tasks/{taskId}/submit` 的详细章节返回示例，补齐 `submitTime`，现在与总表和后端实现一致。
- `2026-04-23`：切换到“查询参数与失败语义”固定审查方向后，新增识别出 `A-87`：`/api/tasks` 与 `/api/admin/tasks` 当前共用 `TaskListQueryRequest`，导致普通用户接口对外暴露了文档外的 `taskNo/profileId/nodeId/userId/failType` 查询参数，合同边界未真正拆开。
- `2026-04-23`：完成 `A-87`，已新增 `MyTaskListQueryRequest` 与 `AdminTaskListQueryRequest` 并切换控制器/查询服务入口，普通用户与管理员任务列表查询合同已拆分；通过 `mvn -pl task-service -am -DskipTests compile` 验证。
- `2026-04-23`：继续沿“查询参数与失败语义”方向复核动态派生字段，新增识别出 `A-88`：`queueReason` 在调用 `scheduler-service` 失败时仍会吞掉异常并伪装成普通排队说明，掩盖下游真实故障。
- `2026-04-23`：完成 `A-88`，已移除 `queueReason` 计算链路对 `scheduler-service` 异常的静默降级，列表/详情查询现在会对真实下游故障显式失败；通过 `mvn -pl task-service -am -DskipTests compile` 验证。
- `2026-04-23`：继续按“节点代理回调安全合同”方向复核，新增识别出 `A-92`：`node-agent -> scheduler-service` 的 `/internal/tasks/{taskId}/dispatch-failed` 虽然客户端已携带 `X-Node-Token`，但 `scheduler-service` 控制器侧仍未执行 token 鉴权，与《接口设计》中“除注册外其余节点代理回调中心接口均需鉴权”的规则不一致。
- `2026-04-23`：完成 `A-92`，已为 `scheduler-service` 的 `dispatch-failed` 回调入口补齐 `X-Node-Token` 必填校验，并复用 `NodeAppService.ensureValidNodeToken(...)` 对请求体 `nodeId` 做正式鉴权；通过 `mvn -pl scheduler-service -am -DskipTests compile` 验证。
- `2026-04-23`：继续沿同一安全合同复核 task-service 侧节点回调入口，新增识别出 `A-93`：`InternalTaskReportController` 虽然在运行时通过 `NodeAgentAuthService` 校验 token，但接口层仍把 `X-Node-Token` 标记为 `required=false`，与正式接口合同不一致。
- `2026-04-23`：完成 `A-93`，已将 `status-report / log-report / result-summary-report / result-file-report / mark-finished / mark-failed` 的 `X-Node-Token` 统一收紧为必填，并通过 `mvn -pl task-service -am -DskipTests compile` 验证。
- `2026-04-23`：继续沿“节点执行前状态回传容错”方向复核，确认 `A-94` 虽已提前写入清单，但实际代码尚未完成；随后已在 `TaskReportManager` 中把 `reportRunning(...)` 的重试范围扩展到 `RestClientException` 与 `BAD_GATEWAY` 等暂态失败，并将重试耗尽后的“运行前状态回传失败”改为通过 `dispatch-failed(recoverable=true)` 回退，而真实准备失败仍保持 `EXECUTOR_START_ERROR + recoverable=false`；通过 `mvn -pl node-agent -am -DskipTests compile` 验证。
- `2026-04-23`：继续沿“状态副作用与调度公平性”方向复核，新增识别出 `A-95`：`TaskStatusDomainService` 对所有流向 `QUEUED` 的迁移都复用 `Task.submit()`，导致 `SCHEDULED/DISPATCHED -> QUEUED` 的 recoverable 回队和节点离线补偿回队也会错误刷新 `submitTime`，破坏“优先级相同按原始 submitTime 升序”的设计规则。
- `2026-04-23`：完成 `A-95`，已将 `QUEUED` 迁移副作用按来源状态拆分：提交入队与管理员重试继续刷新 `submitTime`，而 recoverable 回队与节点离线补偿回队改为保留原始 `submitTime`，仅清理 `nodeId/fail/start/end` 等运行态字段；通过 `mvn -pl task-service -am -DskipTests compile` 验证。
- `2026-04-23`：继续沿“状态幂等与资源计数闭环”方向复核，新增识别出 `A-96`：`TaskLifecycleManager.reportStatus(...)` 在首次 `DISPATCHED -> RUNNING` 成功后若 `release-reservation` 暂态失败，会静默吞掉异常；而后续重复 `RUNNING` 回报又直接按幂等返回，无法再补释放一次安全的节点预占。
- `2026-04-23`：完成 `A-96`，已在幂等 `RUNNING` 回报分支中补做 `releaseReservationQuietly(...)`，利用释放接口自身的幂等保护补齐 `node_reservation/reservedCount` 修复路径；通过 `mvn -pl task-service -am -DskipTests compile` 验证。
- `2026-04-23`：继续沿同一“状态幂等与资源计数闭环”方向下钻，新增识别出 `A-97`：`TaskLifecycleManager.reportStatus(...)` 在正常 `DISPATCHED -> RUNNING` 分支仍对 `release-reservation` 采用静默模式，导致“任务已转 RUNNING、预占未释放、节点又拿到成功回执”的半成功场景不会再触发任何补偿重试。
- `2026-04-23`：完成 `A-97`，已将 `reportStatus(...)` 的正常 `RUNNING` 分支和幂等 `RUNNING` 分支都改为严格释放模式；释放失败会显式抛出，由 node-agent 沿既有重试链路重试整次状态回报，从而补齐 `reservedCount/node_reservation` 的最终一致性；通过 `mvn -pl task-service -am -DskipTests compile` 验证。
- `2026-04-23`：继续沿“用户可见路径字段口径”方向复核，新增识别出 `A-98`：任务输入文件查询接口中 `storagePath` 已做展示路径映射，但 `unpackDir` 仍原样返回，和接口文档里的统一路径口径不一致。
- `2026-04-23`：完成 `A-98`，已将 `TaskQueryAppService.toTaskInputFileResponse(...)` 中的 `unpackDir` 统一改为通过 `TaskStoragePathSupport.toDisplayTaskPath(...)` 输出；通过 `mvn -pl task-service -am -DskipTests compile` 验证。
- `2026-04-23`：继续沿“状态幂等与资源计数闭环”方向下钻，新增识别出 `A-99`：节点离线补偿中，`TaskDispatchManager.markNodeOfflineTasksFailed(...)` 对 `SCHEDULED/DISPATCHED -> QUEUED` 的预占释放仍采用静默模式，而 `NodeHeartbeatChecker` 只扫描 `ONLINE` 节点，导致一旦首次补偿出现“任务已回队、预占未释放”的半成功，后续也不会对已 `OFFLINE` 节点再次补偿，可能永久遗留 `reservedCount/node_reservation` 脏数据。
- `2026-04-23`：完成 `A-99`，已将离线补偿中的预占释放改为严格模式，释放失败会回滚整次回队事务；同时 `NodeHeartbeatChecker` 已补齐对 stale `OFFLINE` 节点的重复补偿扫描能力，从而保证首次补偿半成功后仍可重试收口；通过 `mvn -pl task-service,scheduler-service -am -DskipTests compile` 验证。
- `2026-04-23`：继续沿“接口字段正式合同与实现取值口径”方向复核，新增识别出 `A-100`：`status-report` 文档正式字段和 node-agent 调用均使用 `changeReason`，但 task-service 当前只读取 `reason`，导致按正式合同上报时状态历史说明可能为空。
- `2026-04-23`：完成 `A-100` 复核，确认 `TaskLifecycleManager.pickReason(...)` 已优先读取 `changeReason` 并兼容 `reason`，实际实现与文档和 node-agent 调用口径一致；通过 `mvn -pl task-service -am -DskipTests compile` 验证。
- `2026-04-23`：继续沿“上传覆盖与文件系统/数据库一致性”方向复核，新增识别出 `A-101`：重新上传 ZIP 时当前实现会先删除旧输入和工作目录，再保存新文件；一旦新文件保存或落库失败，数据库事务可回滚但旧输入文件已丢失，和真实使用场景下覆盖上传的安全性要求不匹配。
- `2026-04-23`：完成 `A-101`，已将覆盖上传流程改为先保存并落库新归档，再清理旧工作区与旧归档路径；同时新增 `deleteTaskPreparedArtifacts(...)` 精确清理 `workdir/logs/output`，不再在上传开始前删除整任务目录，并收紧失败回滚时的文件删除条件；通过 `mvn -pl task-service -am -DskipTests compile` 验证。
- `2026-04-23`：继续沿“调度策略与节点预占闭环”方向复核，新增识别出 `A-102`：`ScheduleAppService.scheduleTask(...)` 当前只对策略选出的首个节点尝试预占，一旦该节点被并发抢满，会直接抛 `NO_AVAILABLE_NODE` 结束本轮，不会继续尝试同批次其他候选节点。
- `2026-04-23`：完成 `A-102`，已为 `ScheduleStrategy` 增加候选节点排序合同，`FcfsLeastLoadStrategy` 输出稳定有序候选列表，`ScheduleAppService.scheduleTask(...)` 改为按序逐个预占并仅对 `NO_AVAILABLE_NODE` 类型失败继续换点尝试；通过 `mvn -pl scheduler-service -am -DskipTests compile` 验证。
- `2026-04-23`：继续沿“状态流转校验边界”方向复核，新增识别出 `A-103`：`TaskDispatchManager.markFailed(...)` 当前会把已进入 `RUNNING` 的任务也按 `dispatch-failed` 幂等成功返回，掩盖迟到派发失败回调与真实运行态冲突的问题。
- `2026-04-23`：完成 `A-103`，已收紧 `dispatch-failed` 状态分支：`RUNNING` 任务现在会按状态冲突拒绝，终态与已回队任务仍保留既有幂等语义；通过 `mvn -pl task-service -am -DskipTests compile` 验证。
- `2026-04-23`：继续沿“节点离线补偿事务边界”方向复核，新增识别出 `A-104`：`markNodeOfflineTasksFailed(...)` 当前把多条任务状态更新与多次 `release-reservation` 放在同一事务中，若后续某条释放失败，会出现前面外部预占已释放、整批本地状态却一起回滚的跨服务不一致。
- `2026-04-23`：完成 `A-104`，已将节点离线补偿改为按任务逐条执行独立事务，确保单条任务内的状态回写与预占释放共同提交或共同回滚，单条失败不会污染整批已成功处理结果；通过 `mvn -pl task-service -am -DskipTests compile` 验证。
- `2026-04-23`：继续沿“任务文件目录合同与上传边界”方向复核，新增识别出 `A-105`：`LocalTaskFileStorageService.saveInputFile(...)` 直接信任上传的 `originalFilename` 拼接存储路径，缺少 basename 清洗与目录边界校验，理论上可突破任务 `input` 目录。
- `2026-04-23`：完成 `A-105`，已为输入归档上传增加文件名清洗与目标路径 `startsWith(inputDir)` 校验，非法路径片段会被直接拒绝，确保任务输入文件始终落在任务专属目录内；通过 `mvn -pl task-service -am -DskipTests compile` 验证。
- `2026-04-23`：继续沿“覆盖上传持久化安全性”方向复核，新增识别出 `A-106`：虽然覆盖上传流程已改为“先保存新文件再清理旧产物”，但物理存储仍使用原文件名，同名上传时仍会先把旧归档文件直接覆盖，数据库失败后无法恢复旧内容。
- `2026-04-23`：完成 `A-106`，已将上传归档的物理存储名改为随机唯一文件名，原始文件名仅保留在 `originName` 字段中，从而消除同名覆盖上传先破坏旧归档内容的问题；通过 `mvn -pl task-service -am -DskipTests compile` 验证。
- `2026-04-23`：继续沿“轻量命令模板运行时约束”方向复核，新增识别出 `A-107`：虽然模板文本本身已禁止复杂 shell 片段，但 `CommandBuilder` 未校验占位符替换值，任务参数等变量仍可通过取值本身把轻量模板扩展成复杂脚本。
- `2026-04-23`：完成 `A-107`，已在 `node-agent` 的 `CommandBuilder` 中增加模板变量值校验，替换前会拒绝带有换行、管道、重定向、命令替换等 shell 控制片段的变量值；通过 `mvn -pl node-agent -am -DskipTests compile` 验证。
- `2026-04-23`：继续沿“结果解析器扩展点是否真正落地”方向复核，新增识别出 `A-108`：`parserName` 虽已从 profile 透传到 node-agent，但执行完成后仍由 `CommandTemplateExecutor` 直接硬编码生成摘要和指标，没有结果解析器注册与选择机制。
- `2026-04-23`：完成 `A-108`，已在 `node-agent` 增加 `ResultParser` 扩展接口、通用解析器和解析器选择服务，`CommandTemplateExecutor` 现在会按 `parserName` 选择解析器生成执行结果，后续新增专用解析器无需改主流程；通过 `mvn -pl node-agent -am -DskipTests compile` 验证。
- `2026-04-23`：继续沿“节点目录合同与执行目录语义”方向复核，新增识别出 `A-109`：`task-service` 仍使用 `log/` 而非设计要求的 `logs/`，`node-agent` 也未创建 `meta/`，且 `WorkDirManager` 把运行时 `workDir/taskDir` 都指向任务根目录；当 `unpackDir` 为 `workdir` 子目录时，会把 `${taskDir}`、`outputDir`、`logDir` 的语义一并带偏。
- `2026-04-23`：完成 `A-109`，已统一 `task-service` 与 `node-agent` 的任务目录合同为 `input/workdir/output/logs/meta`；`node-agent` 现在会把内部 `workDir` 固定到 `.../workdir`，`${taskDir}` 优先复用校验得到的 `unpackDir`，并按路径中的 `workdir` 段稳定反推出任务根目录，兼容 `unpackDir` 等于 `workdir` 或其子目录两种场景；通过 `mvn -pl task-service,node-agent -am -DskipTests compile` 验证。
- `2026-04-23`：继续沿“结果文件上报与共享目录边界”方向复核，新增识别出 `A-110`：`result-file-report` 当前只校验任务状态，不校验上报的 `storagePath` 是否位于当前任务 `output/` 下，也不校验共享目录中是否存在该文件，可能把不可下载或越界的结果路径写入 `task_result_file`。
- `2026-04-23`：完成 `A-110`，已在 `TaskResultManager.saveResultFile(...)` 中补齐 `output/` 目录边界、真实文件存在性和展示名安全校验，并改为以实际文件大小落库；后续 `A-190/A-191` 已进一步明确 `fileName` 是展示名，不要求等于 `storagePath` 物理路径末段；通过 `mvn -pl task-service -am -DskipTests compile` 验证。
- `2026-04-23`：继续沿“节点终态回传与状态来源清晰性”方向复核，新增识别出 `A-111`：node-agent 在求解器返回成功后，如果结果摘要、结果文件或 `mark-finished` 回传异常，会落入外层统一异常处理并调用 `mark-failed`，把回传链路问题误记为求解运行失败。
- `2026-04-23`：完成 `A-111`，已在 node-agent 中区分求解成功后的回传异常与真实运行失败，并为成功/失败/超时终态回传补充统一重试；求解器实际成功后不会再因摘要、结果文件或终态回调异常被误写为 `FAILED`；通过 `mvn -pl node-agent -am -DskipTests compile` 验证。
- `2026-04-23`：继续沿“scheduler -> node-agent 调用安全合同”方向复核，新增识别出 `A-112`：`/internal/dispatch-task` 与 `/internal/cancel-task` 当前未执行 `X-Node-Token` 鉴权，scheduler-service 调用时也未携带 token，与《接口设计》中节点代理接口统一 token 鉴权的规则不一致。
- `2026-04-23`：完成 `A-112`，已为 node-agent 的 `/internal/dispatch-task`、`/internal/cancel-task` 补齐 `X-Node-Token` 鉴权，并让 scheduler-service 调用链同步携带节点 token；通过 `mvn -pl scheduler-service,node-agent -am -DskipTests compile` 验证。
- `2026-04-23`：继续沿“公共异常层错误码口径”方向复核，新增识别出 `A-113`：虽然业务服务中的 `400/409` 裸数字大多已清理，但 `common-lib` 的 `GlobalExceptionHandler` 和 `BizException(String)` 默认构造仍直接写裸数字，说明 A-40 在共享层仍有遗漏。
- `2026-04-23`：完成 `A-113`，已为 `common-lib` 补齐 `INTERNAL_SERVER_ERROR` 常量，并把 `BizException(String)` 与 `GlobalExceptionHandler` 中残留的 `400/401/403/404/500` 全部收口到共享错误码常量；通过 `mvn -pl common-lib -am -DskipTests compile` 验证。
- `2026-04-23`：继续沿“网关异常层错误码口径”方向复核，新增识别出 `A-114`：`gateway-service` 的 `GatewayExceptionHandler` 仍直接写 `401/403/500`，并在 JSON 序列化兜底时手写了 `{\"code\":500,...}`，与共享错误码常量口径未完全统一。
- `2026-04-23`：完成 `A-114`，已将 `GatewayExceptionHandler` 中的异常响应与 JSON 兜底体统一切换到共享错误码常量；通过 `mvn -pl gateway-service -am -DskipTests compile` 验证。
- `2026-04-23`：继续沿“节点注册/心跳/事实上报合同”方向复核，新增识别出 `A-115`：`NodeAppService.registerNodeFromAgent(...)` 当前会先复用 `registerNode(...)` 执行一次基于 `solverIds` 的能力替换，再执行一次带版本 `solvers` 的能力替换，导致节点注册链路重复写能力表且不具备单事务原子性。
- `2026-04-23`：完成 `A-115`，已将 `registerNode(...)` 与 `registerNodeFromAgent(...)` 收口为事务化注册入口，并拆出单次 `saveOrUpdateNode(...)`；节点代理注册现在会直接按带版本的 `solvers` 一次性同步事实能力，不再先写一轮无版本能力。通过 `mvn -pl scheduler-service -am -DskipTests compile` 验证。
- `2026-04-23`：继续沿同一方向复核，新增识别出 `A-116`：`node-agent` 在缺失版本配置时会伪造 `solverVersion=\"v1\"` 上报，且注册链路未显式校验重复 `solverId`，与“节点注册上报本机实际求解器及版本”的正式设计不一致。
- `2026-04-23`：完成 `A-116`，已在 `SchedulerNodeClientImpl` 中补齐本地求解器配置校验，缺失版本或重复 `solverId` 时直接失败，不再伪造默认版本；同时 `scheduler-service` 侧已为节点注册请求补齐重复 `solverId` / 空版本校验，避免以数据库唯一约束异常兜底。通过 `mvn -pl node-agent -am -DskipTests compile` 与 `mvn -pl scheduler-service -am -DskipTests compile` 验证。
- `2026-04-23`：继续沿“调度策略与状态确认合同”方向复核，新增识别出 `A-117`：`TaskScheduleJob` 当前仅单次调用 `mark-scheduled` 与 `mark-dispatched`，若 task-service 已成功持久化但响应在网络层丢失，调度器会误把本轮调度当作失败，出现错误释放预占或错误失败审计。
- `2026-04-23`：完成 `A-117`，已为 `mark-scheduled` 与 `mark-dispatched` 增加显式重试，并为 `mark-scheduled` 补齐“同节点已进入后续状态”的幂等恢复判定，确保调度器真正按接口文档中的网络重试合同消费内部接口；通过 `mvn -pl scheduler-service -am -DskipTests compile` 验证。
- `2026-04-23`：继续沿“节点执行前失败回调闭环”方向复核，新增识别出 `A-118`：`node-agent` 的执行前失败只单次调用 scheduler-service `dispatch-failed`，若回调链路瞬时失败，任务可能无法回队或失败，节点预占也可能不能及时释放。
- `2026-04-23`：完成 `A-118`，已为 `TaskReportManager.reportPreRunFailure(...)` 增加 `dispatch-failed` 有限重试封装，网络异常和 `BAD_GATEWAY` 会重试，真实业务冲突不盲目重试；通过 `mvn -pl node-agent -am -DskipTests compile` 验证。
- `2026-04-23`：继续沿“结果查询与下载边界”方向复核，新增识别出 `A-119`：结果文件下载接口当前直接信任库内 `storagePath`，若存在旧脏数据或异常路径，理论上可能越过任务 `output/` 目录边界读取文件。
- `2026-04-23`：完成 `A-119`，已在 `TaskResultAppService.openResultFile(...)` 下载前补齐 `output/` 目录边界、文件名一致性和真实存在性校验，越界路径与缺失文件统一按 `RESULT_FILE_NOT_FOUND` 收口；通过 `mvn -pl task-service -am -DskipTests compile` 验证。
- `2026-04-23`：继续沿“结果摘要回传字段合同”方向复核，新增识别出 `A-120`：`result-summary-report` 的正式字段已统一为 `successFlag`，但 task-service 请求 DTO 与保存逻辑仍兼容文档外 `success` 布尔字段，形成双口径残留。
- `2026-04-23`：完成 `A-120`，已删除 `ResultSummaryReportRequest` 中的 `success` 兼容字段，并把 `successFlag` 收紧为必填且仅允许 `0/1`；`TaskResultManager.saveResultSummary(...)` 现只按正式字段 `successFlag` 落库。通过 `mvn -pl task-service -am -DskipTests compile` 验证。
- `2026-04-23`：继续沿“日志回传字段合同”方向复核，新增识别出 `A-121`：`log-report` 的正式字段只有 `logContent`，但 `LogReportRequest` 仍保留文档外 `content` 兼容别名，存在多口径残留。
- `2026-04-23`：完成 `A-121`，已删除 `LogReportRequest` 中的 `content` 兼容别名，日志回传请求只保留正式字段 `logContent`；通过 `mvn -pl task-service -am -DskipTests compile` 验证。
- `2026-04-23`：继续沿“状态回传字段合同”方向复核，新增识别出 `A-122`：`status-report` 的正式字段已统一为 `toStatus/changeReason`，但 `StatusReportRequest` 与 `TaskLifecycleManager` 仍兼容文档外 `status/reason` 字段，存在双口径残留。
- `2026-04-23`：完成 `A-122`，已删除 `StatusReportRequest` 中的 `status/reason` 兼容字段，并移除 `TaskLifecycleManager.pickStatus/pickReason` 的回退逻辑，`status-report` 现只按正式字段合同消费；通过 `mvn -pl task-service -am -DskipTests compile` 验证。
- `2026-04-23`：继续沿“任务创建/编辑请求字段合同”方向复核，新增识别出 `A-123`：`POST /api/tasks` 与 `PUT /api/tasks/{taskId}` 的正式字段已统一为 `params`，但请求 DTO 仍保留设计外 `paramsJson` 的 `JsonAlias` 兼容入口。
- `2026-04-23`：完成 `A-123`，已删除 `CreateTaskRequest` 与 `UpdateTaskRequest` 中的 `paramsJson` 兼容别名，请求现在只保留正式字段 `params`；通过 `mvn -pl task-service -am -DskipTests compile` 验证。
- `2026-04-23`：继续沿“scheduler-service 节点查询响应合同”方向复核，新增识别出 `A-124`：节点列表、节点详情、可用节点响应仍暴露设计文档未定义的 `effectiveLoad/loadRatio`，存在前后端围绕设计外字段耦合的风险。
- `2026-04-23`：完成 `A-124`，已从 `NodeListItemResponse`、`NodeDetailResponse`、`AvailableNodeResponse` 及其组装链路中移除设计外 `effectiveLoad/loadRatio` 字段，节点查询响应现仅保留《接口设计》正式字段；通过 `mvn -pl scheduler-service -am -DskipTests compile` 验证。
- `2026-04-23`：继续沿“node-agent 下发/取消接口请求合同”方向复核，新增识别出 `A-125`：`DispatchController` 当前未对 `DispatchTaskRequest`、`CancelTaskRequest` 执行 `@Valid` 校验，且 DTO 几乎没有正式必填约束，不完整请求可能拖到执行阶段才失败。
- `2026-04-23`：完成 `A-125`，已为 `DispatchTaskRequest`、`CancelTaskRequest` 补齐最小必要 Bean Validation 约束，并在 `DispatchController` 的 `/internal/dispatch-task`、`/internal/cancel-task` 上启用 `@Valid`；通过 `mvn -pl node-agent -am -DskipTests compile` 验证。
- `2026-04-23`：继续沿“首版不支持运行中取消的接口残留”方向复核，新增识别出 `A-126`：`task-service` 中 `SchedulerClient.cancelTaskOnNode(...)` 无任何调用方，但 `scheduler-service` 仍对外保留设计外 `/internal/nodes/{nodeId}/cancel-task` 及对应服务链路，和首版状态合同不一致。
- `2026-04-23`：完成 `A-126`，已移除 `scheduler-service` 的 `/internal/nodes/{nodeId}/cancel-task` 控制器入口、对应服务/管理方法，以及 `task-service` 中无调用方的 `SchedulerClient.cancelTaskOnNode(...)`；并把 `NodeTaskCancelRequest.java` 清空为首版占位说明，避免继续提供可用 DTO 类型。通过 `mvn -pl task-service,scheduler-service -am -DskipTests compile` 验证。
- `2026-04-23`：继续沿“节点接入正式口径”方向复核，新增识别出 `A-127`：`scheduler-service` 中仍残留 `NodeRegisterRequest -> registerNode(...)` 这套手工节点注册死链路，但当前既无控制器入口，也与最新设计中“节点仅通过 `/api/node-agent/register` 自注册上报”不一致。
- `2026-04-23`：完成 `A-127`，已移除 `NodeFacade/NodeManageManager/NodeAppService/NodeDomainService/NodeAssembler` 中仅服务于旧手工节点注册的无入口方法链，并把 `NodeRegisterRequest.java` 清空为首版占位说明；通过 `mvn -pl scheduler-service -am -DskipTests compile` 验证。
- `2026-04-23`：继续沿“节点心跳鉴权正式口径”方向复核，新增识别出 `A-128`：`scheduler-service` 仍保留无 token 的 `heartbeat(...)` 重载方法，虽然当前无调用方，但语义上弱化了“除注册外节点代理调用中心服务均需 `X-Node-Token`”的正式设计。
- `2026-04-23`：完成 `A-128`，已移除 `NodeFacade/NodeManageManager/NodeAppService` 中无调用方的无 token 心跳重载，节点心跳链路现在只保留携带 `nodeToken` 的正式调用路径；通过 `mvn -pl scheduler-service -am -DskipTests compile` 验证。
- `2026-04-23`：继续沿“节点回传鉴权正式合同”方向复核，新增识别出 `A-129`：`task-service` 当前需要调用 scheduler-service 的 `/internal/nodes/{nodeId}/token/verify` 校验节点回传 token，但该必要内部接口尚未纳入《接口设计》的正式契约。
- `2026-04-23`：完成 `A-129`，已在《接口设计》的 scheduler-service 内部协作接口总表和详细章节中补齐 `/internal/nodes/{nodeId}/token/verify`，明确调用方、查询参数、返回语义和鉴权职责；当前代码实现与文档合同已一致。
- `2026-04-23`：继续沿“核心设计文档交叉一致性”方向复核，新增识别出 `A-130`：`后端设计.md` 的 scheduler 节点接入章节仍保留旧控制器拆分 `NodeRegisterController / NodeHeartbeatController / InternalNodeCapacityController`，且未表达 `verifyNodeToken(...)` 的正式职责，已与接口设计和当前实现脱节。
- `2026-04-23`：完成 `A-130`，已将 `后端设计.md` 中该章节收口为当前正式结构：`NodeAgentController` 负责注册/心跳，`InternalNodeReservationController` 负责预占，`InternalSchedulerController` 负责调度内部协作与 `verifyNodeToken(...)`；文档口径现已与代码和《接口设计》一致。
- `2026-04-23`：继续沿“核心设计文档交叉一致性”方向复核，新增识别出 `A-131`：`需求分析与系统设计.md` 的安全性需求仍写为“节点代理需使用 token 或 nodeKey 鉴权”，与当前统一使用 `nodeToken / X-Node-Token` 的正式术语不一致。
- `2026-04-23`：完成 `A-131`，已将 `需求分析与系统设计.md` 的相关表述收口为“节点代理使用 `nodeToken` 鉴权，除注册接口外统一通过请求头 `X-Node-Token` 传递”，现已与《接口设计》、`后端设计.md` 和实现保持一致。
- `2026-04-23`：继续沿“核心设计文档交叉一致性”方向复核，新增识别出 `A-132`：`后端设计.md` 的附录/命名示例里仍残留旧控制器名 `NodeRegisterController / NodeHeartbeatController / InternalNodeCapacityController / InternalNodeHeartbeatController`，以及旧字段 `paramsJson`，容易和当前正式结构混淆。
- `2026-04-23`：完成 `A-132`，已同步修订 `后端设计.md` 的控制器清单、统一命名规范、scheduler 包树示例和命令构建说明，统一为当前正式结构和字段口径 `params`。
- `2026-04-23`：继续沿“数据库设计与实现字段一致性”方向复核，新增识别出 `A-133`：`数据库设计.md` 的“服务与数据表对应”汇总仍漏写 `node_reservation`，但正式建表 SQL 与实现均已将其作为 scheduler-service 主表使用。
- `2026-04-23`：完成 `A-133`，已将 `数据库设计.md` 的服务-数据表汇总修订为 `scheduler-service` 持有 `compute_node`、`node_reservation`、`node_solver_capability`、`schedule_record` 四张正式主表。
- `2026-04-23`：继续沿“数据库设计与实现字段一致性”方向复核，新增识别出 `A-134`：`solver_definition.description` 与 `solver_profile_file_rule.description` 已在设计文档和建表 SQL 中成为正式字段，但 `solver-service` 领域模型、PO/Mapper、接口 DTO 以及 `task-service` 的内部消费仍残留 `remark` 旧口径，形成数据库字段、接口契约与实现语义不一致。
- `2026-04-23`：完成 `A-134`，已将 `solver-service` 的求解器定义、文件规则领域模型、持久化 PO/Mapper、请求响应 DTO 以及 `task-service/common-lib` 的内部消费统一收口到正式字段 `description`，移除设计文档外的 `remark` 残留；通过 `mvn -pl common-lib,solver-service,task-service -am -DskipTests compile` 验证。
- `2026-04-23`：继续沿“数据库设计与实现字段一致性”方向复核，新增识别出 `A-135`：核心设计文档已将 `task_file.unpack_dir` 统一定义为“校验后真实执行目录，可等于 `workdir` 或其子目录”，但 `docs/sql/init_databases.sql` 与 `docs/sql/patch_zip_only_schema_upgrade.sql` 仍残留旧注释“解压后工作目录”，与当前目录合同不一致。
- `2026-04-23`：完成 `A-135`，已将 `docs/sql/init_databases.sql` 与 `docs/sql/patch_zip_only_schema_upgrade.sql` 中 `task_file.unpack_dir` 注释统一改为“校验后真实执行目录，可等于 workdir 或其子目录”。
- `2026-04-23`：继续沿“接口/状态字段残留旧口径”方向复核，新增识别出 `A-136`：`后端设计.md` 中仍残留旧节点接入示例与方法签名，包括 `NodeDTO`、`NodeRegisterRequest`、无 token 心跳式签名等，和当前 node-agent 自注册与 token 心跳正式合同不一致。
- `2026-04-23`：继续沿“接口/状态字段残留旧口径”方向复核，新增识别出 `A-137`：`后端设计.md` 仍多处保留 `NodeOfflineFailRequest` 与 `failTasksByOffline(...)` 旧命名，但当前正式实现为 `NodeOfflineTasksRequest` 与 `markNodeOfflineTasksFailed(...)`。
- `2026-04-23`：完成 `A-136`，已修订 `后端设计.md` 中节点接入、节点管理、node-agent 客户端、调度策略等章节的旧 `NodeDTO / NodeRegisterRequest` 口径，统一为 `NodeAgentRegisterRequest`、`NodeHeartbeatRequest + X-Node-Token`、`NodeInfo` 与 `ComputeNode`。
- `2026-04-23`：完成 `A-137`，已将 `后端设计.md` 中离线补偿旧命名 `NodeOfflineFailRequest / failTasksByOffline(...)` 统一为 `NodeOfflineTasksRequest / markNodeOfflineTasksFailed(...)`。
- `2026-04-23`：继续沿“服务内部接口、节点回调接口、内部 DTO 契约”方向复核，新增识别出 `A-138`：`/internal/tasks/queued` 的正式载荷字段已统一为 `params`，但 `common-lib` 的 `TaskDTO` 仍额外暴露 `paramsJson`，task-service 同时写入两个字段，形成内部接口双口径残留。
- `2026-04-23`：完成 `A-138`，已删除 `TaskDTO.paramsJson` 与 task-service 的双写逻辑，内部调度载荷统一只暴露 `params`；同步修订 `common-lib.md` 的 `TaskDTO/FileRuleDTO` 旧字段说明，并通过 `mvn -pl common-lib,task-service,scheduler-service -am -DskipTests compile` 验证。
- `2026-04-23`：继续沿“审查清单自身与最新正式契约一致性”方向复核，新增识别出 `A-139`：清单前文仍残留“任务详情已聚合状态历史与调度记录”“任务详情接口已支持 statusHistory/scheduleRecords”等过期结论，已与 `A-57` 收口后的正式设计冲突。
- `2026-04-23`：完成 `A-139`，已将清单前文统一修订为当前正式口径：`GET /api/tasks/{taskId}` 仅返回基础详情，状态历史与调度记录分别通过独立接口查询。
- `2026-04-23`：继续沿“模块说明文档与正式接口合同一致性”方向复核，新增识别出 `A-140`：`solver-service.md` 仍保留 `description/remark` 双口径说明，`task-service.md` 仍把 `StatusReportRequest` 描述为支持已删除的 `status/reason` 兼容字段，与当前实现和《接口设计》不一致。
- `2026-04-23`：完成 `A-140`，已将 `solver-service.md` 统一修订为正式 `description` 口径，并将 `task-service.md` 中 `StatusReportRequest` 的说明收口为 `nodeId/fromStatus/toStatus/changeReason/operatorType`。
- `2026-04-23`：继续沿“正式接口文档内部一致性”方向复核，新增识别出 `A-141`：`接口设计.md` 中管理员调优先级接口仍有“仅非终态任务可调”的旧表述，但当前实现与《状态流转校验规则表》均已收口为仅 `CREATED / VALIDATED / QUEUED` 可调。
- `2026-04-23`：完成 `A-141`，已将 `接口设计.md` 中 `/api/admin/tasks/{taskId}/priority` 的核心契约总表、接口总规约表和详细章节统一修订为 `CREATED / VALIDATED / QUEUED` 状态边界。
- `2026-04-23`：继续按“修改一个设计文档后同步关联设计文档”的要求复核 `A-141`，新增识别出 `A-142`：`状态流转校验规则表.md` 虽已限定优先级调整只允许 `CREATED / VALIDATED / QUEUED`，但冲突语义仍写成“终态任务返回冲突”，未覆盖 `SCHEDULED / DISPATCHED / RUNNING`。
- `2026-04-23`：完成 `A-142`，已将 `状态流转校验规则表.md` 中管理员调优先级接口的冲突语义同步修订为“非允许状态返回冲突”。
- `2026-04-23`：继续沿“调度派发确认与预占释放闭环”方向复核，新增识别出 `A-143`：`TaskScheduleJob` 在 node-agent 已经接收任务后，如果随后 `mark-dispatched` 重试仍失败，当前只记录调度失败，不会调用 `dispatch-failed` 补偿链路，可能导致任务停留 `SCHEDULED` 且节点预占不能及时释放。
- `2026-04-23`：完成 `A-143`，已将 `TaskScheduleJob.handleScheduleException(...)` 调整为：任务一旦已成功标记 `SCHEDULED`，后续派发阶段异常统一进入 `handleDispatchFailureQuietly(...)`，覆盖 node-agent 未接收和已接收但 `mark-dispatched` 失败两类场景；通过 `mvn -pl scheduler-service -am -DskipTests compile` 验证。
- `2026-04-23`：继续沿“dispatch-failed 迟到回写幂等语义”方向复核，新增识别出 `A-144`：`接口设计.md` 与 `task-service` 已将同一 `taskId + nodeId` 的终态迟到派发失败回写视为幂等忽略，但 `状态流转校验规则表.md` 仍写成“终态冲突”；同时 `TaskDispatchManager.markFailed(...)` 对已 `FAILED` 的迟到回写未校验 `nodeId` 一致性。
- `2026-04-23`：完成 `A-144`，已为 `TaskDispatchManager.markFailed(...)` 补齐已 `FAILED` 场景下的 `nodeId` 一致性校验，并将 `状态流转校验规则表.md` 中 `dispatch-failed` 的终态冲突语义同步修订为“同一 `taskId + nodeId` 的终态迟到回写可幂等忽略”；通过 `mvn -pl task-service -am -DskipTests compile` 验证。
- `2026-04-23`：继续沿“任务查询解释字段与正式合同一致性”方向复核，新增识别出 `A-145`：`TaskQueryAppService.resolveQueueReason(...)` 的最终兜底分支仍返回“前方仍有更高优先级或更早提交任务”，但《接口设计》明确该场景应返回“排队中，等待调度器处理”。
- `2026-04-24`：完成 `A-145`，已将 `TaskQueryAppService.resolveQueueReason(...)` 的最终兜底文案修正为“排队中，等待调度器处理”，并把前置排队原因文案补齐为“前方仍有更高优先级或更早提交的任务，等待调度”；通过 `mvn -pl task-service -am -DskipTests compile` 验证。
- `2026-04-24`：继续沿“任务查询与调度审计返回合同”方向复核，新增识别出 `A-146`：`task-service` 侧 `SchedulerClient.listTaskScheduleRecords(...)` 对 scheduler-service 返回的单任务调度记录仍缺少正式字段硬校验，若 `taskNo/strategyName/scheduleStatus/createdAt` 等字段缺失，理论上仍可能向前端透出不完整记录。
- `2026-04-24`：完成 `A-146`，已在 `SchedulerClient.listTaskScheduleRecords(...)` 增加单任务调度记录正式字段硬校验，并在 `nodeId` 非空时同步要求 `nodeName` 有效，同时校验返回记录 `taskId` 与请求一致；通过 `mvn -pl task-service -am -DskipTests compile` 验证。
- `2026-04-24`：继续沿“queueReason 依赖的节点快照返回合同”方向复核，新增识别出 `A-147`：`SchedulerClient.getQueueNodeSnapshot(...)` 对 `/internal/nodes/available` 与 `/api/nodes` 的返回仍存在宽松解析，非法记录会被忽略、缺失 `total` 会被当作 0，仍可能把 scheduler-service 坏回执误判为真实的“暂无节点/节点满载”业务状态。
- `2026-04-24`：完成 `A-147`，已收紧 `SchedulerClient.getQueueNodeSnapshot(...)` 对 `/internal/nodes/available` 和 `/api/nodes` 的结构校验，坏回执会显式报错，不再折算为 0 去污染 `queueReason`；通过 `mvn -pl task-service -am -DskipTests compile` 验证。
- `2026-04-24`：继续沿“任务查询名称补全字段完整性”方向复核，新增识别出 `A-148`：`task-service` 的 `SolverClient.getSolverName/getProfileName`、`SchedulerClient.getNodeName`、`UserClient.getUsername` 在下游返回 `code=0` 但名称字段为空时仍会把空值继续透传到任务列表、详情或管理员任务查询结果中。
- `2026-04-24`：完成 `A-148`，已收紧 `SolverClient.getSolverName/getProfileName`、`SchedulerClient.getNodeName`、`UserClient.getUsername` 的名称字段校验，`code=0` 但名称为空时会显式报错，不再向任务查询结果透传空名称；通过 `mvn -pl task-service -am -DskipTests compile` 验证。
- `2026-04-24`：继续沿“驾驶舱节点汇总消费校验”方向复核，新增识别出 `A-149`：`SchedulerClient.getOnlineNodeSummary()` 对 `/api/nodes` 返回的节点记录仍存在弱校验，非法行可能被静默跳过或被计入在线节点总数，仍有污染 `onlineNodeCount/avgNodeLoad` 的风险。
- `2026-04-24`：完成 `A-149`，已收紧 `SchedulerClient.getOnlineNodeSummary()` 对节点记录的关键字段校验，坏记录将显式报错，不再被静默计入或忽略；通过 `mvn -pl task-service -am -DskipTests compile` 验证。
- `2026-04-24`：继续沿“查询接口参数边界与公开合同”方向复核，新增识别出 `A-150 ~ A-152`：分别对应 `/api/schedules` 仍暴露文档外 `strategyName` 筛选、任务/节点/调度查询对枚举与长度边界缺少统一收口、查询时间参数仍未按全局 `yyyy-MM-dd HH:mm:ss` 合同绑定且缺少时间范围校验。
- `2026-04-24`：完成 `A-150`，已从 `SchedulePageQueryRequest` 与 `ScheduleRecordMapper` 中移除 `/api/schedules` 公开查询链路对 `strategyName` 的筛选支持；通过 `mvn -pl scheduler-service -am -DskipTests compile` 验证。
- `2026-04-24`：完成 `A-151`，已在 `TaskQueryBuilder`、`NodeAppService`、`ScheduleAppService` 中补齐查询参数规范化、长度约束与枚举边界校验，非法筛选值现会显式返回参数错误，不再静默落成空查询结果；通过 `mvn -pl task-service -am -DskipTests compile` 与 `mvn -pl scheduler-service -am -DskipTests compile` 验证。
- `2026-04-24`：完成 `A-152`，已为 `/api/tasks`、`/api/admin/tasks`、`/api/schedules` 的 `startTime/endTime` 统一补齐 `yyyy-MM-dd HH:mm:ss` 绑定格式，并增加 `startTime <= endTime` 校验；通过 `mvn -pl task-service -am -DskipTests compile` 与 `mvn -pl scheduler-service -am -DskipTests compile` 验证。
- `2026-04-24`：继续沿“驾驶舱节点汇总分页合同一致性”方向复核，新增识别出 `A-153`：`task-service` 侧 `SchedulerClient.getOnlineNodeSummary()` 仍把 `/api/nodes` 当作可用 `pageSize=1000` 的单页全量接口使用，既违反公开分页上限，也会在节点规模扩大后低估 `onlineNodeCount/avgNodeLoad`。
- `2026-04-24`：完成 `A-153`，已将 `SchedulerClient.getOnlineNodeSummary()` 改为按公开分页合同循环拉取 `/api/nodes`，单页固定使用 `200` 并累计到 `total`；通过 `mvn -pl task-service -am -DskipTests compile` 验证。
- `2026-04-24`：继续沿“调度记录分页排序合同”方向复核，新增识别出 `A-154`：`/api/schedules` 当前仍按 `id DESC` 返回，和接口设计要求的时间倒序展示不一致，理论上会在历史补录或时间不连续场景下打乱前端默认顺序。
- `2026-04-24`：完成 `A-154`，已将 `ScheduleRecordMapper.page(...)` 收口为按 `created_at DESC, id DESC` 分页，并通过 `mvn -pl scheduler-service -am -DskipTests compile` 验证。
- `2026-04-24`：继续沿“接口响应时间格式合同”方向复核，新增识别出 `A-155`：文档虽已把查询时间参数绑定收口到 `yyyy-MM-dd HH:mm:ss`，但公共层 `JacksonConfig` 仍是占位实现，任务、调度、节点等响应对象里的 `LocalDateTime` 实际仍可能按默认 ISO 格式对外返回，造成前后端时间合同只收了一半。
- `2026-04-24`：完成 `A-155`，已在 `common-lib` 中补齐全局 Jackson 时间配置，统一 `LocalDateTime` 的序列化与反序列化格式为 `yyyy-MM-dd HH:mm:ss`，并将 `gateway-service` 启动扫描范围收口到 `com.example.cae` 以确保共享配置实际生效；通过 `mvn -pl common-lib,task-service,scheduler-service,gateway-service,solver-service,user-service -am -DskipTests compile` 验证。
- `2026-04-24`：继续沿“公开接口合同与字段语义一致性”方向复核，新增识别出 `A-156/A-157`：外部响应对象仍存在 `id` 与业务 ID 同义双字段外露，且求解器/模板分页查询实现与《接口设计》正文示例未完全同步。
- `2026-04-24`：完成 `A-156/A-157`，已收口用户、求解器、模板、文件规则公开响应字段语义，并补齐《接口设计》中 `GET /api/solvers` 与 `GET /api/profiles` 的分页合同；通过 `mvn -pl user-service,solver-service,task-service -am -DskipTests compile` 验证。
- `2026-04-24`：根据“设计文档同步修改”要求，已将 `GET /api/solvers`、`GET /api/profiles` 分页查询合同同步补充到《后端设计》《需求分析与系统设计》《数据库设计》，避免只改接口设计造成文档口径分裂。
- `2026-04-24`：继续沿“solver-service 查询参数边界”方向复核，新增并完成 `A-158`：已为求解器/模板分页查询补齐 `enabled=0/1`、字符串长度约束和空白归一化，并同步《接口设计》参数说明；通过 `mvn -pl solver-service -am -DskipTests compile` 验证。
- `2026-04-24`：继续沿“动作型接口与节点能力注册合同”方向复核，新增并完成 `A-159`：节点注册落库前已校验 `solvers[].solverId` 必须存在于全局求解器定义，未知求解器拒绝注册；同步更新《接口设计》《后端设计》《需求分析与系统设计》《数据库设计》；通过 `mvn -pl scheduler-service -am -DskipTests compile` 验证。
- `2026-04-24`：继续沿“内部接口字段合同一致性”方向复核并完成 `A-160`：`/internal/solvers/{solverId}` 已改为返回内部 DTO `InternalSolverDetailResponse`，字段显式包含 `solverId`，与 `scheduler-service` 调用方读取逻辑一致；同步更新《接口设计》《后端设计》；通过 `mvn -pl solver-service,scheduler-service -am -DskipTests compile` 验证。
- `2026-04-24`：继续沿“内部模板接口路径参数语义”方向复核，新增 `A-161`：发现《接口设计》仍将 `/internal/profiles/{profileId}` 写成 `/internal/profiles/{id}`，与实现和调用方语义不一致，需要同步收口文档合同。
- `2026-04-24`：完成 `A-161`：已将《接口设计》内部模板接口统一为 `GET /internal/profiles/{profileId}` 并补齐返回字段，同时在《后端设计》中补充 `InternalProfileDetailResponse` 内部接口说明；该项为文档合同收口，无需编译验证。
- `2026-04-24`：继续沿“数据库脚本与代码依赖一致性”方向复核，新增 `A-162`：发现旧库升级脚本未覆盖 `node_reservation`、`compute_node.reserved_count` 和文件幂等唯一约束，存在部署后调度预占或重复上报失败风险。
- `2026-04-24`：完成 `A-162`：已扩展 `docs/sql/patch_zip_only_schema_upgrade.sql`，补齐 `scheduler_db` 的 `reserved_count/node_reservation` 升级段，以及 `task_file/task_result_file` 幂等唯一约束升级段；该项为 SQL 脚本修正，无需 Java 编译。
- `2026-04-24`：继续沿“调度前任务定义一致性”方向复核，新增 `A-163`：发现 scheduler-service 调度前缺少 `profile.solverId == task.solverId` 与 `profile.taskType == task.taskType` 的二次校验，需要作为队列任务派发前的最后防线补齐。
- `2026-04-24`：完成 `A-163`：已在 scheduler-service 调度前补齐模板归属求解器和任务类型校验，并同步《需求分析与系统设计》《接口设计》《后端设计》；通过 `mvn -pl scheduler-service -am -DskipTests compile` 验证。
- `2026-04-24`：继续沿“任务提交入队前定义有效性”方向复核，新增 `A-164`：发现 `submitTask(...)` 未重新复核求解器/模板当前启用状态和归属关系，管理员在校验后禁用模板或求解器时，任务仍可能进入队列。
- `2026-04-24`：完成 `A-164`：已在任务提交入队前重新校验求解器/模板当前启用状态与归属关系，并同步《接口设计》《后端设计》《需求分析与系统设计》；通过 `mvn -pl task-service -am -DskipTests compile` 验证。
- `2026-04-24`：继续沿“调度确认与节点回传竞态”方向复核，新增 `A-166`：发现 `mark-dispatched` 的迟到确认幂等语义在《接口设计》和状态规则文档之间表述不一致，需要同步状态规则文档以匹配当前实现的抗重试设计。
- `2026-04-24`：完成 `A-165`：已显式收口 `dispatch-failed recoverable=true` 回队逻辑，可恢复派发失败不写任务主表失败字段，不可恢复派发失败才写 `failType/failMessage`；通过 `mvn -pl task-service -am -DskipTests compile` 验证。
- `2026-04-24`：完成 `A-166`：已同步 `mark-dispatched` 迟到确认幂等语义到《状态流转校验规则表》《任务状态流转实现表》《后端设计》《需求分析与系统设计》，与《接口设计》和当前实现保持一致。
- `2026-04-24`：继续沿“节点日志回传幂等与运行输出完整性”方向复核，新增 `A-167`：发现 `log-report` 对 `logContent` 使用 `@NotBlank`，会拒绝求解器输出空行并可能导致 node-agent 日志读取线程提前结束，需要放宽为空字符串可保存、仅禁止 `null`。
- `2026-04-24`：完成 `A-167`：已放宽节点日志回传 `logContent` 校验为空字符串可保存、仅禁止 `null`，并同步《接口设计》《状态流转校验规则表》；通过 `mvn -pl task-service -am -DskipTests compile` 验证。
- `2026-04-24`：继续沿“结果回传与终态闭环”方向复核，新增 `A-168`：发现 node-agent 在求解器成功后若结果摘要、结果文件或 `mark-finished` 回调持续失败，只记录本地错误，可能导致中心任务长期停留 `RUNNING`，需要补齐终态收口。

- 2026-04-24：完成 A-168：已在 node-agent 成功后回调失败路径补齐 mark-failed 终态兜底，避免任务长期停留 RUNNING，并同步《状态流转校验规则表》《后端设计》；通过 mvn -pl node-agent -am -DskipTests compile 验证。
- `2026-04-24`：继续沿“结果文件字段合同与数据库枚举语义”方向复核，新增 `A-169`：发现结果文件 `fileType` 未按 `RESULT/LOG/REPORT/IMAGE` 校验，异常节点上报可能写入文档外类型。

- 2026-04-24：完成 A-169：已为结果文件 ileType 补齐 ResultFileTypeEnum 枚举校验，只允许 RESULT/LOG/REPORT/IMAGE，并同步《接口设计》《状态流转校验规则表》《后端设计》；通过 mvn -pl task-service -am -DskipTests compile 验证。- `2026-04-24`：继续沿“节点离线补偿健壮性与批处理隔离”方向复核，新增 `A-170`：发现 `NodeHeartbeatChecker` 触发离线补偿时未做单节点异常隔离，若某个节点调用 task-service 补偿失败，可能中断本轮后续离线节点处理；需要改为逐节点捕获并记录异常，失败节点依赖下轮定时任务重试。
- `2026-04-24`：完成 `A-170`：已为 `NodeHeartbeatChecker` 补齐单节点异常隔离，在线超时标记离线与已离线补偿重试均按节点捕获异常，单节点失败不再阻断本轮其他节点处理；同步《后端设计》《需求分析与系统设计》《状态流转校验规则表》；通过 `mvn -pl scheduler-service -am -DskipTests compile` 验证。
- `2026-04-24`：继续沿“调度抢占确认幂等与重复下发风险”方向复核，新增 `A-171`：发现 `TaskScheduleJob` 将 `mark-scheduled` 返回的同节点 `DISPATCHED/RUNNING/终态` 也视为可继续派发，可能在并发调度或迟到响应场景下重复调用 node-agent；需要区分“可继续下发”“已推进需停止”“是否需要释放本次预占”。
- `2026-04-24`：完成 `A-171`：已将 `TaskScheduleJob` 的抢占确认处理拆分为“可继续下发”和“失败后是否释放预占”两类判断，避免同节点已 `DISPATCHED/RUNNING/终态` 时重复通知 node-agent；并同步《接口设计》《状态流转校验规则表》《后端设计》《需求分析与系统设计》；通过 `mvn -pl scheduler-service -am -DskipTests compile` 验证。
- `2026-04-24`：继续沿“node-agent 下发接口幂等”方向复核，新增 `A-172`：发现 node-agent 对同一 `taskId` 的重复下发会返回 `CONFLICT task already running`，scheduler 可能把它当作可恢复派发失败并回队，存在中心状态与节点实际执行不一致风险；需要将同一任务重复下发收口为 accepted 幂等响应，不启动重复执行。
- `2026-04-24`：完成 `A-172`：已将 node-agent 对同一 `taskId` 的重复下发收口为 accepted 幂等响应，不再抛出 `task already running` 冲突，也不会启动重复执行线程；同步《接口设计》《后端设计》《状态流转校验规则表》《需求分析与系统设计》；通过 `mvn -pl node-agent -am -DskipTests compile` 验证。
- `2026-04-24`：继续沿“dispatch-failed 调用方与节点鉴权一致性”方向复核，新增 `A-173`：发现 node-agent 执行前失败会调用 `/internal/tasks/{taskId}/dispatch-failed` 并携带 `X-Node-Token`，但 task-service 当前该接口未读取也未校验 token，且《接口设计》仍只写 scheduler-service 调用方；需要补齐节点 token 校验与文档契约，避免节点执行前失败回写链路鉴权语义不清。
- `2026-04-24`：完成 `A-173`：已为 task-service 的 `dispatch-failed` 补齐可选 `X-Node-Token` 校验，node-agent 执行前失败回写携带 token 时会校验任务绑定节点与 token；scheduler-service 内部调用保持兼容；同步《接口设计》《后端设计》《状态流转校验规则表》《需求分析与系统设计》；通过 `mvn -pl task-service -am -DskipTests compile` 验证。
- `2026-04-25`：A-173 的 task-service 可选 token 兼容口径已被 `A-204` 覆盖；最新正式边界为 node-agent 执行前失败只回调 scheduler-service，由 scheduler-service 校验 `X-Node-Token`，task-service 内部 `dispatch-failed` 不再读取节点 token。
- `2026-04-24`：继续沿“失败类型字段合同与枚举一致性”方向复核，新增 `A-174`：发现 `mark-failed` 与 `dispatch-failed` 对 `failType` 只做非空/长度校验，未严格限定到《数据库设计》和 `FailTypeEnum` 定义的统一失败类型，可能写入文档外失败类型；需要补齐服务端枚举校验并同步接口/状态规则表述。
- `2026-04-24`：完成 `A-174`：已在 `TaskResultManager.mark-failed` 与 `TaskDispatchManager.dispatch-failed` 中补齐 `FailTypeEnum` 枚举校验，运行期 `mark-failed` 继续禁止 `TIMEOUT` 类型并要求通过 `mark-finished finalStatus=TIMEOUT` 表达超时；同步《接口设计》《状态流转校验规则表》《后端设计》；通过 `mvn -pl task-service -am -DskipTests compile` 验证。
- `2026-04-24`：继续沿“派发失败回队/失败后的预占释放闭环”方向复核，新增 `A-175`：发现 node-agent 执行前失败会直接调用 task-service 的 `dispatch-failed`，但当前只有 scheduler-service 的 `handleDispatchFailure(...)` 路径会释放 `node_reservation`；节点直连回写会导致任务已回队/失败但 `reservedCount` 未释放，需要将释放预占收口到 task-service 的 `dispatch-failed` 状态变更成功路径，并保持 scheduler 侧重复释放幂等。
- `2026-04-24`：完成 `A-175`：已将 `dispatch-failed` 成功回写 `QUEUED/FAILED` 后的预占释放收口到 task-service，node-agent 执行前失败直连回写不会再泄漏 `reservedCount`；scheduler-service 原有重复释放调用保留并依赖 `node_reservation` 幂等保护；同步《接口设计》《后端设计》《状态流转校验规则表》《需求分析与系统设计》；通过 `mvn -pl task-service -am -DskipTests compile` 验证。
- `2026-04-25`：A-175 的职责口径已被 `A-203` 覆盖；最新正式边界为 node-agent 执行前失败应回调 scheduler-service 入口，task-service 的 `dispatch-failed` 仅负责状态回写，不直接释放预占，预占释放统一由 scheduler-service 在状态回写 ACK 后执行。
- `2026-04-24`：继续沿“命令模板占位符合同与 node-agent 实现一致性”方向复核，新增 `A-176`：发现 node-agent `CommandBuilder` 实际支持 `taskId/taskNo/solverId/solverCode/solverExecMode/solverExecPath/profileId/taskType/taskDir/inputDir/outputDir/logDir` 以及任务参数占位符，但《接口设计》模板约束只列出其中一部分，容易造成模板配置和实现认知不一致；需要同步核心设计文档中的允许占位符列表。
- `2026-04-24`：完成 `A-176`：已同步《接口设计》《后端设计》《需求分析与系统设计》中的 `commandTemplate` 允许占位符列表，明确首版稳定运行上下文占位符与任务参数占位符边界，继续禁止 `${workDir}` 作为对外模板占位符；该项为文档合同收口，无需 Java 编译。
- `2026-04-24`：继续沿“模板配置前置校验与真实使用便利性”方向复核，新增 `A-177`：发现 solver-service 保存模板时仅禁止 `${workDir}` 和复杂 shell 片段，但未校验未知占位符；如管理员误写 `${taskdir}`，会到 node-agent 执行阶段才失败，不利于真实配置使用。需要在模板新增/编辑时基于稳定占位符和 `paramsSchema` 参数属性做前置校验。
- `2026-04-24`：完成 `A-177`：已在 solver-service 模板新增/编辑和内部模板读取链路中加入 `commandTemplate` 未知占位符前置校验，稳定运行上下文占位符与 node-agent 保持一致，任务参数占位符必须来自 `paramsSchema/paramsSchemaJson.properties`，`${workDir}` 仍禁止；同步《接口设计》《后端设计》《需求分析与系统设计》；通过 `mvn -pl solver-service -am -DskipTests compile` 验证。
- `2026-04-24`：继续沿“任务参数校验与命令模板执行前闭环”方向复核，新增 `A-178`：发现模板保存阶段已要求任务参数占位符来自 `paramsSchema.properties`，但任务校验阶段只按 JSON Schema 校验 required/type 等规则；若模板引用 `${caseName}`，而该字段未声明为 required 且也未由文件规则派生，任务可能仍进入 `VALIDATED/QUEUED`，最终在 node-agent 命令替换阶段失败。需要在任务校验通过前基于“用户参数 + 派生参数”检查 commandTemplate 中的任务参数占位符是否已有实际值。
- `2026-04-24`：完成 `A-178`：复核确认 `TaskValidationManager.validateArchiveAndRules(...)` 已在合并用户参数和文件规则派生参数后调用 `validateCommandTemplateParams(...)`，会在进入 `VALIDATED/QUEUED` 前拦截命令模板引用但缺失或为空的任务参数；调度下发使用 `sim_task.params_json` 中的合并后参数，node-agent 仍保留未解析占位符兜底校验。该项为代码确认，无需新增修改。
- `2026-04-24`：继续沿“上传规范接口与前端任务创建契约一致性”方向复核，新增 `A-179`：发现 solver-service 的 `upload-spec` 实际返回 `paramsSchema/paramsSchemaJson/timeoutSeconds/description`，但《接口设计》上传规范示例未体现这些字段，容易导致前端只按文件规则实现而忽略任务参数表单渲染；需要同步接口文档，明确 `upload-spec` 是前端创建任务与上传文件的主要配置来源。
- `2026-04-24`：完成 `A-179`：已同步《接口设计》上传规范返回示例和字段语义，明确 `upload-spec` 必须返回 `uploadMode/archiveRule/fileRules/paramsSchema/paramsSchemaJson`，前端使用参数 schema 渲染任务参数表单，后端在创建、更新、校验阶段再次校验；该项为接口文档合同收口，无需 Java 编译。
- `2026-04-24`：继续沿“模板参数 schema 前置校验与真实使用便利性”方向复核，新增 `A-180`：发现 solver-service 保存模板时只在命令模板使用任务参数占位符时才解析 `paramsSchema` 的 `properties`；如果管理员配置非法 JSON Schema 但命令模板暂未引用参数，错误会延迟到任务创建或校验阶段暴露。需要在模板新增/编辑时前置校验 `paramsSchema/paramsSchemaJson` 本身的基本合法性。
- `2026-04-24`：完成 `A-180`：已在 `ProfileTemplateContractValidator` 中补齐 `paramsSchema/paramsSchemaJson` 轻量 JSON Schema 合同校验，非空时必须是 JSON object，并校验 `properties/required/items/additionalProperties/pattern` 等 task-service 当前支持字段；同步《接口设计》《后端设计》《需求分析与系统设计》；通过 `mvn -pl solver-service -am -DskipTests compile` 验证。
- `2026-04-24`：继续沿“文件规则 ruleJson 与任务校验实现一致性”方向复核，新增 `A-181`：发现 `deriveParam.pattern` 在 task-service 任务校验阶段会被编译执行，但 solver-service 保存文件规则时只检查该字段是否存在，未校验正则合法性；非法正则会导致模板保存成功、用户任务校验阶段才暴露配置错误。需要在文件规则新增/编辑时前置校验 `deriveParam.pattern`。
- `2026-04-24`：完成 `A-181`：已在 `ProfileTemplateContractValidator.validateDeriveParam(...)` 中补齐 `deriveParam.pattern` 正则合法性校验，并同步《接口设计》《后端设计》《需求分析与系统设计》《数据库设计》中的 `ruleJson` 正则字段保存期校验要求；通过 `mvn -pl solver-service -am -DskipTests compile` 验证。
- `2026-04-24`：继续沿“文件后缀规则真实使用便利性”方向复核，新增 `A-182`：发现 `allowSuffix` 如果配置为 `.zip`，前端和管理员理解上合理，但 task-service 上传校验、任务校验以及 solver-service `upload-spec` 输出均按原字符串处理，可能与实际文件后缀 `zip` 比较失败。需要统一后缀归一化语义。
- `2026-04-24`：完成 `A-182`：已在 task-service 上传校验、解压后文件后缀校验和 solver-service `upload-spec` 输出中统一将 `allowSuffix` 归一化为去除前导点的小写后缀，兼容 `zip` 与 `.zip` 配置；同步《接口设计》《后端设计》《需求分析与系统设计》《数据库设计》；通过 `mvn -pl task-service,solver-service -am -DskipTests compile` 验证。
- `2026-04-24`：继续沿“ruleJson 作用域选择器白名单与静默忽略风险”方向复核，新增 `A-183`：发现 task-service 会把非元数据 key 按运行变量匹配，无法匹配的未知 key 会被静默忽略；solver-service 保存文件规则时也把任何未知 key 当作非空作用域选择器放行。若管理员误写 `solverCodess`，规则可能保存成功并在运行期误生效。需要明确作用域选择器白名单，并在保存和任务校验阶段拒绝未知选择器。
- `2026-04-24`：完成 `A-183`：已在 solver-service 文件规则保存校验中限定作用域选择器白名单，并在 task-service 校验存量规则时将未知选择器作为 `TEMPLATE_RULE_CONFLICT` 处理；白名单包括 `solverId/solverIds`、`profileId/profileIds`、`taskType/taskTypes`、`solverCode/solverCodes`、`solverExecMode/solverExecModes`、`solverExecPath/solverExecPaths`；同步《接口设计》《后端设计》《需求分析与系统设计》《数据库设计》；通过 `mvn -pl task-service,solver-service -am -DskipTests compile` 验证。
- `2026-04-24`：继续沿“文件类型枚举与数量/必需规则基础语义”方向复核，新增 `A-184`：发现 solver-service 保存文件规则时只校验 `fileType` 非空，task-service 对未知 `fileType` 默认不做类型过滤；如管理员误写 `FIEL`，可能导致 `requiredFlag`、`minCount/maxCount` 基于错误匹配集合误通过。需要将 `fileType` 收紧为首版枚举，并对存量非法类型做任务校验兜底。
- `2026-04-24`：完成 `A-184`：已在 solver-service 文件规则新增/编辑时限定 `fileType` 只能为 `ZIP/FILE/DIR/DIRECTORY`，并在 task-service 任务校验阶段将存量非法 `fileType` 作为 `TEMPLATE_RULE_CONFLICT` 处理；同步《接口设计》《后端设计》《需求分析与系统设计》《数据库设计》；通过 `mvn -pl task-service,solver-service -am -DskipTests compile` 验证。
- `2026-04-24`：继续沿“必需文件规则与数量规则冲突”方向复核，新增 `A-185`：发现 `requiredFlag=1` 表示必须存在匹配项，但当前允许同时配置 `ruleJson.maxCount/max=0`；该组合自相矛盾，会在任务校验阶段表现为用户文件数量错误，而不是模板规则冲突。需要在保存期拒绝该组合，并在任务校验阶段兜底识别历史冲突规则。
- `2026-04-24`：完成 `A-185`：已在 solver-service 文件规则合同校验中拒绝 `requiredFlag=1 + maxCount/max=0`，并在 task-service 校验存量规则时按 `TEMPLATE_RULE_CONFLICT` 处理；同步《接口设计》《后端设计》《需求分析与系统设计》《数据库设计》；通过 `mvn -pl task-service,solver-service -am -DskipTests compile` 验证。
- `2026-04-24`：继续沿“文件规则路径合同与目录安全”方向复核，新增 `A-186`：发现 `pathPattern` 当前会在任务校验阶段剥离前导 `/` 后继续匹配，solver-service 保存规则时也未拒绝绝对路径或 `..`；`fileNamePattern` 也缺少“只能是文件名 glob”的保存期约束。该问题可能让模板配置与 `unpackDir/caseDir` 相对目录合同不一致，也可能把模板错误表现为用户上传文件错误。需要明确路径规则只能相对已校验执行目录，并在保存期和任务校验期双重兜底。
- `2026-04-24`：完成 `A-186`：已在 solver-service 文件规则新增/编辑时拒绝绝对 `pathPattern`、包含 `..` 的路径以及夹带路径的 `fileNamePattern`；task-service 校验存量规则时将非法路径合同按 `TEMPLATE_RULE_CONFLICT` 处理，不再静默归一化为可匹配规则；同步《接口设计》《后端设计》《需求分析与系统设计》《数据库设计》及 `docs/sql/init_databases.sql` 中的枚举注释；通过 `mvn -pl task-service,solver-service -am -DskipTests compile` 验证。
- `2026-04-24`：继续沿“ruleJson 元数据保存期与任务校验期一致性”方向复核，新增 `A-187`：发现 solver-service 保存新规则时已经校验 `maxSizeMb`、`deriveParam/deriveParams` 的基础结构和正则，但 task-service 校验历史规则时仍可能把非法 `maxSizeMb` 当成未配置、把 `deriveParams` 中非对象项静默跳过，且保存期未限制 `deriveParam` 内未知字段。这会导致模板配置错误不够显式，和“模板错误返回 TEMPLATE_RULE_CONFLICT”的设计不一致。
- `2026-04-24`：完成 `A-187`：已在 solver-service 保存期补齐 `deriveParam` 与 `preprocess` 子字段白名单，并禁止同一规则同时配置 `deriveParam` 与 `deriveParams`；task-service 解析存量 `ruleJson` 时同步校验 `allowSuffix/minCount/maxCount/maxSizeMb/deriveParam/deriveParams/preprocess` 合同，非法历史配置统一按 `TEMPLATE_RULE_CONFLICT` 拦截，不再静默降级或跳过；同步《接口设计》《后端设计》《需求分析与系统设计》《数据库设计》；通过 `mvn -pl task-service,solver-service -am -DskipTests compile` 验证。
- `2026-04-24`：继续沿“校验后执行目录跨服务合同”方向复核，新增 `A-188`：发现 node-agent 的 `WorkDirManager` 会先根据 `unpackDir` 推导任务根目录并创建 `workdir`，如果调度请求携带的 `unpackDir` 等于 `.../workdir` 但共享目录实际尚不存在，目录准备步骤会先创建一个空 `workdir`，导致后续 `InputFilePrepareService` 的“validated unpackDir exists”检查被误通过，最终可能在空目录执行求解器；同时 task-service 下发排队任务时未显式确认归档文件已具备 `unpackDir`，仍存在让节点走兼容解压路径的空间。
- `2026-04-24`：完成 `A-188`：已在 node-agent 目录准备阶段前置校验调度请求中的已校验 `unpackDir`，必须在创建运行目录前真实存在且为目录，并统一校验多归档 `unpackDir` 一致性；task-service 下发 queued 任务前已强制要求存在输入归档且归档具备 `unpackDir`，否则按冲突中止派发，确保首版目录合同为“校验阶段解压并确认执行目录，节点复用该目录执行”。同步《接口设计》《后端设计》《需求分析与系统设计》；通过 `mvn -pl task-service,node-agent -am -DskipTests compile` 验证。
- `2026-04-24`：继续沿“日志分片与完整日志还原合同”方向复核，新增 `A-189`：发现 node-agent 使用 `BufferedReader.readLine()` 读取求解器 stdout/stderr 后直接上报行内容，换行符会被丢弃；task-service 下载完整日志时按 `seqNo` 直接拼接 `logContent`，导致完整日志可能变成一长行，和接口文档中“`logContent` 保留求解器原始输出、空行合法”的语义不一致。
- `2026-04-24`：完成 `A-189`：已在 node-agent `ProcessLogReader` 按行读取后补回 `\n` 再上报，使日志分片天然携带行分隔符；task-service 继续按分片顺序直接拼接，避免中心侧猜测换行。同步《接口设计》《后端设计》；通过 `mvn -pl node-agent -am -DskipTests compile` 验证。
- `2026-04-24`：继续沿“结果文件上报与嵌套目录去重”方向复核，新增 `A-190`：发现 node-agent 会递归收集 `outputDir` 下所有普通文件，但上报 `fileName` 时只使用 `file.getName()`；若输出目录存在 `a/result.dat` 与 `b/result.dat`，task-service 的 `uk_task_file_type_name(task_id, file_type, file_name)` 会把二者视为同一结果文件并发生覆盖。同时 task-service 要求 `fileName` 必须等于 `storagePath` 末段，阻止节点用相对目录信息生成唯一展示名。
- `2026-04-24`：完成 `A-190`：已将 node-agent 结果文件上报改为基于相对 `outputDir` 的路径生成不含路径分隔符的展示名，例如 `a/result.dat` 上报为 `a__result.dat`，从而在不修改首版数据库唯一键的前提下避免嵌套同名文件覆盖；task-service 仍校验 `fileName` 为普通文件名、`storagePath` 位于任务 `output/` 且真实存在，但不再要求 `fileName` 等于路径末段。同步《接口设计》《后端设计》《数据库设计》；通过 `mvn -pl task-service,node-agent -am -DskipTests compile` 验证。
- `2026-04-24`：继续沿“结果文件展示名与下载链路一致性”方向复核，新增 `A-191`：发现 `A-190` 已允许嵌套文件使用编码展示名，但 `TaskResultAppService` 下载校验仍要求 `fileName` 等于 `storagePath` 路径末段，会导致 `a__result.dat -> output/a/result.dat` 这类合法结果文件可上报但不可下载；同时深层结果路径编码后可能超过 `task_result_file.file_name(255)`，导致成功任务结果文件回传失败。
- `2026-04-24`：完成 `A-191`：已放开下载侧“展示名必须等于物理文件名”的旧校验，保留展示名普通文件名安全校验、任务 `output/` 边界校验和真实文件存在校验；node-agent 生成嵌套展示名时会替换路径分隔符与控制字符，超过 255 字符时追加短哈希截断，保证与接口和数据库字段长度一致。同步《接口设计》《后端设计》《需求分析与系统设计》《数据库设计》；通过 `mvn -pl task-service,node-agent -am -DskipTests compile` 验证。
- `2026-04-24`：继续沿“节点执行成功后的回调链路”方向复核，新增 `A-192`：发现 task-service 当前允许结果摘要/结果文件在任意终态补写，可能让已 `FAILED / CANCELED` 的迟到节点回调写入结果；同时 node-agent 对终态 ACK 要求必须等于本次期望终态，和“中心已先进入终态则按幂等忽略迟到失败/完成回传”的规则不一致。
- `2026-04-24`：完成 `A-192`：已将结果摘要/结果文件接收阶段收紧为仅 `RUNNING / SUCCESS / TIMEOUT`，并将 node-agent 终态 ACK 校验改为接受中心返回的任一终态作为幂等完成；同步《接口设计》《状态流转校验规则表》《需求分析与系统设计》；通过 `mvn -pl task-service,node-agent -am -DskipTests compile` 验证。
- `2026-04-24`：继续沿“结果文件展示名安全边界”方向复核，新增 `A-193`：发现下载链路已拒绝包含控制字符的 `fileName`，但结果文件上报入库链路只校验路径分隔符，可能让非法展示名先入库、后续下载再被拒绝，形成“可上报不可下载”的合同偏差。
- `2026-04-24`：完成 `A-193`：已在 `TaskResultManager.validateResultFilePath(...)` 补齐控制字符校验，确保 `fileName` 写入侧和下载侧安全规则一致；设计文档中既有“不得包含控制字符”的合同无需新增字段，仅更新清单记录；通过 `mvn -pl task-service -am -DskipTests compile` 验证。
- `2026-04-24`：继续沿“节点运行日志可观察性”方向复核，新增 `A-194`：发现 node-agent 的 stdout/stderr 日志读取线程会直接调用 `log-report`，一旦中心服务短暂异常或网络抖动导致上报抛错，日志读取线程会退出，主执行线程只等待 `join` 而不会感知该异常，后续求解器输出会丢失，和“前端可轮询实时日志刷新”的设计目标不一致。
- `2026-04-24`：完成 `A-194`：已为 node-agent 日志分片上报增加短重试和降级策略，持续失败时只丢弃当前日志分片并记录本地告警，不再中断 stdout/stderr 读取线程，也不影响求解器主流程和终态回传；同步《后端设计》《状态流转校验规则表》；通过 `mvn -pl node-agent -am -DskipTests compile` 验证。

- `2026-04-25`：继续沿 node-agent 执行生命周期方向复核，新增 `A-195`：发现求解器进程超时时强杀后未等待进程退出、stdout/stderr 日志读取线程正常路径使用无边界 `join`、超时路径不等待日志线程收口，且运行中本地取消会尝试走 `CANCELED` 语义，和首版“仅 QUEUED -> CANCELED、不支持 RUNNING -> CANCELED”的状态合同存在偏差。
- `2026-04-25`：完成 `A-195`：已修复 node-agent 进程执行生命周期收口问题，超时会强杀并等待求解器进程退出、正常/超时/中断路径均限时等待 stdout/stderr 日志线程；运行中本地取消不再走 `CANCELED` 上报，而是按首版状态合同转换为预运行失败或运行失败。通过 `mvn -pl node-agent -am -DskipTests compile` 验证。
- `2026-04-25`：继续沿“节点容量模型与心跳语义”方向复核，新增 `A-196`：发现 node-agent 心跳上报的 `runningCount` 直接取本地 active task 数，会把已接收但尚未成功回传 `RUNNING` 的任务也算作真实运行数；这与最新设计中 `runningCount` 表示真实运行数、`reservedCount` 表示调度预占数的两层容量模型不一致，可能在预运行阶段造成容量重复统计。
- `2026-04-25`：完成 `A-196`：已将 node-agent 本地并发判断与心跳运行数拆分，本地接收任务仍按 active task 控制最大并发，心跳 `runningCount` 仅统计已成功回传 `RUNNING` 的任务，避免预运行阶段与 scheduler-service 的 `reservedCount` 重复表达容量。通过 `mvn -pl node-agent -am -DskipTests compile` 验证。
- `2026-04-25`：继续沿“调度下发确认与异常补偿”方向复核，新增 `A-197`：发现 scheduler-service 在 node-agent 已 accepted 后，如果随后 `mark-dispatched` 因网络或 task-service 短暂异常失败，会进入统一异常处理并调用 `dispatch-failed`，可能把中心任务从 `SCHEDULED/DISPATCHED` 回退到 `QUEUED`，而节点实际已经接收并可能继续准备执行，造成中心状态与节点执行状态分裂。
- `2026-04-25`：完成 `A-197`：已在 scheduler-service 中区分 node-agent 是否已 accepted。节点尚未 accepted 的真实派发失败仍按 `dispatch-failed` 回队/失败处理；节点已 accepted 后若 `mark-dispatched` 确认失败，只记录调度确认失败，不主动回队，避免中心状态与节点实际执行分裂。同步《接口设计》《状态流转校验规则表》《后端设计》；通过 `mvn -pl scheduler-service -am -DskipTests compile` 验证。
- `2026-04-25`：继续沿“节点离线补偿与预占释放边界”方向复核，新增 `A-198`：发现 task-service 在处理 `SCHEDULED/DISPATCHED -> QUEUED` 离线补偿时，把任务状态更新和 scheduler-service `release-reservation` 放在同一个事务内严格执行；若预占记录缺失或释放接口短暂失败，任务状态会整体回滚并继续停留在旧派发状态，导致离线补偿无法优先收口任务生命周期。
- `2026-04-25`：完成 `A-198`：已将 task-service 节点离线补偿中的 `SCHEDULED/DISPATCHED -> QUEUED` 与预占释放解耦，任务状态先完成回队，随后尽力释放预占；释放失败只记录告警，不回滚状态补偿。同步《状态流转校验规则表》《任务状态流转实现表》《后端设计》；通过 `mvn -pl task-service -am -DskipTests compile` 验证。
- `2026-04-25`：继续沿“预占容量一致性恢复能力”方向复核，新增 `A-199`：发现当前 `node_reservation` 已作为预占幂等真值，但缺少按明细重算 `compute_node.reserved_count` 的恢复入口；当历史异常、网络抖动或释放失败造成汇总字段与明细表不一致时，节点可能长期假满载或容量偏低。
- `2026-04-25`：完成 `A-199`：已新增 scheduler-service 内部容量重算接口 `POST /internal/nodes/{nodeId}/reconcile-reservation`，按 `node_reservation.status=RESERVED` 重算并修复单节点 `reserved_count`，用于历史异常、释放失败或运维审计后的容量汇总恢复；同步《接口设计》《后端设计》《数据库设计》；通过 `mvn -pl scheduler-service -am -DskipTests compile` 验证。
- `2026-04-25`：继续沿“任务状态与预占明细交叉一致性”方向复核，新增 `A-200`：发现 A-199 已能修复 `reserved_count` 汇总，但无法识别 `node_reservation.status=RESERVED` 明细本身是否仍对应合法任务阶段；若任务已终态、已回队、已解绑节点或任务不存在，仍可能形成“汇总正确但明细脏”的容量解释偏差。
- `2026-04-25`：完成 `A-200`：已新增只读审计接口 `POST /internal/nodes/{nodeId}/audit-reservation`，返回节点 `reservedCount`、`RESERVED` 明细数、整体一致性和问题列表；审计规则仅把 `SCHEDULED / DISPATCHED` 且任务绑定节点等于预占节点视为合法当前预占，不自动修改任何状态或明细。同步《接口设计》《后端设计》《数据库设计》；通过 `mvn -pl scheduler-service,task-service -am -DskipTests compile` 验证。
- `2026-04-25`：继续沿“预占释放失败后的可观测性和补偿闭环”方向复核，新增 `A-201`：发现 task-service 对 `release-reservation` 失败主要依赖日志告警；离线补偿等静默路径虽然不会回滚已完成状态收口，但调度记录中缺少结构化失败记录，管理员难以从任务调度历史定位容量异常。
- `2026-04-25`：完成 `A-201`：已在 task-service 的预占释放失败处理路径补齐 `schedule_record` 失败审计写入，记录 `taskId/nodeId/FAILED/失败说明`；严格路径记录后继续抛错，静默路径记录失败后继续保持状态收口优先。同步《接口设计》《后端设计》《状态流转校验规则表》；通过 `mvn -pl task-service -am -DskipTests compile` 验证。
- `2026-04-25`：继续沿“调度失败记录和状态历史的一致性”方向复核，新增并完成 `A-202`：发现调度失败记录虽然会落库，但 `NO_AVAILABLE_NODE` 相关场景过于笼统，且 scheduler-service 内部写入 `scheduleMessage` 未统一做 255 字符列宽保护；已细化失败原因短说明并在成功、失败及 `/internal/schedules` 写入路径统一截断。同步《接口设计》《后端设计》《数据库设计》；等待本轮编译验证。
- `2026-04-25`：继续沿“dispatch-failed 与预占释放职责边界”方向复核，新增并完成 `A-203`：发现 task-service `dispatch-failed` 成功回写状态后仍会直接调用 scheduler-service 释放预占，而 scheduler-service `handleDispatchFailure(...)` 收到 ACK 后也会释放同一预占，和“scheduler-service 统一释放派发失败预占”的最新边界不一致。已移除 task-service 该直接释放动作，并同步《接口设计》《后端设计》《任务状态流转实现表》《状态流转校验规则表》；等待本轮编译验证。
- `2026-04-25`：继续沿“node-agent 执行前失败链路”方向复核，新增并完成 `A-204`：确认 node-agent 实现已回调 scheduler-service，但 task-service 控制器和《接口设计》仍残留 node-agent 可直连 task-service、由 task-service 校验 `X-Node-Token` 并释放预占的旧合同。已移除 task-service 可选 token 分支，并同步《接口设计》《状态流转校验规则表》；等待本轮编译验证。
- `2026-04-25`：继续沿“node-agent 已 accepted 后 mark-dispatched 确认失败”方向复核，新增并完成 `A-205`：确认该场景不应立即 `dispatch-failed` 回队，否则可能造成中心状态与节点执行状态分裂；实际补偿依赖 node-agent 后续 `RUNNING` 或执行前失败回调。已将 scheduler-service 的调度失败记录说明细化为“节点已接收但中心确认失败”，并同步《接口设计》《后端设计》《状态流转校验规则表》；等待本轮编译验证。
- `2026-04-25`：继续沿“RUNNING 回传失败后的兜底链路”方向复核，新增并完成 `A-206`：发现 node-agent 在 `status-report(RUNNING)` 重试耗尽后会按执行前失败走 `dispatch-failed(recoverable=true)`；若中心实际已进入 `RUNNING` 但 ACK 丢失，该回队会被拒绝，而 node-agent 随后清理本地任务，可能导致中心长期 `RUNNING`。已增加状态冲突下的 `mark-failed(RUNTIME_ERROR)` 二级兜底，并同步《接口设计》《后端设计》《状态流转校验规则表》；等待本轮编译验证。
- `2026-04-25`：继续沿“运行期终态回传失败闭环”方向复核，新增并完成 `A-207`：发现 node-agent 会把进程异常、网络异常或中心回调异常原样写入 `dispatch-failed.reason` / `mark-failed.failMessage`，若异常文本超过接口和数据库约束的 255 字符，原本用于收口的失败回调会被参数校验拒绝，可能让任务继续停留 `RUNNING` 或派发中间态。已在 node-agent 统一归一化回调失败说明，补默认值并截断到 255 字符；同步《接口设计》《后端设计》《状态流转校验规则表》；等待本轮编译验证。
- `2026-04-25`：继续沿“任务状态与预占明细交叉一致性”方向复核，新增并完成 `A-208`：发现如果 `DISPATCHED -> RUNNING` 时释放预占失败，当前 `RUNNING -> SUCCESS / TIMEOUT / FAILED` 以及节点离线补偿 `RUNNING -> FAILED(NODE_OFFLINE)` 都不会再次尝试释放该残留预占，可能形成“任务已终态但 `node_reservation` 仍为 `RESERVED`”的脏数据。已在 task-service 终态收口和离线补偿的运行态失败路径补齐二次预占释放；释放失败继续记录 `schedule_record` 审计，但不回滚终态。同步《需求分析与系统设计》《接口设计》《后端设计》《任务状态流转实现表》《状态流转校验规则表》；等待本轮编译验证。
- `2026-04-25`：继续沿“节点离线补偿入口前置条件”方向复核，新增并完成 `A-209`：发现正式设计已要求 `/internal/tasks/node-offline/fail` 的 `nodeId` 必须对应当前已标记为 `OFFLINE` 的节点，但 task-service 当前未校验该前置条件，理论上可能被错误调用为在线节点执行离线补偿。已在 task-service 入口前置查询 scheduler-service 节点状态并要求必须为 `OFFLINE`，否则返回冲突；该项为实现追平既有合同，无需再改接口/设计正文。等待本轮编译验证。
- `2026-04-25`：继续沿“节点离线补偿与调度审计一致性”方向复核，新增并完成 `A-210`：发现节点离线补偿虽然会写状态历史并收口 `RUNNING -> FAILED(NODE_OFFLINE)` / `SCHEDULED,DISPATCHED -> QUEUED`，但没有补写结构化 `schedule_record`，管理员在调度时间线上难以直接定位“本轮调度最终因节点离线收口”。已在 task-service 的离线补偿路径中为每条实际变更任务补写 `schedule_record(FAILED)`，保留 `taskId/nodeId/scheduleMessage=node offline ...`；同步《需求分析与系统设计》《接口设计》《后端设计》《任务状态流转实现表》《状态流转校验规则表》。等待本轮编译验证。
- `2026-04-25`：继续沿“求解器成功后的回调链路”方向复核，新增并完成 `A-211`：发现 node-agent 在求解器成功但结果摘要、结果文件或 `mark-finished` 回调持续失败时，会以 `RUNTIME_ERROR` 将任务收口为 `FAILED`，且 task-service 不会清理之前已写入的成功结果元数据，可能让前端看到“失败任务仍带成功结果”。已新增统一失败类型 `CALLBACK_ERROR`，并在 task-service 的 `mark-failed(CALLBACK_ERROR)` 成功落库后同步清理结果摘要与结果文件元数据；同步《需求分析与系统设计》《后端设计》《接口设计》《数据库设计》《任务状态流转实现表》《状态流转校验规则表》。等待本轮编译验证。
- `2026-04-25`：继续沿“CALLBACK_ERROR 清理边界”方向复核，新增并完成 `A-212`：发现 `mark-failed(CALLBACK_ERROR)` 后的结果元数据清理与终态落库同处一条事务链，如果清理异常会把已写入的 `FAILED` 终态一起回滚，和“终态优先收口”的主原则冲突。已改为尽力清理：失败只记录告警，不回滚终态；同步《需求分析与系统设计》《后端设计》《接口设计》《数据库设计》《任务状态流转实现表》《状态流转校验规则表》。等待本轮编译验证。
- `2026-04-25`：继续沿“节点离线补偿与结果元数据一致性”方向复核，新增并完成 `A-213`：发现如果节点在结果回传阶段被判离线，task-service 会将任务从 `RUNNING` 补偿为 `FAILED(NODE_OFFLINE)`，但不会清理此前已写入的结果摘要/结果文件元数据，仍可能形成“失败任务带成功结果”。已在离线补偿的 `RUNNING -> FAILED(NODE_OFFLINE)` 路径补齐结果元数据清理；清理失败只记录告警，不回滚终态。同步《需求分析与系统设计》《后端设计》《接口设计》《数据库设计》《任务状态流转实现表》《状态流转校验规则表》。等待本轮编译验证。
- `2026-04-25`：`A-213` 已通过 `mvn -pl task-service -am -DskipTests compile`；`git diff --check` 已通过，仅存在仓库既有 `LF/CRLF` 行尾提示。
- `2026-04-25`：`A-212` 已通过 `mvn -pl task-service,node-agent,common-lib -am -DskipTests compile`；`git diff --check` 已通过，仅存在仓库既有 `LF/CRLF` 行尾提示。
- `2026-04-25`：本轮变更已统一通过 `mvn -pl task-service,node-agent,scheduler-service -am -DskipTests compile`；`git diff --check` 已通过，仅存在仓库既有 `LF/CRLF` 行尾提示。
